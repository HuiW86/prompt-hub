---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-03
status: active
audience: [ai]
description: M-X.2 MCP server (14 tools, committed da8b682/e58d71a) passed /review — scope CLEAN, no P0/P1 blockers, core concurrency + dedup logic verified correct. 7 P2 findings logged (2 trust-boundary, write-limit asymmetry, mutex-poison, mid-session schema, list_compositions token amp). Awaiting omar's fix-scope decision (A/B/C/D) before touching code. Doc ripple (CLAUDE.md §7 + features §3.7) still pending.
related:
  - CLAUDE
  - mcp-write-pipeline
  - 06-prd
  - 015-expose-mcp-write-pipeline
---

# Handoff — M-X.2 /review 收口（findings + 待决策）

<!-- Generated by /checkpoint at 2026-06-03 17:40 -->

## Objective

对 M-X.2 commit（`da8b682` / `e58d71a`，14 tool MCP server）跑 `/review`，在进 M-X.3 UI 前确认数据层 + MCP 层没有结构性问题。

## Completed

- **`/review` 全跑完**（结构化 pass + 独立对抗 subagent）：scope **CLEAN**（commit 就是 M-X.2 范围，无 drift），**无 P0/P1 阻塞**。
- **核验通过的正确点**：去重靠 `idx_drafts_hash_pending` 唯一索引 + 捕获约束冲突（race-safe，`draft_repo.rs:71`）；无锁跨 `.await`；`tool_name` 服务端盖章防伪造（`tools.rs:154`）；截断 char 边界安全；in-batch 去重正确（e2e 覆盖）。
- **撤回一个误判**：`import_json` 配额在 size-check 之前扣是**有意反滥用**顺序，非 bug。

## In Progress

- 无代码改动。working tree clean。等 omar 选 fix 范围后再动手。

## Next Actions

1. **等 omar 决策**：A) 只补两条信任边界校验 / B) A + Mutex 恢复 / C) 全部 P2 / D) 先不改。
2. **若 A**（最便宜、最该补，符合 plan §5.4）：
   - `tools.rs:154` `into_provenance` — `confidence` clamp `0.0..=1.0` + 拒绝非有限值（NaN/Inf 会被 serde 序列化成 `null` 污染审计）。
   - `tools.rs:28` `PayloadArg::from`（或 tool 边界）— 拒绝 `schema_version != latest`（现仅 v1）。
3. **doc ripple 回写**（§7 八步收尾，独立于上面）：`CLAUDE.md §7` 状态指针（M-X.2 已 review）+ `docs/design/07-features.md §3.7` prompt-hub-mcp `in-progress` → `done`。
4. **之后进 M-X.3 UI 收件箱**：Scene 草稿 tab + 顶部 badge + `promote_draft`/`update_draft`/`discard_draft` Tauri IPC（`src-tauri/src/commands.rs`，promote 入参含 `group_kind?` 对齐 PRD §10.3）。

## Risks & Decisions

P2 findings 全表（按价值排序，详细见会话）：
- **[信任边界]** `tools.rs:148` `confidence: f32` 无范围/有限性校验（conf 9）。
- **[信任边界]** `tools.rs:31+` `schema_version` 接受任意 u32（conf 7）。
- **[限额不对称]** `server.rs:245` `create_draft`/`update_draft`/`save_conversation`/`bootstrap` 无 payload size cap（`import_json` 有 64KB）；限流只管 `import_json`，单写路径无限——runaway loop 可绕过所有 import 加固（conf 7-8）。注：250MB DB guard 已按 plan defer 到 M-X.4，但单写 cap 是 plan 未覆盖缺口。
- **[鲁棒性]** `server.rs` 全部 `lock().expect("db mutex poisoned")`：任一 handler 持锁 panic → 14 tool 全部永久 panic（panic 可达性低）。修：`lock().unwrap_or_else(|e| e.into_inner())`（conf 6）。
- **[鲁棒性]** `db.rs:96` schema 只启动校验；app 中途迁移后长连接写陈旧 schema。修：每写 tool 起头 re-check `user_version`（conf 7）。
- **[token 放大]** `server.rs:584` `list_compositions` 返全表全字段（modifiers/macros 都投影成 preview brief）；3 个 read tool 在 Rust 过滤而非 SQL（本地小数据可接受）（conf 6）。
- **[可选]** `open_write_checked` 假定 WAL 未断言；DB 文件中途删无重连；`unchecked_transaction` 依赖 drop 回滚（today fine）。

建议：A 两条 + Mutex 恢复现在补；`create_draft` size-cap + mid-session schema recheck 记 TODO 进 M-X.3/M-X.4。

## Verify

```bash
cd /Users/apple/WeChatProjects/prompt-hub/src-tauri
cargo test --workspace                                  # 期望 41/41
cargo clippy --workspace --all-targets -- -D warnings  # 0 警告
cargo fmt --check                                       # clean
git log --oneline -1                                    # 本次 checkpoint（仅 HANDOFF）
```

## Modified Files

- 无源码改动。本次仅刷新 `HANDOFF.md`（review 结论 + 待决策留痕）。
