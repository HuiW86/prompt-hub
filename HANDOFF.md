---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-05
status: active
audience: [ai]
description: asset-editing 收尾验收 workflow S1-S4 收口。S1 分隔条键盘 focus 经全量重启实机 ✓；S2 跑 bench 时发现 auto-cycle 版（bench.rs）在 tokio worker 线程直调 AppKit 违反主线程约束、重构后从未跑通，修复（run_on_main_thread 派发）后首跑 P95=14.696ms；S3 复核 clippy/fmt clean；S4 回流 learnings v0.3 / plan v0.4 / 本文件。唯一悬留：#3/#4 ModifierGrid/Composition UI 落点（等 omar）。
related:
  - CLAUDE
  - asset-editing-and-adaptive-layout
  - 014-nspanel-isa-swizzle
  - 012-lock-visual-quality-anchor
---

# Handoff — asset-editing 收尾验收 workflow（S1-S4 收口 + bench 主线程 bug 修复）

<!-- Generated by /checkpoint at 2026-06-05 -->

## Objective

跑收尾验收 workflow（门控线性 S1→S4），把 asset-editing 两项诚实悬留清零：① 分隔条键盘 focus 实机；② bench 留痕。#3/#4 落点排除在外（等 omar）。

## Completed（本会话）

- **S1 键盘 focus 手测 ✓**：先全量重启冷启动（杜绝 HMR 中间态误判）→ 原生窗口 Tab 聚焦 PanoramaSeparator + ←→ 调宽两项均过。
- **S2 bench 留痕 + 真 bug 修复**：跑 `bench:hotkey-wake` 时 SIGTRAP 崩溃，crash report 定位 `tokio-rt-worker` 线程违反「Must only be used from the main thread」。根因：`bench.rs` auto-cycle 在 worker 线程直调 `show()`/`order_front`（违反 macos.rs 注释 + lib.rs:89 已有正确范式）——重构后从未在 macOS 跑通过，HANDOFF 旧 baseline 10.49ms 实为 M0-3 inline 版数字。修复：wake/hide 经 `run_on_main_thread` 派发、timing 在主线程闭包内测。修复后首跑 **P95=14.696ms**（+OS dispatch ~10ms ≈ 25ms，远低于 C1 200ms）。
- **S3 复核 ✓**：bench.rs diff 正确（镜像生产范式）；`cargo clippy --features bench` clean + `cargo fmt --check` clean + 编译过。
- **S4 收口**：回流 learnings **v0.3**（信条三 ③ 键盘 focus 转 ✓ / 信条四 bench bug 自指注脚 / 附录 macOS run_on_main_thread）+ plan **v0.4**（§6 去「待补」/ §7 加第 5 条 bench 留痕）+ 本文件精简。

## Next Actions

1. **commit 本会话改动**：`bench.rs` + `learnings.md` + plan + HANDOFF（feat/fix 混合，建议拆 `fix(bench):` 主线程修复 + `docs:` 回流，或单条 checkpoint）。
2. **（阻塞，等 omar）#3/#4 ModifierGrid/Composition UI 落点**：后端 `order_index` 全链路已 done，编辑 UI 主界面落点未拍板。⚠️ 建 Composition 工作台时须复检 [[02-constitution#B2]] 自检清单 ①。
3. **（可选）回归 `bench:cold-start`**：本次只跑了 hotkey-wake；cold-start 另有 subprocess + CGWindow probe 路径，未验。

## Risks & Decisions

- **bench auto-cycle 修复仅 macOS 实跑验证**：非 macOS 分支（`set_focus`）未跑，但同样经 run_on_main_thread 派发、逻辑对称，低风险。
- **CLAUDE.md §7 仍写「M0-3 P95=10.49ms」**：那是 inline 历史真值，与 auto-cycle 14.696ms 方法不同——**并列说明、勿覆盖**；本次未改 CLAUDE.md（非文档八步范围），下次顾问/状态更新时可注脚。
- **#3/#4 暂缓**（omar 主动搁置）：非不可逆、未开 ADR，留痕 plan §7 #1。
- 无 remote——checkpoint 仅本地提交。

## Verify

- `cargo build --manifest-path src-tauri/Cargo.toml --features bench`（bench 二进制编译）
- 前台 `! pnpm bench:hotkey-wake`（auto-cycle P95 ≤200ms，上次 14.696ms）
- `cargo clippy --workspace --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings` + `cargo fmt --manifest-path src-tauri/Cargo.toml --check`
- `pnpm exec prettier --check docs HANDOFF.md`（文档格式）

## Modified Files (this session)

- `src-tauri/src/bench.rs`（主线程 bug 修复：run_on_main_thread 派发 + channel 回传 timing）
- `docs/learnings.md`（v0.3：信条三/四回流 + 附录 macOS 主线程约束）
- `docs/plans/asset-editing-and-adaptive-layout.md`（v0.4：§6/§7 验收回流）
- `HANDOFF.md`（本文件，精简 + 状态更新）
