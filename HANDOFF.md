# Handoff — 承载量批次落地（紧凑密度档 + 话术卡标题化）；omar 走查中，v2+密度两分支推送/验收/并回待收
<!-- Generated by /checkpoint at 2026-07-23 00:59 -->

## Objective
omar 反馈「一屏承载内容不够多」：新分支 `reshape/density-compact`（自 `reshape/ui-v2` 切出，stack 关系）落地承载量批次——紧凑密度档（结构 token 收紧、字号不动）+ Scene 话术卡静息态标题-only。主线不变：真机全量验收 → 推送 → 并回 main。

## Completed
- **紧凑密度档**（`f190002`，verifier PASS）：tokens.css §3c `:root.compact` 收紧 9 锚点高度 + 2 子网格间距（`--s-3_5` 首版 10px 经 omar 真机反馈回调 12px）；settingsStore 新增 `density`（默认 comfortable=v2 基线像素不变；root 类 + localStorage 同 theme 机制，旧 persisted 免 version bump）；SettingsModal 外观页密度段控件；新增 `src/styles/density-gate.test.ts`（覆盖项必须为 base 已有 token + 严格更小 px + 禁 `--t-*`/`--lh-*`，verifier 4 变异实测全红）
- **话术卡标题-only**（`66933e7`，omar 2026-07-23 追加裁决）：`ScenePanel.module.css` `.phraseContent` 静息 `display:none`，整理态整卡点击展开为内容唯一上屏时刻（D-0 保留强化）；不分密度档生效
- 测试 324→330（34 文件）/ lint / prettier / build 全绿；CHANGELOG 两条目；verifier 抓 §3c 注释与 `--s-*` 实际作用域不符已修正
- dev server 已在密度分支重启；曾误 `git add -A` 扫入 `.codex/`+`AGENTS.md`，已剔除恢复未跟踪

## In Progress
- omar 实时走查 title-only 卡 + 紧凑档观感（dev 运行中，CSS 热更即时可见）

## Next Actions
1. **接 omar 走查反馈**：卡太秃/展开手感 → 调 `ScenePanel.module.css` `.phraseContent`；紧凑档仍挤 → 调 tokens.css §3c 值
2. **推送 origin**：`reshape/ui-v2`（领先 4 commit）+ `reshape/density-compact`（领先 2）(carried from 2026-07-22，扩)
3. **真机全量验收**（1024×640）：原清单（驾驶舱布局 + 整舱换色 + verifier NEEDS HUMAN 4 项 + scrim 45% + 批次 A/B + 浅色 × 五 accent 走查）+ 新增：紧凑档 × 两模式（macro tile 44px 截断 / 设置弹窗与搜索浮层收紧观感 / 一屏承载计数）+ title-only 卡两态 (carried from 2026-07-21，扩)
4. 验收后并回 main：`reshape/ui-v2` 先 `merge --no-ff`，`reshape/density-compact` 随后（stack）(carried from 2026-07-21，扩)
5. 契约回流八步：product-spec §4.0/§13.4 + §4.0.7 卡片解剖（title-only）+ 外观设置（density）；design-spec §2/§8/§9 + §3c token 层（light §1d 色值随批入）(carried from 2026-07-21，扩)
6. omar 人审批次：design-spec v0.15 draft + ADR-023/024 措辞 + 图标定稿追认 + product-spec v0.15/v0.16 + features v1.10/v1.11 + 01-spec v0.7 + 旧账 ADR-021/scene.color (carried from 2026-07-06)
7. 补焦点恢复负路径测试（`src/components/__tests__/ScenePanelFocusRestore.test.tsx`）(carried from 2026-07-21)
8. bench-c1 `continue-on-error` 处置 omar 复核 (carried from 2026-07-12)
9. 上会话 3 commit（`a24c7c0`/`4b722c1`/`716bd4c`）补 verifier (carried from 2026-07-07)
10. **可发现性裁决 ×4**（+1）：hover 动作簇微显 / 场景删除入口外露 / 整理态保窗 vs 改 D-0 契约二选一 / **新增：title-only 后调用态卡面无内容线索（看内容只剩编辑入口），误复制风险与内容可发现性**——涉 product-spec §4.0.7，改契约走八步 (carried from 2026-07-06，扩)
11. P0-2 Composition 链路 ADR（P0-5 系于此，`DraftInbox.tsx:43-48`）(carried from 2026-07-06)
12. v0.1.0 交付余量：落盘日志（tauri-plugin-log 立项）/ 安装·快捷键·FAQ 文档 (carried from 2026-07-06)
13. tag dry-run 验 `.dmg` + ADR-017 Phase 6 updater 真机验收（仓库零 release，首发即激活更新；顺带验新图标 .app/Dock 观感）(carried from 2026-07-02/06)
14. P2 余 4 评估 / ops-spec §3 定时备份（`repo-core/src/backup.rs` 底座）/ P1-5 Phase 可配置性 / Macro 网格末行 auto-fit (carried from 2026-07-06)
15. CLAUDE-DESIGN v0.2 重传 + v2 基调同步 / v0.1.0 发布时机（omar 外部依赖）(carried from 2026-07-02)
16. 评审遗留补 design-spec 上游：ipc-contract 扩扫 / `--color-danger` / `--scrim` 语义回流（随人审八步）(carried from 2026-07-02)
17. PRD §6.1 soft-delete 矛盾 + `status: pre-code` 僵尸 / ai-dev-lifecycle 仓收尾 (carried from 2026-07-02/06)
18. cargo test 计数口径核对（verifier 报 94+3 vs 旧账 155）(carried from 2026-07-21)

## Risks & Decisions
- **密度档 token 值 + title-only 卡形态均为授权代笔 / omar 口头裁决**，真机走查（Next 1/3）通过前不定稿、不入对外发布说明；卡片解剖与外观设置契约未回流，文档-代码短暂失配（pending 八步，挂 Next 5）
- **title-only 副作用**：调用态卡面零内容线索（无悬停预览），误复制风险 → 并入 Next 10 可发现性裁决，不就地改行为
- verifier NEEDS HUMAN：`--s-3_5`/`--s-2_5` 收紧同时波及设置弹窗/搜索浮层（by design 已如实注释，观感待人眼）
- dev 裸二进制 WebKit 存储在 `~/Library/WebKit/prompt-hub`，正式 .app 在 `dev.prompt-hub`，两套设置互不相通 (carried)
- HMR 后 store 驱动行为异常先重启 dev 进程（Zustand 旧实例）(carried)
- `.codex/` / `AGENTS.md` 归 omar，保持未跟踪 (carried)

## Verify
- `pnpm test` → 330/330（34 文件，含 density-gate 3 例）
- `pnpm lint` && `pnpm exec prettier --check .` && `pnpm build`
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml` → 0 failed（本批 Rust 零 diff）
- `cargo clippy --workspace --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings`

## Modified Files
- `f190002`：`src/styles/tokens.css`（§3c）/ `src/stores/settingsStore.ts` / `src/components/SettingsModal.tsx` / `src/styles/density-gate.test.ts`（新增）/ 两测试文件 / `docs/design/CHANGELOG.md`
- `66933e7`：`src/components/ScenePanel.module.css` / `src/components/scene/ViewPhraseCard.tsx` / `docs/design/CHANGELOG.md`
- 本 checkpoint：`HANDOFF.md` / `docs/.metrics/cochange.log`
