---
type: handoff
project: prompt-hub
created: 2026-05-19
updated: 2026-05-19
status: active
audience: [ai]
description: prompt-hub 会话断点——doc audit 8 项修复完成 + 上游 tech-stack v1.0 涟漪已并入 git
related:
  - CLAUDE
  - plan
  - tech-stack
---

# Handoff — doc audit fixes + frontmatter author fields
<!-- Generated by /checkpoint at 2026-05-19 -->

## Objective

把 prompt-hub 文档体系从「13 文档 + 9 ADR 全部落盘但未审计」推进到「全量审计 + 真问题修复 + frontmatter 主笔人字段机器可校验」状态，为下一步建仓提供干净文档基线。

上游遗留目标（来自上一会话）：把 prompt-hub 从「ADR 全决议但设计文档未涟漪」推进到「全栈拍板 + 第一阶段 MVP 可立即建仓」。**仍待执行**。

## Completed

### 本会话（commit 52421a3）

**审计**：手动跑等价 docaudit（/docaudit skill 因 API schema bug 不可用），扫描 13 主文档 + 9 ADR + HANDOFF 的索引/链接/编号/版本/铁律落地/主笔人分工 7 维度。

**修复**（15 文件改动，3 假阳性后剩 8 项真问题）：
- `CLAUDE.md:139` 删除"已解锁"列错误的 `D7（测试栈）/`，保留正确的 D7=quick-shortcut plugin / D11=测试栈
- `CLAUDE.md` 加 §5.4 Anchor 命名约定（明文承认 §N-XX 复合 anchor 是项目约定，不强求机器跳转）
- `CLAUDE.md` frontmatter 加 `status: ratified` + `author: co`
- `user-flows.md:40` `[[constitution#A4 推论]]` → `[[constitution#A2]] 本地优先推论——单本地后端，双形态共享`
- `adr/007:22` `[[features#测试覆盖列]]` → `[[features#§5]]`
- `adr/005` (5 处) 列表项自指 wikilink `[[CLAUDE#§6 忌讳清单 #8]]` / `[[CLAUDE#§6-#8]]` → `[[CLAUDE#§6]] 第 8 项`
- 13 主文档全部补齐 frontmatter `status` + `author` 字段（spec=human / constitution=human / CLAUDE/user-flows/product-spec/design-spec/plan=co / prd/tech-stack/features/sitemap/test-spec/ops-spec=ai）

### 上游会话（commit be879e2）

- tech-stack v1.0 ratified（[[tech-stack]]）
- ADR-001~004/006~009 Accepted；ADR-005 Proposed 占位
- 设计文档涟漪完成

## In Progress

无——本会话目标全部交付，已 commit。

## Next Actions

### 优先级 P0 — 推进项目阶段

1. **建仓第一阶段 MVP**（per [[plan#§0]] + [[plan#第一阶段]]）
   - 跑 `pnpm create tauri-app prompt-hub --template react-ts`（[[CLAUDE#§2]] 命令列表）
   - 落实 [[plan#§0-T1]] 设计 Token 全量初始化
   - 落实 [[plan#§0-T5]] 全栈技术形态锁定 → `package.json` + `Cargo.toml` 版本号严格按 [[tech-stack#§3]] 锁定
   - 实现 200ms 主形态唤起 benchmark（[[constitution#C1]]）
2. **或推进 ADR-005**：等 omar 提供 prompt-combiner 仓库 → 跑技术调研 → ADR-005 Proposed → Accepted（[[adr/005-prompt-combiner-reuse]]）

### 优先级 P1 — 审计方法论改进

3. **保存审计教训到 memory**（feedback 类型）：审计时下结论前必须 grep 验证文档实际文本，避免脑补字符串再"发现"自己的 bug。本会话假阳性率 25%（3/12）

### 优先级 P2 — 可选 polish

4. 考虑给 prd.md（1164 行）拆出 `prd-data-model.md`（§6 单独 ~400 行）
5. adr/000-template 添加"已使用本模板的 ADR"反向索引（小价值）

## Risks & Decisions

- **AI 改动 spec.md frontmatter**：spec.md 是 🧑 人主笔（[[CLAUDE#§5.2]]）。本会话 AI 加了 `status: pre-code` + `author: human` 两个机械字段，未改正文。若 omar 认为这违反"AI 禁起草人主笔文档"，可 revert spec.md 这两行并手动加。
- **审计假阳性**：3/12 findings 是 AI 脑补——TOP 5 #1 (CLAUDE C2/A3) 和 #4 (adr/003 spec#8.2) 及 D 节 spec 拆分导航说明，下次审计必须先 grep 验证才下结论。
- **/docaudit skill 不可用**：API 400 input_schema 报错（top-level oneOf/allOf/anyOf 不支持）。已绕过用手动审计完成等价工作。skill 本身需要修。
- **ADR-005 仍 Proposed**：不阻塞第一阶段 MVP 建仓（可走"重写"路径，[[tech-stack#§3-D8]]），仅影响代码迁移节奏。

## Verify

```bash
# 当前 git 状态
git log --oneline -3  # 应见 52421a3 doc audit fixes + be879e2 tech-stack ripple

# 验证修复
grep -c "author:" /Users/apple/Desktop/人机协作/prompt-hub/*.md  # 应 ≥13
grep -rn "constitution#A4\|CLAUDE#§6 忌讳" /Users/apple/Desktop/人机协作/prompt-hub/  # 应无结果

# 验证 anchor convention 已落
grep -A2 "5.4 Anchor" /Users/apple/Desktop/人机协作/prompt-hub/CLAUDE.md
```

## Modified Files

### 本会话 commit 52421a3
- `CLAUDE.md`（D7 矛盾修复 + §5.4 新增 + frontmatter status/author）
- `adr/005-prompt-combiner-reuse.md`（5 处列表项 wikilink 改纯文字）
- `adr/007-choose-test-stack.md`（features#§5 anchor 修复）
- `constitution.md`（frontmatter author: human）
- `tech-stack.md` / `features.md` / `sitemap.md` / `test-spec.md` / `ops-spec.md`（frontmatter author: ai）
- `spec.md` / `prd.md` / `product-spec.md` / `design-spec.md` / `plan.md`（frontmatter status + author）
- `user-flows.md`（A4 anchor 修复 + frontmatter author: co）

### HANDOFF.md（独立 amend）
- 本文件，新增 frontmatter + 重写为本会话断点
