---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-17
status: active
audience: [ai]
description: ADR-017 自动更新已转 Accepted（隐私披露 ratification gate 已就绪：ops-spec §9 v0.2 + prd §7.3/N1 v0.9）。下一步进 §6 实现落地，C3/C4 + 功能涟漪留待实现后。
related:
  - CLAUDE
  - 017-enable-auto-update
  - 10-ops-spec
  - 06-prd
---

# Handoff — ADR-017 自动更新（已 Accepted，待进实现）

<!-- Generated by /checkpoint at 2026-06-17 -->

## Objective
给应用加自动更新能力（`tauri-plugin-updater` + GitHub Releases + GitHub Actions 自动出包，mac 先行预留全平台）。本轮：补齐 ADR-017 §5.1 的隐私披露 ratification gate，经人审批准后把 ADR 转 Accepted。

## Completed
- **隐私披露 gate 文档已起草**（1+2 组合落点）：
  - `10-ops-spec.md` v0.1→v0.2：新增 **§9 出站网络与隐私披露**——唯一出站场景 / 协议层被动元数据（IP+时间戳·SNI·JA3·UA 覆盖为 `prompt-hub-updater`）/ 节律与开关（首启 opt-in + 总开关零出站 + 低频去节律）/ 与 §5.2 禁用监控正交
  - `06-prd.md` v0.8→v0.9：C1 §7.3 加 updater 唯一出站例外指针；C2 §8.2 N1 override 列开「唯一显式声明的受限豁免」口子，消除 prd 字面对 updater 的否决
  - `CHANGELOG.md` + `MANIFEST.md`：版本回写（顺带修正 MANIFEST prd v0.7→实际 drift）
- **ADR-017 转 Accepted**（2026-06-17，Hui Wang 人审批准）：frontmatter / 头部 / 决策者 / §2 Status 四处一致，gate 就绪状态已标注

## In Progress
- 无代码在写。ADR 已 Accepted，等进实现

## Next Actions
1. **进实现（§6 落地清单逐项核对）**：关键新增——写进现有 `src-tauri/capabilities/default.json`（**非** main.json）放行 `updater:*` + `process:default`（#2273 relaunch 权限）/ 签名拆**独立 job**（build≠sign）/ `latest.json` 预留强制最低版本字段 / Vite `envPrefix` 锁 `['VITE_']` + TAURI_* 白名单（禁 `TAURI_SIGNING_*` 进前端）/ 后台 `tauri::async_runtime::spawn` + 首启 opt-in / `.github/workflows/release.yml` action pin SHA + 自动 provenance 断言（tag↔manifest↔artifact）
2. **C3/C4 涟漪（实现时同批）**：`06-prd` §8.3 L2「关闭网络权限」描述补 updater 例外 / `10-ops-spec` §5.2 telemetry 措辞澄清（§9.4 已铺垫）
3. **功能涟漪（实现后，方法论 §7）**：`07-features` 加自动更新功能项 / `09-tech-stack` 登记 updater+process+dialog 依赖 + Rust toolchain ≥1.77.2 / `CLAUDE.md §7` 状态指针（ADR 进度 14→15 Accepted，017 入列）
4. **可选（人主笔）**：`02-constitution` A2 加 ADR-017 指针 note（已备文本，见上次会话）
5. （遗留）主形态窗口屏幕适配**真机验收**仍未做：`pnpm tauri dev` 多屏+改分辨率点 ⌥Space 验 re-fit/不崩（#15170 fix）

## Risks & Decisions
- ADR-017 已 Accepted——A2 从「零出站」正式进入「受限豁免」状态，披露文档（ops-spec §9）是豁免链最后一环，已就位
- **外部前置**：rollback kill-switch、签名 job 隔离、latest.json 强制最低版本字段，落地实现时才可验真
- C3/C4 + 功能涟漪按拍板 defer 到实现阶段，勿在 updater 未落地时回写「已有自动更新功能」（会造成 features/tech-stack 与代码 drift）
- **外部依赖**：Claude Design pipeline 待 omar 上传 [[CLAUDE-DESIGN]] 跑 Task β

## Verify
- `git log --oneline -1`（确认本次 checkpoint 落地）
- 实现阶段：`cargo test --workspace --manifest-path src-tauri/Cargo.toml`
- 实现阶段：`pnpm bench:hotkey-wake`（验更新检查后台 spawn 不拖慢 ⌥Space 唤起，守 C1 200ms）

## Modified Files
- `docs/design/10-ops-spec.md`（新增 §9 + bump v0.2）
- `docs/design/06-prd.md`（§7.3 C1 + N1 C2 + bump v0.9）
- `docs/adr/017-enable-auto-update.md`（status Proposed→Accepted 四处 + §5.1 gate 就绪标注）
- `docs/design/CHANGELOG.md`（2026-06-17 条目）
- `docs/MANIFEST.md`（ops-spec→v0.2 / prd→v0.9）
