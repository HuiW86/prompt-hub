---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-05
status: active
audience: [ai]
description: asset-editing P3 启动。P1.5 NSPanel 键盘拖动 spike 经 omar 实测证伪「键盘 sensor 未验证」风险（Tab→Space→↑↓ 在 key window 生效）。P3a.3 AlignmentPhrase 编辑面编码完成（编辑态 toggle：常态保持 44px 只读 chip 行 + 行末「✎ 管理」入口，点击展开纵向管理列表，dnd 排序 + 改名/改内容/删除 + 新增，协议层紫配色）。全验证绿（test 73 / lint / prettier / build）。已 checkpoint 4c9c0ea。关键架构发现：product-spec §13 主形态无 Modifier/Composition 展示区 → #3/#4 挂起待落点决策。剩 #6 手测、#7 B2 自检、P3b 自适应、P4 分隔条。
related:
  - CLAUDE
  - asset-editing-and-adaptive-layout
  - 03-product-spec
  - 05-design-spec
  - 016-choose-dnd-and-resizable-layout
  - 014-nspanel-isa-swizzle
---

# Handoff — asset-editing P3 启动（P1.5 spike 通过 + P3a.3 AlignmentPhrase 编辑面编码完成）

<!-- Generated by /checkpoint at 2026-06-05 02:10 -->

## Objective

执行 [[asset-editing-and-adaptive-layout]] **P3**：4 类资产编辑 UI + 内容自适应布局。
本会话把 plan 的「步骤列表」升级为带验收关卡的 workflow（7 个 TaskCreate 任务），
并以 **P1.5 NSPanel 键盘拖动 spike** 作前置解锁，再推进 P3a 编辑 UI。串行顺序：
阶段0 spike → 阶段1 三类编辑 UI（#3/#4/#6 + #7 B2 自检）→ 阶段2 P3b 自适应 → 阶段3 P4 分隔条。

## Completed (this session)

- **P1.5 NSPanel 键盘拖动 spike（#5 done）**：代码层逐环验证链路无缺口——`KeyablePanel`
  override `canBecomeKeyWindow→true` + `makeKeyWindow()` + `makeFirstResponder(ns_view)` →
  WKWebView 收键盘；`@dnd-kit/dom@0.4.0` `defaultPreset.sensors=[PointerSensor,KeyboardSensor]`
  （index.js:2176）默认白送键盘 sensor，绑 handle（`<button>` 可 Tab 聚焦），Space 激活/方向键移动/
  Space·Enter·Tab 结束（index.js:1626-1630）。**omar 实测确认生效** → 阶段1 做全功能（含键盘拖动）。
- **前置核实**：ADR-016 = Accepted，三依赖全已装（`@dnd-kit/react@0.4.0`/`helpers@0.4.0`/
  `react-resizable-panels@4.11.2`）→ P4 无依赖阻塞；research 已证伪「v4 仅百分比」，px↔% 换算 util 可省。
- **P3a.3 AlignmentPhrase 编辑面编码（#6，待手测）**：`AlignmentPhrases.tsx` 全重写——
  **编辑态 toggle** 设计：常态保持 44px 只读 chip 行（点击复制不变）+ 行末「✎ 管理」入口；
  点击 `setEditMode(true)` 展开 `.editRegion` 纵向管理列表（脱离 44px 约束）。编辑态含
  `DragDropProvider` + `SortablePhraseRow`（handle/dot/name/「默认」badge/改名/删除二次确认）+
  `PhraseEditor`（name+content 表单，create/edit 复用 MacroGrid 模式）+「新增」「完成」按钮。
  切相位 `useEffect` 重置编辑态（避免编辑不可见的旧 phase）。删默认话术 catch 后端拒绝 → toast。
- **CSS（协议层紫，禁 --task）**：`AlignmentPhrases.module.css` 追加 ~230 行——`.manageBtn`/
  `.editRegion`/`.editHeader`/`.addBtn`/`.doneBtn`/`.list`/`.row`/`.rowHandle`/`.rowBadge`/
  `.actBtn`/`.confirm*`/`.editor*`，semantic 全用 `--protocol`(-8/-16/-fg) family（design-spec
  §633「chip 用绿=违宪」），focused outline `--protocol`。token 全部核验存在，无裸 px/hex/ms。
- **验证全绿**：`pnpm test` 73（无回归）/ lint clean / prettier 已 format / `pnpm build` ✓
  （tsc + vite，1636 modules）。已 checkpoint `4c9c0ea`。

## In Progress

- **#6 AlignmentPhrase 编辑面**：编码 + 自动验证完成，**待 omar 手测 5 步**（Done 定义未达成）。
  dev server 后台运行中（bg ID `bobl44gz7`，端口 1420，已 HMR 热更新）。

## Next Actions

1. **#6 手测验收**（dev 已跑）：⌥Space 唤起 → ⌘1-8 切到有话术的相位 → 点 chip 行末「✎ 管理」→
   测：新增 / 改名 / 改内容 / 拖序（指针+键盘 Tab·Space·↑↓）/ 删除二次确认 / 删默认话术应被后端拒。
   通过则 `TaskUpdate #6 completed`。
2. **#3 ModifierGrid / #4 Composition 区落点决策**（阻塞）：product-spec §13 主形态 7 区无二者展示区，
   spec 定位为辅形态/配置面板（`⌘,`）。omar 已选「先做 AlignmentPhrase」，落点仍待定——
   二选一：建 `⌘,` 配置面板外壳放二者编辑面（遵 spec，工作量大）vs 主形态加区域 8/9 走方法论 §7
   回流 §13（改 spec，可能触 ADR-012 视觉锚）。决策后才能解挂 #3/#4。
3. **#7 B2 物理分离自检**：3 项清单（AlignmentPhrase 不进 Composition/Macro 工作台、SOP 不引、
   Macro 区不展示 AlignmentPhrase）。本会话编辑态仍限定在区域 2-bis，未渗入，但需正式过清单。
4. **P3b 自适应布局（#1）**：`Dashboard.module.css` grid 改 minmax/auto + 空态，screencapture 验收。
5. **P4 可拖分隔条（#2）**：依赖已就绪，P3 视觉验收后上。
6. **文档涟漪**：learnings.md L66「键盘 sensor + resize-handle focus 未实机验证」中键盘 sensor 已被
   证伪，待回流（resize-handle focus 仍待 P4 验）。

## Risks & Decisions

- **#3/#4 落点未决（最高优先级阻塞）**——见 Next Actions 2。做错会违 product-spec §13「主形态克制
  80% 高频」或触 B2。omar 已确认绕开先做 #6，但二者最终落点必须拍板才能继续阶段1收口。
- **编辑态 toggle 是实现决策**（非 spec 规定）——chip 行 24px 太窄塞不下常驻 inline actions，故选
  toggle。若 omar 手测后想要别的形态（如浮层/侧栏），需返工 `AlignmentPhrases.tsx`。
- **删默认话术依赖后端拒绝**——前端只 catch + toast，保护逻辑在 P2b 后端（`delete_alignment_phrase`
  对 phase default 返错）。手测须覆盖此路径确认后端真的拒。
- **dnd 竞态防护**沿用 MacroGrid 模式：本地 `items` 为拖动期唯一渲染源，`onDragEnd` 才写回 store，
  `event.canceled` 回弹；切相位 useEffect 重置。无 isDragging 显式门控（拖动中 store 不 refetch，同 MacroGrid）。
- 无 remote——checkpoint 仅本地提交（`git remote` 空）。

## Verify

- `pnpm test`（73 passed）
- `pnpm lint` · `pnpm exec prettier --check .`
- `pnpm build`（tsc + vite）
- 本会话未改 Rust，`cargo test --workspace` 无需重跑（前序 82 passed 仍有效）

## Modified Files (this session)

- `src/components/AlignmentPhrases.tsx`（全重写：编辑态 toggle + dnd + editor）
- `src/components/AlignmentPhrases.module.css`（追加 ~230 行编辑态样式，协议层紫）
