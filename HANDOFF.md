---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-05
status: active
audience: [ai]
description: 收尾 asset-editing 剩余任务的 workflow（A→B→C→D，#3/#4 落点决策暂缓）。A #7 B2 物理分离自检全过（clean）。B P3 走 X 方案（列机制交 P4，空态已就位免新代码）。C P4 可拖分隔条已编码：panoramaGrid 3 列重构为 react-resizable-panels v4 Group/Panel/Separator + useDefaultLayout localStorage 持久化，全百分比免 px↔% 换算；代码层全绿（test 73 / lint / prettier / build）。⚠️ 但用户在真实 Tauri webview 看到运行时报错并点了重启，报错原文未取得——当前 working state 可能不可用，需先诊断（或 git revert a347d17 回退）。D 文档涟漪未动。
related:
  - CLAUDE
  - asset-editing-and-adaptive-layout
  - 016-choose-dnd-and-resizable-layout
  - Dashboard
---

# Handoff — asset-editing 收尾 workflow（P4 分隔条运行时报错待诊断）

<!-- Generated by /checkpoint at 2026-06-05 03:20 -->

## Objective

收尾 [[asset-editing-and-adaptive-layout]] 剩余任务，分 4 phase 执行（#3/#4 ModifierGrid/Composition 落点决策**暂缓**，由用户拍板）：
- **Phase A** = #7 B2 物理分离自检
- **Phase B** = P3 内容自适应布局
- **Phase C** = P4 可拖分隔条
- **Phase D** = 文档涟漪收口（方法论 §7）

task 列表见会话 TaskList（#1 A done / #2 B done / #3 C in_progress / #4 D pending）。

## Completed

- **Phase A #7 B2 物理分离自检（clean，无代码）**：3 项全过——① `MacroGrid.tsx` 零 AlignmentPhrase 引用 + Composition 工作台尚未建（#4 暂缓）故 trivially 成立 ② `SopProgress.tsx` 零引用 ③ Macro 区零展示。AlignmentPhrase 仅现身合法三处：专属区 `AlignmentPhrases.tsx` / 全局搜索 `SearchBar`+`SearchOverlay` / 草稿箱 `DraftInbox.tsx`（promote 目标类型标签）。
- **Phase B P3（X 方案，无新代码）**：与用户确认 **X 方案**——列机制交 P4（放弃「空列自动收缩」，因与持久化拖拽互斥），改「默认比例 + 可拖 + 持久化」。各区空态已就位（MacroGrid/ScenePanel/RecentList/AlignmentPhrases），SopProgress 是设计占位，行高已 `auto` 自适应 → P3 代码交付已被既有工作满足。
- **Phase C P4 编码（代码层全绿，运行时未验）**：
  - `src/layouts/Dashboard.tsx`：引 `Group/Panel/Separator/useDefaultLayout`，`panoramaGrid` div 重构为 `Group`（3 Panel：macro/scene/col3 + 2 Separator）；`useDefaultLayout({id:"panorama-cols", storage:localStorage})` 取 `defaultLayout`+`onLayoutChanged` 接 Group（hook 置于早返回之前）。Panel 全百分比：defaultSize 42/30/28（承接原 1.4/1/0.9），minSize 22/18/18。
  - `src/layouts/Dashboard.module.css`：删 `.panoramaGrid` 固定列+逐列 border，加 `.panoramaGroup`（height 100%）/`.panel`（min-width 0 + overflow hidden）/`.separator`（hairline + hover `--border-3`，全 token）。
  - **关键简化**：v4.11.2 的 Panel min/defaultSize 接受百分比字符串 → 全用 `%`，免 px↔% 换算 util、免 resize 监听（% 自动随窗口缩放）。比 plan 预估干净。
  - 布局存 localStorage（不入 SQLite、不上传，守 [[02-constitution#A2]]）。
  - 验证：`pnpm test` 73/73 / lint clean / prettier clean / `pnpm build`（tsc+vite）✓。

## In Progress

- **Phase C P4 验收未完成**——代码绿但**用户在真实 Tauri webview 看到运行时报错并点了重启**（报错原文未取得）。jsdom 测不到布局测量类错误，故 build/test 绿不代表 webview 不报错。当前 working state（commit `a347d17`）**可能不可用**。

## Next Actions

1. **诊断运行时报错（最高优先级阻塞）**：向用户要报错原文（红框/console 头几行，尤其 `react-resizable-panels`/`Group`/`Panel` 字样）。定位 `src/layouts/Dashboard.tsx` 的 Group/Panel 渲染。**若卡住可 `git revert a347d17`** 回退到可用 `441764b`，再排查。
2. **前台跑 bench**（后台 GUI spawn 不起来、零样本）：用户在输入框敲 `! pnpm bench:hotkey-wake`，确认 P95 ≤200ms（baseline ~10.49ms；前端改动逻辑上不影响 Rust 唤起路径）。
3. **手测 5 项**（原生窗口）：分隔条可见可拖 / 最小宽卡住不塌 0 / **拖出比例→退出重进→localStorage 恢复** / 键盘 Tab 聚焦 separator + ←→ 调宽 / Macro 0·1·N 不破版。
4. **Phase D 文档涟漪（方法论 §7）**：`07-features` 回写（资产可编辑/排序 + 可拖列布局）；P3/P4 列机制变更评估 `05-design-spec`（ADR-012 约束）；`learnings.md` L66「键盘 sensor 未实机验证」已被 #6 手测证伪→回流，resize-handle focus 待 C 手测补；#6 切相位语义变化评估 `user-flows`/`product-spec` B3；**#3/#4 暂缓决策留痕 + 记「Composition 工作台建后须复检 B2 ①」**。

## Risks & Decisions

- **未解运行时报错**：Dashboard P4 改动在真实 webview 抛错，用户已重启，原文未取得——这是当前头号阻塞。回退点 `git revert a347d17`（可安全回到 `441764b`，P4 是独立 commit）。
- **X 方案已定**：列宽 = 默认比例 + 用户可拖 + 持久化，**放弃空列自动收缩**（与持久化拖拽本质互斥，用户已确认选 X）。
- **#3/#4 ModifierGrid/Composition 落点决策**仍暂缓（用户主动搁置），未来落地建 Composition 工作台时须复检 B2 ①。
- `bench:hotkey-wake` 仅前台 GUI 会话可跑。
- 无 remote——checkpoint 仅本地提交（`git remote` 空）。

## Verify

- `pnpm test`（73 passed）
- `pnpm lint` · `pnpm exec prettier --check .`
- `pnpm build`（tsc + vite）
- 前台：`pnpm bench:hotkey-wake`（P95 ≤200ms）
- 原生窗口：⌥Space 唤起确认无报错 + 分隔条拖拽 + 退出重进比例恢复
- 本会话未改 Rust，`cargo test --workspace` 无需重跑

## Modified Files (this session)

- `src/layouts/Dashboard.tsx`（panoramaGrid → resizable Group/Panel/Separator + useDefaultLayout 持久化）
- `src/layouts/Dashboard.module.css`（.panoramaGrid 固定列 → .panoramaGroup/.panel/.separator，全 token）
