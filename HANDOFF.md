---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-12
status: active
audience: [ai]
description: bench 回归收口（两项均无回归，hotkey-wake 旧 ~0.02ms baseline 证伪）+ M-X.4 create_draft/update_draft 64KB size-cap 落地（repo-core 层单一出处 + MCP 错误映射 + 3 测试）。验证全绿。剩余唯一待办：Claude Design Task β（外部依赖 omar）。
related:
  - CLAUDE
  - 06-prd
  - 015-expose-mcp-write-pipeline
---

# Handoff — bench 回归 + M-X.4 size-cap 收口
<!-- Generated by /checkpoint at 2026-06-12 05:50 -->

## Objective
完成 AE 收口后的两项并行待办：M0-4 签名后 bench 回归 + M-X.4 `create_draft` 单写 size-cap。

## Completed
- bench 回归（先清理周一遗留 dev session 解除 bench 卡死）：cold-start P95=257.84ms / p50=174.58ms（p50 与 ~193ms baseline 持平，尾部噪声；C1 不约束）；hotkey-wake P95=13.5/12.9ms 两轮稳定——与 asset-editing plan §7 #5 记录的 auto-cycle 修复后 14.696ms 一致，**旧 ~0.02ms baseline 是从未跑通的失效数字**。判定：两项均无回归（用户选 (a) 接受为新 baseline）
- CLAUDE.md §2 bench 注释更正为正确口径
- M-X.4：64KB cap（PRD §10.1.1）下沉 repo-core——`draft_repo.rs` 加 `MAX_PAYLOAD_BYTES` + `check_payload_size`（create_draft / update_draft 双检查）；`error.rs` 加 `PayloadTooLarge`；MCP `errors.rs` 加映射（Trim 恢复指引）；`server.rs` import_json 改用共享常量
- 测试：repo-core `create_oversize_payload_is_rejected` + `update_oversize_payload_is_rejected_and_row_untouched`；e2e `create_draft_rejects_oversize_payload`
- 验证全绿：pnpm test 87/87 / lint / prettier / build ✓ / cargo test --workspace 51 全过 / clippy 0 / fmt clean

## In Progress
- 无

## Next Actions
1. Claude Design Task β：待 omar 上传 [[CLAUDE-DESIGN]] sticky context（外部依赖，无代码动作）
2. （可选）CLAUDE.md §7 状态指针涟漪：M-X.4 从 P2 defer 列表移除

## Risks & Decisions
- 决策：size-cap 同时覆盖 update_draft（仅查 create 会被 update 绕过，使 cap 失效）；cap 常量单一出处在 repo-core，MCP import_json 复用
- hotkey-wake 旧 ~0.02ms baseline 已证伪并在 CLAUDE.md 注明，避免未来误判回归

## Verify
- `pnpm test && pnpm lint && pnpm exec prettier --check . && pnpm build`
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml`

## Modified Files
- `CLAUDE.md`（§2 bench baseline 更正）
- `src-tauri/crates/repo-core/src/draft_repo.rs` / `error.rs` / `lib.rs`
- `src-tauri/crates/prompt-hub-mcp/src/errors.rs` / `server.rs` / `tests/e2e.rs`
