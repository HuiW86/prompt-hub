# Handoff — 改进计划全量落地 + 评审修复 + 拆分提交推送 + CI 首跑全绿
<!-- Generated by /checkpoint at 2026-07-02 23:35 -->

## Objective
prompt-hub 整体提升计划闭环：多 agent 执行 P0 快修 ×6 / P2 护栏 ×3 / P3 UI 提升 ×7 / 文档涟漪，随后 /review 高强度评审（10 findings 上报）→ 修复 5 条 CONFIRMED 正确性问题 → 按逻辑拆 8 个 commit 推送 main → 新 CI workflow 首跑验收。**本轮全部完成，工作区干净。**

## Completed
- **P0 快修 ×6 / P2 护栏 ×3 / P3 UI 提升 ×7 / 文档涟漪**：全部落地（明细见 `docs/design/CHANGELOG.md` 2026-07-01 条目与 ADR-020；此前 HANDOFF 版本的逐项清单已由该记账取代）
- **评审修复 ×5**（全部 verifier PASS 复核）：
  1. toastStore 新增 `showError`，全仓 20+ 错误 toast 调用点切 error intent（4s + role=alert）——`src/stores/toastStore.ts` + 5 个组件
  2. `ScenePanel.tsx` handleMoveScene 在 reorder 前 `setActiveSceneId` 钉住被移场景（修「默认选中态移错场景」）
  3. `ScenePanel.tsx` handleCreateScene 经 `keepEditOnSceneChange` ref 保持编辑模式（就地重命名承诺成立）
  4. `ScenePanel.tsx` 子阶段 + phrase 两处 dnd 持久化失败补 snapshot 回滚
  5. `updaterStore.ts` auto 失败改 `{status:'idle', error:null}` 诚实状态 + 注释如实化
- **拆分提交**：8 个 commit（ci `0e3d226` / backend `5ee47e7` / design `e604268` / feedback `7ed2247` / updater `d221a93` / panorama `2b9aa54` / ipc-gate `e06829f` / docs `40fe65e`），已 push `origin/main`
- **CI 首跑全绿**：run 28601712987，frontend ~47s / rust ~3m46s 双 job success——P2-1 实战验收通过

## In Progress
- 无（会话闭环，无半成品）

## Next Actions
1. **真机复验 P3 视觉项**：暗 band（ADR-020）/ light 主题重绘 / 像素对齐包——双主题各截一屏比对 Promptscape 设计稿；顺带核对 P3-2 Draft 编辑保存与 P3-6「设为默认」的 DB 落盘（`sqlite3` 查 `alignment_phrases.is_default` / `drafts.payload`）
2. **`pnpm bench:hotkey-wake` 复测守 C1**：基线 12.9-13.5ms；现在 P95>200ms 会退出码 1
3. **ADR-017 Phase 6 真机验收**：updater opt-in / 检查链路（承接前序 HANDOFF）
4. **CLAUDE-DESIGN v0.2 重传**：omar 在 claude.ai/design 替换旧 design system（外部依赖，`docs/design/CLAUDE-DESIGN.md:28`）
5. **v0.1.0 发布**（时机由 omar 定）：`git tag v0.1.0 && git push origin v0.1.0` 触发 release.yml；两道手动关卡（release-signing Environment 审批 + draft release 人工 Publish）
6. **评审遗留 5 条非阻塞项**（后续择机）：① 三个行内编辑器统一补 IME Enter 守卫（`DraftInbox.tsx:274`/`MacroGrid.tsx:316`/`AlignmentPhrases.tsx:361`）；② ipc-contract gate 扩扫 `src/**` 或加 ESLint no-restricted-imports；③ 引入语义化 `--color-danger` token 替换 Toast 借用的 amber swatch；④ 三份行内编辑器去重（抽共享组件）；⑤ dnd 列表壳 4 处去重（抽 `useSortableList` hook）
7. **CI action pin 升级**：checkout / setup-node / pnpm-action / rust-cache 的 SHA 目标 Node 20 已弃用（run annotation），ci.yml + release.yml 一并升新 SHA
8. **PRD §6.1 Modifier soft-delete vs 代码硬删矛盾**：AE 批次欠账，已在 prd 登记 drift，改代码还是改文档待 omar 裁定

## Risks & Decisions
- **主 shell bash 故障疑已恢复**：本会话 git add/commit/push 全部正常持久化（前序会话结论「git 写不持久化」未复现），可解除「git 写走隔离 Workflow」的规避措施
- P0-5 Composition promote 止血是临时闸（`DraftInbox.tsx` 单点常量 `PROMOTE_BLOCKED_HINT`），解锁条件 = Composition 获得查看/搜索/删除 UI 承载
- P3-4 band token 为双主题同值设计（mode-invariant by design）；band 内未来新增引用 `--skeleton`/`--shadow-*` 需补作用域重映射（见 ADR-020）
- P3-7 遗留：mono 计数惯例与 design-spec §9 `.ph-meta`(sans) 系统性分叉待 spec 收编；`.ph-code` preset 暂无消费者
- rust-cache post 步骤对 `src-tauri/target/tests/target` 报 ENOENT 为 trybuild 嵌套目录导致的无害噪音，job 不受影响

## Verify
- `pnpm test` → 152/152（16 文件，含 token-gate / b2-separation / ipc-contract 三 gate）
- `pnpm lint` && `pnpm exec prettier --check .` && `pnpm build`
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml` → 231 passed；clippy `-D warnings` / fmt --check 全绿
- `gh run list --workflow ci.yml --limit 1` → success

## Modified Files
- `HANDOFF.md`（本文件——其余改动已在 8 个 commit 内，见 `git log db272e5..40fe65e`）
