# Handoff — 重塑 v2 全落地（身份/双布局/外观闭环），真机全量验收与并回 main 待收
<!-- Generated by /checkpoint at 2026-07-22 02:10 -->

## Objective
ADR-023 + ADR-024（含补遗）：v0.1.0 发布前完成 UI+组件架构系统性重塑。第一性定位 = 手动挡认知节奏工具的"驾驶舱"，界面按使用时刻组织而非数据模型平铺；Rust 层 / 数据契约 / spec / constitution 不动。

## Completed
- **W1–W6 基础重塑**（`9161920`/`1fb7b1a`/`d934884`，2026-07-21 verifier 对抗审查 PASS，七门禁全绿）：排版角色 7→12 preset + 64 处简写清零 + 正文 14；响应式底线三修；动效语言 + reduced-motion；ScenePanel 拆 7 文件 / promptStore 拆 12 切片（API 冻结零测试改动）/ useFocusRestore 修 A1-05；UpdaterBanner 失败降级 StatusBar
- **v2 身份**（`3c533a5`，ADR-024）：恒定深色驾驶舱（light 显式设置 + persist v2 迁移）、`--brand` violet token、相位主角化（18px + flex-grow + brand 章）、Macro 命令牌（56px + brand 芯片）、Recent 时间线轨
- **v2 双布局**（`c1af3ef`）：interactionMode 驱动编排——调用态驾驶舱（Macro 热度排序热区 + Recent 主列 / Scene 右轨 + SOP 钉底 / Modifier 底托盘）vs 整理态工作室（经典全景）；六区两模式同屏（哲学二）；App.test Tab 序 mode-aware
- **v2 深化**（`41fff89`）：子阶段序号仪表芯片、话术卡阅读余量、SopProgress 从破损虚线框改正经仪表卡（原 Next 5「SopProgress 占位观感」+「Macro 图标语义」done：brand 芯片已统一）
- **外观闭环两轮（omar 实机反馈驱动）**：`6ba982c` 跟随系统恢复真语义（`.system` 类门控 media query，裸默认仍深色）+ 强调色文案如实化；`2bc53fe` 主题类回归测试锁 system/.light/.dark；`c3b5ec9` **强调色升级为身份主题色**（ADR-024 补遗：非中性 swatch 接管 `--brand`+dim/ghost，一键整舱换色；中性默认 violet）
- **诊断勘误**：两次"没变化/浅色带"误判均源于 `/Applications` 旧版实例抢快捷键——已成为标准排查项；HMR 下 Zustand 旧实例导致外观按钮握旧闭包 → 重启进程根治
- **验证**：前端 316/316 / lint / prettier / build 全绿；全部推送 origin（`c3b5ec9`）

## In Progress
- 无半成品；dev server 后台跑动中（bgbfoltsk）；未跟踪 `.codex/`+`AGENTS.md` 留置（归 omar）

## Next Actions
1. **真机全量验收**（1024×640 基线）：驾驶舱布局（点「调用」）+ 整舱换色（设置五色块）+ verifier NEEDS HUMAN 清单（elevation 手感 / overlay 入场 / 窄窗降级 / StatusBar 更新重试链路）+ scrim 45% + 批次 A NEEDS HUMAN 4 项 + 批次 B 移动/撤销链路——完成前不入对外发布说明 (carried from 2026-07-21)
2. 验收通过后 `reshape/ui-v2` 并回 main（git merge --no-ff，含 ADR-023/024 全链）(carried)
3. product-spec §4.0/§13.4 布局与 Tab 序契约回流 + design-spec §2/§8/§9 v2 身份回流（八步，draft 待人审）——App.test.tsx:202 cockpit 序断言是代码侧事实源 (carried，扩)
4. omar 人审批次：design-spec v0.15 draft + ADR-023/024 措辞 + product-spec v0.15/v0.16 + features v1.10/v1.11 + 01-spec v0.7 追认 + 旧账 ADR-021/scene.color (carried from 2026-07-06)
5. light 主题在 v2 身份下的细调（`tokens.css :root.light` 段——v2 只做了深色，light 观感未按驾驶舱语言校）(carried，缩)
6. 补焦点恢复"负路径"测试（src/components/__tests__/ScenePanelFocusRestore.test.tsx，verifier 建议，非阻塞）(carried)
7. bench-c1 `continue-on-error` 处置 omar 复核 (carried from 2026-07-12)
8. 上会话 3 commit（`a24c7c0`/`4b722c1`/`716bd4c`）补 verifier (carried from 2026-07-07)
9. 可发现性裁决 ×2：hover 动作簇微显 / 场景删除入口外露 (carried from 2026-07-06)
10. P0-2 Composition 链路 ADR（P0-5 系于此，`DraftInbox.tsx:43-48`）(carried from 2026-07-06)
11. v0.1.0 交付余量：落盘日志（tauri-plugin-log 立项）/ 安装·快捷键·FAQ 文档 (carried from 2026-07-06)
12. tag dry-run 验 `.dmg` + ADR-017 Phase 6 updater 真机验收 (carried from 2026-07-02/06)
13. P2 余 4 评估 / ops-spec §3 定时备份（`repo-core/src/backup.rs` 底座）/ P1-5 Phase 可配置性 / Macro 网格末行 auto-fit (carried from 2026-07-06)
14. Dock 图标正式素材（violet hub mark——现 UI 身份已对齐 violet）/ CLAUDE-DESIGN v0.2 重传+v2 基调同步 / v0.1.0 发布时机（omar 外部依赖）(carried from 2026-07-02)
15. 评审遗留补 design-spec 上游：ipc-contract 扩扫 / `--color-danger` / `--scrim` 语义回流（随人审八步）(carried from 2026-07-02)
16. PRD §6.1 soft-delete 矛盾 + `status: pre-code` 僵尸 / ai-dev-lifecycle 仓收尾 (carried from 2026-07-02/06)
17. cargo test 计数口径核对（verifier 报 94+3 vs 旧账 155）(carried from 2026-07-21)

## Risks & Decisions
- **ADR-024 及补遗均为授权代笔**（omar"完全重构/发挥你的能力"授权 + 实机反馈迭代），措辞与"恒定深色身份、强调色=身份主题色"两决策待 omar 复核追认
- 双布局的 Tab 序契约（cockpit：macro→recent→scene→sop）已改代码与测试，product-spec §13.4 未回流前存在文档-代码短暂失配（标记 pending human review）
- 正文 14 / 命令牌 / 热度排序等全站变更仅 source+截图级验证；真机验收前不定稿
- HMR 后外观等 store 驱动行为异常时：先重启 dev 进程再排查（Zustand 旧实例现象，本轮已两次踩中）
- `.codex/`/`AGENTS.md` 归 omar

## Verify
- `pnpm test` → 316/316（33 文件）
- `pnpm lint` && `pnpm exec prettier --check .` && `pnpm build`
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml` → 0 failed（Rust 全程零 diff）
- `cargo clippy --workspace --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings`

## Modified Files
- 分支 `reshape/ui-v2` 全推送：`9161920`→`1fb7b1a`→`d934884`→`122e959`→`3c533a5`→`c1af3ef`→`03d4810`→`41fff89`→`6ba982c`→`2bc53fe`→`c3b5ec9`
- 本 checkpoint：`HANDOFF.md` / `docs/.metrics/cochange.log`
