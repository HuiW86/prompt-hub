---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-04
status: active
audience: [ai]
description: asset-editing P2a.1（Modifier 后端全链路）编码 + 自验完成。migration 0006 加 per-quadrant order_index、repo-write modifiers.rs CRUD/reorder（8 测试）、5 Tauri IPC、前端 Modifier type + ipc + store slice（乐观，象限内 reorder）。全验证绿（cargo --workspace 31 / pnpm test 65 / clippy·fmt·lint·prettier·build clean）。已 checkpoint 提交 ee88188。剩 P2a.2 Modifier UI（ModifierGrid 四象限 + dashboard 落点，待布局决策，与 plan P3 交叉）。
related:
  - CLAUDE
  - asset-editing-and-adaptive-layout
  - 016-choose-dnd-and-resizable-layout
  - 014-nspanel-isa-swizzle
  - 09-tech-stack
---

# Handoff — asset-editing P2a.1 Modifier 后端全链路（编码+自验完成，剩 P2a.2 UI）

<!-- Generated by /checkpoint at 2026-06-04 11:42 -->

## Objective

执行 [[asset-editing-and-adaptive-layout]] P2：把 P1 已建立的「order_index +
直接编辑 + dnd-kit 拖拽排序」模式扩展到其余 3 类资产。串行顺序 **P2a Modifier →
P2b AlignmentPhrase → P2c Composition**（共享文件碰撞，不可并行落盘）。本会话完成
P2a 的后端 + IPC + store 部分（P2a.1）。

锁定决策（本会话确认）：
- **D-a** Modifier reorder 按**每象限**（group_kind：cognition/action/delivery/constraint），
  order_index 在每个象限内从 0 重排
- **D-b** Composition 用 JSON 列、update 带 modifier_ids（留到 P2c）
- **D-c** AlignmentPhrase order_index 每 phase、删默认句拒绝（留到 P2b）

## Completed (this session)

- **编排**：动态工作流——Phase 0 我直读 P1 五锚点；Phase 1 三并行 Explore agent
  扇出分析 Modifier/Composition/AlignmentPhrase 差异；Phase 2/3 编排器就地对抗验证 +
  汇总（独立 synthesis agent 无信息增益，故不另起）。产出串行实施清单 + 3 决策门。
- **P2a.1 数据层**：`migrations/0006_modifiers_order_index.sql`（ADD COLUMN NOT NULL
  DEFAULT 0 CHECK≥0；**PARTITION BY group_kind** 的 ROW_NUMBER 回填；建
  `idx_modifiers_order (group_kind, order_index)`）；`db.rs` 注册 Migration v6；
  `models.rs` Modifier 加 `order_index: i64`；`repo.rs` list_modifiers SELECT 加列 +
  ORDER BY 改 `group_kind ASC, order_index ASC, created_at ASC`。
- **P2a.1 写路径**：`repo-write/src/modifiers.rs`（新）create（象限内 MAX+1）/update（不动
  group_kind/order_index）/delete/reorder（象限内重排 + 跨象限 id 拒绝）+ missing_modifier +
  8 测试；`asset_repo.rs` insert_modifier 补 order_index = 象限内 MAX+1；`lib.rs` 导出 4 fn；
  `commands.rs` 加 5 IPC（list_modifiers + 4 写经 with_write_conn→guard_schema_then）；
  `src/lib.rs` generate_handler 注册 5 命令。
- **P2a.1 前端**：`ipc/types.ts` 加 Modifier interface（groupKind: GroupKind + orderIndex）；
  `ipc/index.ts` 加 listModifiers + create/update/delete/reorderModifiers（reorder 带 groupKind）；
  `promptStore.ts` 加 modifiers slice + 4 方法（create await+append / update·delete 乐观回滚 /
  reorder 仅重排目标象限、其余象限保位）；refreshAll + promoteDraft 都拉 listModifiers。
- **测试**：promptStore.test 加 4 例（create / update·delete 回滚 / reorder 象限隔离+回滚）+
  fakeModifiers fixture；App.test 三处 mock 补 list_modifiers。
- **验证全绿**：`cargo test --workspace` 31（含 8 新）/ `pnpm test` 65（含 4 新）/ clippy 0 /
  fmt clean / lint clean / prettier clean / build ✓。已 checkpoint 提交 `ee88188`。

## In Progress

- 无半成品。P2a.1 后端→store 闭环 + 自验完成。

## Next Actions

1. **P2a.2 Modifier UI**（待布局决策）：新建 `src/components/ModifierGrid.tsx` —— Modifier
   此前**前端零基础**（无组件），需新建四象限网格 + dnd-kit useSortable（象限内排序，调
   `reorderModifiers(groupKind, ids)`）+ 集中 ModifierEditor（带 group_kind 选择）。
   **决策门**：dashboard 落点未定——MacroGrid 旁独立卡？还是等 plan P3「内容自适应」一起做？
   先和 omar 确认落点再写组件（B2：Modifier 是任务层，不与协议层混排）。
2. **P2b AlignmentPhrase**：migration 0007（per-phase order_index）；`repo-write/alignment_phrases.rs`
   create/update/delete/reorder——delete 须拒绝 `id == Phase.defaultAlignmentPhraseId`（D-c）；
   reorder 按 phase 分桶；store 操作 `alignmentPhrasesByPhase` 分组结构而非平铺数组。
   已有 `AlignmentPhrases.tsx`（只读区，ADR-013 独立 region）——加编辑须守 B2 不渗入工作台。
3. **P2c Composition**：migration 0008；内容是 modifier_ids 数组（非自由文本），update 签名
   `(id, name, modifier_ids)`；JSON 列保持（D-b），junction 表若需另开 ADR。

## Risks & Decisions

- **P2a.2 落点未决**——Modifier 此前无任何前端，dashboard 槽位与 [[asset-editing-and-adaptive-layout]]
  P3「内容自适应」交叉，须 omar 拍板，勿擅自塞进现有 grid。
- **update_modifier 不换象限**——刻意与 macro 编辑路径对齐（仅改 name/content）；跨象限移动需
  order_index 重算，已 defer。
- **reorder 完整性靠调用方约定**——`reorder_modifiers(group_kind, ids)` 假设 ids 是该象限完整可见
  顺序；传部分会让未触及行保旧 order_index。前端 reorderModifiers 永传该象限全量，当前安全。
- **migration 串行号**——P2b/P2c 必须依次用 0007 / 0008，db.rs MIGRATIONS 顺序登记，勿撞 0006。
- **B2 物理分离**——Modifier 任务层；P2b AlignmentPhrase 协议层编辑 UI 禁渗入 Composition/Macro 工作台。
- 无 remote——checkpoint 仅本地提交（`git remote` 空）。

## Verify

- `cargo test --workspace --manifest-path src-tauri/Cargo.toml`（31 passed，含 modifiers 8 例）
- `pnpm test`（65 passed，含 4 新 store 例）
- `pnpm build` · `pnpm lint` · `pnpm exec prettier --check .`
- `cargo clippy --workspace --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings`
- 实机（P2a.2 后）：`pnpm tauri dev` → ModifierGrid 四象限新增/编辑/删除/象限内拖拽

## Modified Files (this session)

- `src-tauri/crates/repo-core/migrations/0006_modifiers_order_index.sql`（新）
- `src-tauri/crates/repo-write/src/modifiers.rs`（新）
- `src-tauri/crates/repo-core/src/{db,models,repo}.rs` · `repo-write/src/{asset_repo,lib}.rs`
- `src-tauri/src/{commands,lib}.rs`
- `src/ipc/{types,index}.ts` · `src/stores/promptStore.ts`
- 测试：`src/stores/__tests__/promptStore.test.ts` · `src/App.test.tsx`
