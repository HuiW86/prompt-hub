---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-03
status: active
audience: [ai]
description: Closed out ADR-012 Phase 5 visual+functional acceptance at 11/11. The 2 previously-blocked items (promote/discard) were verified by user hand-clicking the inbox buttons; DB cross-check confirmed correct writes (modifier→modifiers group_kind=cognition, alignment_phrase→alignment_phrases phase-plan, macro discard not persisted, all 3 drafts soft-deleted→discarded, inbox emptied→Scene fallback). Rippled CLAUDE §7 + features v0.7.
related:
  - CLAUDE
  - 07-features
  - 012-lock-visual-quality-anchor
---

# Handoff — ADR-012 Phase 5 acceptance closed (11/11)

<!-- Generated by /checkpoint at 2026-06-03 -->

## Objective

Close the last 2/11 of ADR-012 Phase 5 (promote/discard flows that CGEvent
injection couldn't trigger last session) via user hand-clicks, then ripple docs.

## Completed (this session)

- **Phase 5 acceptance 11/11 done.** User manually ran all 4 acceptance steps
  in the live `pnpm tauri dev` window. DB cross-check (dev DB) confirmed:
  - modifier「结构化输出」→ `modifiers` table, `group_kind=cognition` (四象限
    popover worked), content intact.
  - alignment_phrase「先对齐范围」→ `alignment_phrases`, `phase_id=phase-plan`,
    `is_default=0`.
  - macro「深挖追问」→ discarded, `macros` unchanged (丢弃 not persisted).
  - All 3 drafts `status` pending→discarded (2 promote soft-delete per ADR-015
    补遗-2 + 1 explicit discard); inbox emptied → tab auto-fell back to Scene.
  - Baseline before: modifiers 0 / alignment_phrases 8 / macros 4 → after:
    1 / 9 / 4. Counts match expectation exactly.
- **Root cause of last session's "button click miss":** purely a CGEvent
  hit-box issue on small targets — UI logic itself was fine, proven by hand-click.
- **Ripple:** CLAUDE §7 (项目阶段 + 下一动作: Phase 5 done, M-X + ADR-012 全链路
  done); `docs/design/07-features.md` v0.6→v0.7 (复制即隐藏/ESC 行 → done,
  §7 + changelog + status).

## Next Actions

1. **M0-4 Developer ID signing spike** — the main remaining M0 item.
2. bench script regression (`pnpm bench:cold-start` / `bench:hotkey-wake`).
3. M-X.4 (P2 defer): `create_draft` single-write size-cap.
4. Claude Design pipeline: waiting on omar to upload [[CLAUDE-DESIGN]] → run Task β.

## Risks & Decisions

- **Live runtime:** `pnpm tauri dev` likely still running (was PID 50788, log
  `/tmp/ph-tauri-dev.log`). Dev DB now has **0 pending drafts** (all 3 consumed).
  To re-run draft acceptance, re-seed first.
- No code changed this session — docs only. No re-test needed for the ripple.

## Verify

- `cargo test --workspace --manifest-path src-tauri/Cargo.toml` (48/48)
- `pnpm test` (57/57) · `pnpm build`
- `cargo clippy --workspace --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings`

## Modified Files (this session)

- `CLAUDE.md` (§7 项目阶段 + 下一动作)
- `docs/design/07-features.md` (v0.6→v0.7: 复制即隐藏行 / §3.6 / §7 / changelog / frontmatter)
