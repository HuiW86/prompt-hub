---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-04
status: active
audience: [ai]
description: Drafted ADR-016 (Proposed) locking dnd-kit + react-resizable-panels for the asset-editing + adaptive-layout plan. Ran 4-track GitHub research for borrowable open-source references. ADR awaits human review → Accepted before any pnpm add. Corrected plan v0.2 "v4 percent-only" assumption inside the ADR (v4 supports px/percent/rem/em). Research archive kept per user decision. Committed bc03f78. No code/deps touched.
related:
  - CLAUDE
  - asset-editing-and-adaptive-layout
  - 016-choose-dnd-and-resizable-layout
  - 09-tech-stack
  - 014-nspanel-isa-swizzle
---

# Handoff — ADR-016 拖拽+布局选型（Proposed，待人审）

<!-- Generated by /checkpoint at 2026-06-04 -->

## Objective

为 [[asset-editing-and-adaptive-layout]] plan 的 P1.4（区域内拖动排序）与 P4
（可拖分隔条）锁定前端依赖选型，按 [[CLAUDE#§5.3]] 先开 ADR 再写依赖。先做
GitHub 开源调研找可借鉴实现，再起草 ADR-016。

## Completed (this session)

- **四路并行 GitHub 调研**（先确认任务分配表，符合 CLAUDE 多 Agent 规则）：
  (A) dnd-kit 0.4.x 新版实战 / (B) react-resizable-panels v4 / (C) 资产 CRUD
  集中编辑入口 / (D) Tauri 2.x + React 整体架构。可借鉴清单：
  - 拖拽：typebot.io（9.9k★，`DragDropProvider`+`move`+`handleRef` 可整段照搬）、
    aether（取消回滚快照范本）、Seelen-UI（轴约束）、puck（纯手动排序）、
    budget-board（服务端失败 onError 回滚）
  - 分隔条：react-resizable-panels（5.3k★，shadcn 底座）、dannysmith/tauri-template
    （同构栈但锁 v3）
  - 资产编辑：usememos/memos（单 editor 入口）、ospfranco/sol（store 领域分层）、
    blinko（软删 toast / 硬删 confirm 分级）
  - Tauri 架构：KatouRyuuji/TagLauncher（栈几乎完全一致，最高优先级参考）、
    dannysmith/tauri-template（tauri-specta 类型绑定 + NSPanel）、MatsDK/TauRPC、
    RandomEngy/tauri-sqlite（user_version 迁移范式）
- **🔴 校正**：plan v0.2 §0 Q7「v4 仅百分比、无 px」**有误**。v4 实际支持
  px/percent/rem/em，可直接传 token px 值，px↔% 换算 util 大部分可省。校正写进
  ADR，**未就地改 plan**（留 ADR Accepted 后回流）。
- **起草 `docs/adr/016-choose-dnd-and-resizable-layout.md`（status: Proposed）**：
  D-A 拖拽选 `@dnd-kit/react@0.4.0`+`@dnd-kit/helpers@0.4.0`（备选 legacy
  core/sortable、react-dnd、pragmatic-dnd、自建拒因）；D-B 分隔条选
  `react-resizable-panels@^4`（锁 `^4.11`，备选 v3、allotment、split-pane、自建
  拒因）。含未来反悔成本 + 回退备选 + NSPanel 未验证风险。
- **保留调研存档**（用户决定）：`docs/research/2026-06-04-resizable-panels.md`
  + `docs/research/README.md`（tech-research 技能产物）。
- 全部提交 `bc03f78`，工作树干净。**未碰任何代码、未 pnpm add。**

## In Progress

- 无半成品。ADR-016 等人审。

## Next Actions

1. **人审 ADR-016 → Accepted**：确认 ① 决策者署名 omar ② 两选型 + 版本锁定
   （dnd-kit 0.4.0 / resizable-panels ^4.11）③ px 校正结论。
2. **Accepted 后**：`pnpm add @dnd-kit/react@0.4.0 @dnd-kit/helpers@0.4.0
   react-resizable-panels@^4.11`（lockfile 提交）；同步 `docs/design/09-tech-stack.md`
   §3 依赖表 +3；把 px 校正回流 [[asset-editing-and-adaptive-layout]] §0 Q7。
   **Accepted 前禁 pnpm add**（[[CLAUDE#§6]] 第 7 项）。
3. **P1.1**：新 migration `macros` 加 `order_index INTEGER NOT NULL DEFAULT 0`，
   按 `created_at`（fallback rowid）回填；bump `db::latest_version()`。
4. **P1.2**：`repo-core` 有序读 + `repo-write` create/update/delete/reorder_macros
   + `commands.rs` 4 Tauri IPC（经 `guard_schema_then`）。
5. **P1.3/P1.4**：前端 store+ipc+types；MacroGrid 集中编辑入口 + dnd-kit 排序
   （handle 用官方 `handleRef`，参 typebot.io）+ token CSS。
6. **P1.5**：NSPanel key-window 实机验证键盘 sensor + resize-handle focus
   （[[014-nspanel-isa-swizzle]]），失败则键盘排序降级为仅指针。

## Risks & Decisions

- **ADR-016 Proposed 是硬阻塞**——P1.4/P4 在 Accepted 前不能动依赖。
- **NSPanel focus 未验证**——键盘 sensor + resize handle 依赖 key-window
  （ADR-014），无公开 lib×NSPanel 冲突证据但须 P1.5 实机验证。
- **dnd-kit 乐观排序竞态**（社区头号坑）——本地 state 为唯一渲染源 + onDragStart
  快照 + `event.canceled` 回滚（参 aether）。
- **B2 物理分离**——拖动严限区域内排序，禁泄漏成跨区/跨类型拖放；AlignmentPhrase
  编辑 UI 不得渗入 Composition/Macro 区。
- **Modifier/Composition 读缺口**——今天前端无 `list_*`，P2.2 须先探明 surfacing
  位置再建编辑 UI。

## Verify

- ADR 状态：`cat docs/adr/016-choose-dnd-and-resizable-layout.md` → `status: Proposed`
- 提交：`git log --oneline -1` → `bc03f78`（工作树 clean）
- baseline 仍绿（pre-existing，本次未跑）：`cargo test --workspace
  --manifest-path src-tauri/Cargo.toml` · `pnpm test` · `pnpm build`

## Modified Files (this session)

- `docs/adr/016-choose-dnd-and-resizable-layout.md`（new, committed `bc03f78`）
- `docs/research/2026-06-04-resizable-panels.md`（new, committed `bc03f78`）
- `docs/research/README.md`（new, committed `bc03f78`）
- `HANDOFF.md`（this checkpoint）
