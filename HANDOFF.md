# Handoff — 应用图标重塑收口（violet hub 定稿）；重塑 v2 真机全量验收与并回 main 待收
<!-- Generated by /checkpoint at 2026-07-22 08:20 -->

## Objective
ADR-023 + ADR-024（含补遗）：v0.1.0 发布前完成 UI+组件架构系统性重塑，Rust 层 / 数据契约 / spec / constitution 不动。重塑 v2 代码侧已全落地（见上一 checkpoint `4452f35`），本轮补齐应用图标正式素材（原 Next 14 的「Dock 图标 violet hub mark」），主线仍是真机全量验收 → 并回 main。

## Completed
- **应用图标重塑**（`5438338`，omar 实机确认「很 ok」）：旧图标为 512 全画布不透明黑方块硬伤（macOS 不裁圆角，Dock 显示为直角黑砖）；新图标按 Apple 规范 1024 画布 + 824 squircle 透明边距，图形精修为 45° 斜辐条 + 三级卫星节点（大/中实心 + 小空心）+ 环芯中枢，`#8b7bff` 品牌紫 + 驾驶舱深色渐变 + ADR-019 subtle elevation 内高光；小尺寸 128/64/32 两轮迭代校验可读
- **矢量源落仓**：`src-tauri/icons/icon.svg` 为单一真相源（文件头注释再生成流程：headless Chrome 渲染 1024 PNG → `pnpm tauri icon`）；全套 raster 重生成（icns 1024 验证可读 / ico / 各尺寸 PNG），iOS/Android 无用产物已删；`tauri.conf.json` 零改动
- **新排查项沉淀**：图标由 `generate_context!` 编译期嵌入二进制，改 `icons/` 不触发 cargo 重编译——需 `touch src-tauri/build.rs` 强制重编译才生效（本轮实测 0.39s 缓存 vs 10.45s 真编译）
- **dev 环境重置**：上会话遗留 dev 进程链（含 Zustand 旧实例隐患）全部清杀后全新拉起
- 重塑 v2 全量成果（W1–W6 / 身份 / 双布局 / 深化 / 外观闭环两轮）见 `4452f35` checkpoint 与 CHANGELOG，本轮零改动
- **light 主题驾驶舱校准**（2026-07-22 夜间自主批次，原 Next 6）：tokens.css §1d 五路身份色浅色加深锚点（白底 ≥4.4:1）+ `::selection` token 化修浅色选区不可见 + `--aux` 浅色加深；新增 theme-parity.test.ts 锁 `:root.light`/`:root.system` 双轨镜像防漂移；316→324 全绿；色值回流 design-spec §2 随 v0.15 人审八步

## In Progress
- dev server 后台跑动中（本会话任务 bczkwc45a，`target/debug/prompt-hub` 已嵌新图标）；未跟踪 `.codex/` + `AGENTS.md` 留置（归 omar）

## Next Actions
1. **推送 origin**：`git push origin reshape/ui-v2`——`5438338`（图标）+ 本 checkpoint 尚未推送
2. **真机全量验收**（1024×640 基线）：驾驶舱布局（点「调用」）+ 整舱换色（设置五色块）+ verifier NEEDS HUMAN 清单（elevation 手感 / overlay 入场 / 窄窗降级 / StatusBar 更新重试链路）+ scrim 45% + 批次 A NEEDS HUMAN 4 项 + 批次 B 移动/撤销链路 + **浅色主题走查（设置切「浅色」×五 accent：logo/相位/Macro 芯片/焦点环加深观感 + 选中文本可见，2026-07-22 校准）**——完成前不入对外发布说明 (carried from 2026-07-21)
3. 验收通过后 `reshape/ui-v2` 并回 main（git merge --no-ff，含 ADR-023/024 全链）(carried from 2026-07-21)
4. product-spec §4.0/§13.4 布局与 Tab 序契约回流 + design-spec §2/§8/§9 v2 身份回流（八步，draft 待人审）——App.test.tsx:202 cockpit 序断言是代码侧事实源 (carried from 2026-07-21)
5. omar 人审批次：design-spec v0.15 draft + ADR-023/024 措辞 + **图标图形定稿追认（AI 代笔，实机已认可）** + product-spec v0.15/v0.16 + features v1.10/v1.11 + 01-spec v0.7 追认 + 旧账 ADR-021/scene.color (carried from 2026-07-06，扩)
6. ~~light 主题在 v2 身份下的细调~~ **Done 2026-07-22**（见 Completed；浅色观感真机走查并入 Next 2 验收清单）
7. 补焦点恢复"负路径"测试（src/components/__tests__/ScenePanelFocusRestore.test.tsx，verifier 建议，非阻塞）(carried from 2026-07-21)
8. bench-c1 `continue-on-error` 处置 omar 复核 (carried from 2026-07-12)
9. 上会话 3 commit（`a24c7c0`/`4b722c1`/`716bd4c`）补 verifier (carried from 2026-07-07)
10. 可发现性裁决 ×2：hover 动作簇微显 / 场景删除入口外露 (carried from 2026-07-06)
11. P0-2 Composition 链路 ADR（P0-5 系于此，`DraftInbox.tsx:43-48`）(carried from 2026-07-06)
12. v0.1.0 交付余量：落盘日志（tauri-plugin-log 立项）/ 安装·快捷键·FAQ 文档 (carried from 2026-07-06)
13. tag dry-run 验 `.dmg` + ADR-017 Phase 6 updater 真机验收——**顺带验证新图标在正式 .app/Dock 的最终观感** (carried from 2026-07-02/06，扩)
14. P2 余 4 评估 / ops-spec §3 定时备份（`repo-core/src/backup.rs` 底座）/ P1-5 Phase 可配置性 / Macro 网格末行 auto-fit (carried from 2026-07-06)
15. CLAUDE-DESIGN v0.2 重传 + v2 基调同步 / v0.1.0 发布时机（omar 外部依赖）——原并列的「Dock 图标正式素材」已 Done 移出 (carried from 2026-07-02，缩)
16. 评审遗留补 design-spec 上游：ipc-contract 扩扫 / `--color-danger` / `--scrim` 语义回流（随人审八步）(carried from 2026-07-02)
17. PRD §6.1 soft-delete 矛盾 + `status: pre-code` 僵尸 / ai-dev-lifecycle 仓收尾 (carried from 2026-07-02/06)
18. cargo test 计数口径核对（verifier 报 94+3 vs 旧账 155）(carried from 2026-07-21)

## Risks & Decisions
- **图标图形为 AI 代笔正式候选**：omar 实机口头认可（「很 ok」），正式定稿追认并入人审批次（Next 5）；后续换色/微调只改 `src-tauri/icons/icon.svg` 再走文件头注释的再生成流程
- **图标不生效排查顺序**：① `touch src-tauri/build.rs` 强制重编译（编译期嵌入）② `killall Dock` 刷缓存 ③ 确认非 `/Applications` 旧版实例 ④ 最终以 `pnpm tauri build` 正式包为准（dev 裸二进制无 .app bundle）
- **ADR-024 及补遗均为授权代笔**（omar「完全重构/发挥你的能力」授权 + 实机反馈迭代），措辞与「恒定深色身份、强调色=身份主题色」两决策待 omar 复核追认 (carried)
- 双布局 Tab 序契约代码已改、product-spec §13.4 未回流，存在文档-代码短暂失配（标记 pending human review）(carried)
- 正文 14 / 命令牌 / 热度排序等全站变更仅 source+截图级验证；真机验收前不定稿 (carried)
- HMR 后 store 驱动行为异常时先重启 dev 进程（Zustand 旧实例）(carried)
- `.codex/` / `AGENTS.md` 归 omar (carried)

## Verify
- `pnpm test` → 324/324（33 文件，+8 theme-parity）
- `pnpm lint` && `pnpm exec prettier --check .` && `pnpm build`
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml` → 0 failed（Rust 全程零 diff，本轮仅 touch 时间戳）
- `cargo clippy --workspace --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings`

## Modified Files
- `5438338`：`src-tauri/icons/`（18 文件——新增 `icon.svg` 矢量源 + `64x64.png`，重生成 icns/ico/各尺寸 PNG/Store Logo）
- 本 checkpoint：`HANDOFF.md` / `docs/.metrics/cochange.log`
