---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-03
status: active
audience: [ai]
description: Closed out M0-4 — the macOS Developer ID signing + notarization spike. Empty-shell DMG built, signed with Developer ID Application (NY89JGE95Q) + hardened runtime + 3 JIT entitlements, notarized (Accepted), stapled, Gatekeeper-accepted, and the release transparent window launches without black-screen. This falsifies the worst-case "macos-private-api conflicts with notarization" assumption. M0 four deliverables now all green. Created entitlements.plist + tauri.conf.json bundle.macOS block + runbook; rippled plan / CLAUDE §7 / features v0.8.
related:
  - CLAUDE
  - prompt-hub-mvp
  - 07-features
  - m0-4-macos-signing
---

# Handoff — M0-4 macOS signing + notarization spike closed

<!-- Generated by /checkpoint at 2026-06-03 -->

## Objective

Run the M0-4 risk-forward spike: prove the full macOS release pipeline
(Developer ID sign → notarize → staple → Gatekeeper → runtime) works with
`macos-private-api` (ADR-008) + hardened runtime, before any release.

## Completed (this session)

- **M0-4 full chain verified end-to-end (2026-06-03):**
  - Built empty-shell bundle via `pnpm tauri build` (exit 0).
  - Signed: `Developer ID Application: Hui Wang (NY89JGE95Q)`, full chain,
    `flags=0x10000(runtime)` (hardened runtime on).
  - Entitlements applied: allow-jit + allow-unsigned-executable-memory +
    disable-library-validation.
  - **Notarization Accepted** (submission `915e10ce-...`) — private API NOT
    rejected; falsifies the worst-case assumption.
  - Stapled DMG + APP; `spctl` both `accepted / source=Notarized Developer ID`.
  - **Runtime:** user launched the notarized release `.app`, ⌥Space woke the
    transparent always-on-top window, no black-screen. JIT entitlement OK at runtime.
  - => **M0 four deliverables all green** (cargo build / 200ms wake / token
    sheet / signing+notarization).
- **Scaffolding created:** `src-tauri/entitlements.plist` (3 JIT entitlements),
  `tauri.conf.json` `bundle.macOS` block (signingIdentity / entitlements /
  providerShortName=NY89JGE95Q), `docs/workflows/m0-4-macos-signing.md` runbook.
- **Notary creds:** stored in keychain profile `ph-notary` (apple-id +
  team NY89JGE95Q + app-specific password). Reusable for future releases.
- **Ripple:** `docs/plans/prompt-hub-mvp.md` (M0-4 ⏳→✅), `CLAUDE.md` §7
  (项目阶段 + 下一动作: M0 四项全绿), `docs/design/07-features.md` v0.7→v0.8.

## Next Actions

1. bench script regression (`pnpm bench:cold-start` / `bench:hotkey-wake`).
2. M-X.4 (P2 defer): `create_draft` single-write size-cap.
3. Claude Design pipeline: waiting on omar to upload [[CLAUDE-DESIGN]] → run Task β.
4. Commit this session's docs+config changes (not yet committed).

## Risks & Decisions

- **No ADR opened:** signing follows ops-spec §1.2 既定决策; the 3 JIT
  entitlements are Tauri's standard set — neither is a 二选一不可逆决策, so the
  runbook record suffices (CLAUDE §5.3).
- **Team ID surprise:** Developer ID cert is on paid team `NY89JGE95Q`, NOT the
  free personal team `58VY3XG89K` (the Apple Development cert). Both signingIdentity
  and providerShortName point at NY89JGE95Q.
- **Notary creds secret:** app-specific password lives only in keychain profile
  `ph-notary`, never in git/config/env files.

## Verify

- `cargo test --workspace --manifest-path src-tauri/Cargo.toml` (48/48)
- `pnpm test` (57/57) · `pnpm build`
- Re-sign/notarize: see `docs/workflows/m0-4-macos-signing.md`.

## Modified Files (this session)

- `src-tauri/entitlements.plist` (new)
- `src-tauri/tauri.conf.json` (bundle.macOS block)
- `docs/workflows/m0-4-macos-signing.md` (new runbook)
- `docs/plans/prompt-hub-mvp.md` (M0-4 交付标准 ✅)
- `CLAUDE.md` (§7 项目阶段 + 下一动作)
- `docs/design/07-features.md` (v0.7→v0.8: changelog + §7 footer + frontmatter)
