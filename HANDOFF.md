---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-01
status: active
audience: [ai]
description: M-X.0 涟漪全闭环——共创 2 份（product-spec v0.7 / design-spec v0.8）经 omar 审定升 ratified，含审定轮 2 决策（badge 不进 Tab cycle / Scene 激活 tab 紫→绿）+ 2 清理。MANIFEST 共创 6/6 ratified、CLAUDE.md §7 指针刷新。M-X.0 闭环，下一站 M-X.1 数据层重构待 omar 验收启动。
related:
  - CLAUDE
  - MANIFEST
  - 03-product-spec
  - 05-design-spec
  - mcp-write-pipeline
---

# Handoff — M-X.0 涟漪闭环：共创 2 份审定升 ratified

<!-- Generated by /checkpoint at 2026-06-01 -->

## Objective

M-X.0 涟漪（ADR-015 MCP write pipeline）收尾最后一环：omar 人审上一 session 起草的共创 2 份 draft（product-spec v0.7 / design-spec v0.8），审定升 `ratified`。本 session 完成审查 + 2 决策 + 2 清理 + 升级 + MANIFEST/CLAUDE.md 回写。M-X.0 全闭环。

## Completed

### 审查 + 4 项修订（commit `24f467d`，4 files +30/-17）

| 项 | 文件 | 改动 |
|---|---|---|
| 清理 1 | `docs/design/05-design-spec.md` §10.4.2 | 草稿 tab 激活态去自问自答的 `--protocol-8` 措辞 → 落定 `--surface-2` + `--border-3`（aux 中性） |
| 清理 2 | `docs/design/05-design-spec.md` §10.4 修订记录 | 「上游一致性发现」由「待裁决」→「已解决」（product-spec §4.0.4 已回补 📥 占位注） |
| 决策 3 | design-spec §10.4.1 + product-spec §13.4 | PendingBadge 删 `focused` 态、定为纯状态指示器**不进 Tab cycle**（保持 6 tab-reachable）；草稿键盘入口走 Scene region → 最左草稿 tab；promote 不绑单 ⏎ 误触 |
| 决策 4 | product-spec §13.3 区域4 | Scene 当前激活 tab「紫」→「绿」(`--task-16` + `--task` border)——纠正早于 design-spec v0.7 ontology 的预存 drift，紫属协议层、用于 Scene = 跨层污染（[[02-constitution#B2]]） |

### 升级 + 回写

- product-spec v0.7 / design-spec v0.8 frontmatter `status: draft → ratified`（omar 审定授权），各自修订记录补「审定轮」条目。
- `docs/MANIFEST.md` v1.2 → v1.3：§1 共创 `4/6 ratified + 2 draft` → `6/6 ratified`；§3 两文状态回写。
- `CLAUDE.md` §7 状态指针刷新：项目阶段→M-X.0 闭环；文档体系→共创 6/6 ratified；ADR 进度→13 Accepted + 1 Proposed + 2 Reserved；tech-stack→v1.2；下一动作→M-X.1。

### 已核实一致（审查结论）

- 5 个 IPC 名与 prd §10.3 完全对齐（`count_pending_drafts` / `list_drafts` / `promote_draft` / `update_draft` / `discard_draft`），`promote_draft` 不暴露给 MCP。
- 铁律守住：B1（drafts≠第4层）/ B2（三组件 aux 中性不染 ontology）/ D1（promote 须 omar 显式、无自动路径）/ C1（badge 查询 ≤1ms 预算）。

## In Progress

无。本 session 已 commit（`24f467d`），M-X.0 涟漪全闭环。

## Next Actions

### B. M-X.1 数据层 + workspace 重构（L 级，3-4 天）

1. 待 omar 验收 M-X.0 后启动。提纲见 `docs/plans/mcp-write-pipeline.md#§8`（12 步）：drafts 表 migration 0003 + 14 tool 双层（repo-core 读 / repo-write 写）+ Cargo workspace 物理拆 repo-core / repo-write 两 crate。

### 并行待办（不阻塞 M-X.1）

2. ADR-012 Phase 5 `pnpm tauri dev` 6 步 manual verify：overlay frame / ⌥Space 唤起 / PhaseBar 8 段 / AlignmentPhrases chip flash / MacroGrid Flame / StatusBar ⌘K + macOS Space fullscreen bug 回归（commit `0fbab2d` 修过）。
3. bench 脚本（cold-start / hotkey-wake）+ M0-4 Developer ID 签名 spike。
4. Claude Design pipeline：待 omar 上传 [[CLAUDE-DESIGN]] 创建 design system 跑 Task β。

## Risks & Decisions

- **决策 3/4 已落定**：badge 非 tab stop（保持 6）；Scene 激活 tab 用绿（按层取色）。后续实现层须遵此，勿回退紫色或把 badge 塞进 tab cycle。
- **收件箱内键盘细节留实现层**：草稿卡方向键选择 + 动作键 promote/discard，但 promote 不可单 ⏎ 误触（守 D1 从容闸门）。
- **M-X.1 物理拆 crate 风险**：workspace 重构涉及 Cargo.toml + 现有 rusqlite 代码迁移，启动前重读 plan §8 + 09-tech-stack v1.2 workspace 决议。

## Verify

```bash
pwd                                                       # /Users/apple/WeChatProjects/prompt-hub
git log --oneline -1                                      # 期望 24f467d checkpoint: M-X.0 ripple — co-authored product-spec/design-spec ratified

grep -n "^status: ratified" docs/design/03-product-spec.md docs/design/05-design-spec.md
grep -n "6/6 ratified" docs/MANIFEST.md
grep -n "绿色" docs/design/03-product-spec.md          # §13.3 区域4 Scene 激活 tab

# 现有功能回归（confirm 第一阶段 MVP 没破）
pnpm lint                                                 # 0 errors
pnpm test                                                 # 55/55
cargo test --manifest-path src-tauri/Cargo.toml           # 12/12
```

## Modified Files

本次 checkpoint（commit `24f467d`，4 files +30/-17）：

- `docs/design/03-product-spec.md` — v0.7 ratified（§13.4 键盘入口 note + §13.3 Scene tab 紫→绿）
- `docs/design/05-design-spec.md` — v0.8 ratified（§10.4.2 激活态清理 + §10.4.1 删 badge focused + 修订记录已解决）
- `docs/MANIFEST.md` — v1.3（共创 6/6 ratified）
- `CLAUDE.md` — §7 状态指针刷新
