# Handoff — light 主题驾驶舱校准落地 + 更新/隐藏两项诊断；重塑 v2 真机全量验收与并回 main 待收
<!-- Generated by /checkpoint at 2026-07-22 19:05 -->

## Objective
ADR-023 + ADR-024（含补遗）：v0.1.0 发布前完成 UI+组件架构系统性重塑，Rust 层 / 数据契约 / spec / constitution 不动。重塑 v2 代码侧全落地（含本轮 light 主题校准，原余项清零），主线仍是真机全量验收 → 并回 main。

## Completed
- **light 主题驾驶舱校准**（`861fe23`，原 Next 6，verifier PASS）：tokens.css §1d 五路身份色浅色加深锚点（surface-1 白底 ≥4.4:1；canvas 上 blue 4.39/amber 4.01 仅作填充色，verifier 实算口径）+ `--accent`/`--brand` 同步接管闭环 ADR-024 补遗 + `::selection` token 化修浅色选区不可见（原 ~1.1:1）+ `--aux` 加深至 ~4.6:1；band 暗岛不动（ADR-020）
- **新增 theme-parity.test.ts**：解析 tokens.css 锁 `:root.light*` ≡ `@media` 内 `:root.system*` 双轨镜像 + 四 accent 覆盖，verifier 实测改值即红；测试 316→324
- **更新功能诊断**（无代码）：「检查更新失败」根因 = 仓库无任何 release/tag，endpoint latest.json 404 —— ADR-017 客户端/CI 链路无恙，属预期状态；激活依赖 Next 12 的 tag dry-run 首发
- **复制不隐藏诊断**（无代码）：实证运行实例 localStorage `interactionMode:"organize"`（dev 裸二进制 WebKit 目录按进程名走 `~/Library/WebKit/prompt-hub`，非 `dev.prompt-hub`）——整理态复制保窗是 D-0 设计行为（product-spec v0.15 §4.0.7），usage 照常落库已验；用户体感为 bug = 模式可发现性缺口，并入 Next 9 裁决
- 图标重塑 / 重塑 v2 全量成果见 `bc88451`/`4452f35` checkpoint 与 CHANGELOG，本轮零改动

## In Progress
- dev server 后台跑动中（target/debug/prompt-hub，8:24 拉起）；未跟踪 `.codex/` + `AGENTS.md` 留置（归 omar）

## Next Actions
1. **推送 origin**：`git push origin reshape/ui-v2` —— 本地领先 4 commit（图标 + light 校准 + 两 checkpoint）(carried from 2026-07-22)
2. **真机全量验收**（1024×640 基线）：驾驶舱布局（点「调用」）+ 整舱换色（设置五色块）+ verifier NEEDS HUMAN 清单（elevation 手感 / overlay 入场 / 窄窗降级 / StatusBar 更新重试链路）+ scrim 45% + 批次 A NEEDS HUMAN 4 项 + 批次 B 移动/撤销链路 + 浅色主题走查（浅色 × 五 accent：身份时刻加深观感 + 选中文本可见）——完成前不入对外发布说明 (carried from 2026-07-21)
3. 验收通过后 `reshape/ui-v2` 并回 main（git merge --no-ff，含 ADR-023/024 全链）(carried from 2026-07-21)
4. product-spec §4.0/§13.4 布局与 Tab 序契约回流 + design-spec §2/§8/§9 v2 身份回流（八步，draft 待人审；light §1d 色值随此批入 design-spec §2）——App.test.tsx:202 cockpit 序断言是代码侧事实源 (carried from 2026-07-21)
5. omar 人审批次：design-spec v0.15 draft + ADR-023/024 措辞 + 图标图形定稿追认 + product-spec v0.15/v0.16 + features v1.10/v1.11 + 01-spec v0.7 追认 + 旧账 ADR-021/scene.color (carried from 2026-07-06，扩)
6. 补焦点恢复"负路径"测试（src/components/__tests__/ScenePanelFocusRestore.test.tsx，verifier 建议，非阻塞）(carried from 2026-07-21)
7. bench-c1 `continue-on-error` 处置 omar 复核 (carried from 2026-07-12)
8. 上会话 3 commit（`a24c7c0`/`4b722c1`/`716bd4c`）补 verifier (carried from 2026-07-07)
9. **可发现性裁决 ×3**（升优先级——2026-07-22 实证 omar 本人把整理态保窗当 bug）：hover 动作簇微显 / 场景删除入口外露 / **整理态状态可见性 vs 修改 D-0 契约（整理态复制也隐藏）二选一**——涉 product-spec §4.0.7，改契约走八步 (carried from 2026-07-06，扩)
10. P0-2 Composition 链路 ADR（P0-5 系于此，`DraftInbox.tsx:43-48`）(carried from 2026-07-06)
11. v0.1.0 交付余量：落盘日志（tauri-plugin-log 立项）/ 安装·快捷键·FAQ 文档 (carried from 2026-07-06)
12. tag dry-run 验 `.dmg` + ADR-017 Phase 6 updater 真机验收——**2026-07-22 确认仓库零 release，「检查更新失败」404 属预期；首发 release 即同时激活更新功能**；顺带验证新图标正式 .app/Dock 观感 (carried from 2026-07-02/06，扩)
13. P2 余 4 评估 / ops-spec §3 定时备份（`repo-core/src/backup.rs` 底座）/ P1-5 Phase 可配置性 / Macro 网格末行 auto-fit (carried from 2026-07-06)
14. CLAUDE-DESIGN v0.2 重传 + v2 基调同步 / v0.1.0 发布时机（omar 外部依赖）(carried from 2026-07-02)
15. 评审遗留补 design-spec 上游：ipc-contract 扩扫 / `--color-danger` / `--scrim` 语义回流（随人审八步）(carried from 2026-07-02)
16. PRD §6.1 soft-delete 矛盾 + `status: pre-code` 僵尸 / ai-dev-lifecycle 仓收尾 (carried from 2026-07-02/06)
17. cargo test 计数口径核对（verifier 报 94+3 vs 旧账 155）(carried from 2026-07-21)

## Risks & Decisions
- **light §1d 色值为 AI 代笔**（原 Next 6 授权范围内），真机浅色走查（Next 2）通过前不定稿；对比度声称以 verifier 实算口径为准（canvas 上 blue/amber 只作填充/焦点环色，不作 canvas 文字色）
- **整理态「复制不隐藏」是设计不是 bug**——但 omar 本人误判为 bug，是 D-0 可发现性的实证反例；处置二选一挂 Next 9，改契约必须走八步，不就地改行为
- **dev 裸二进制的 WebKit 存储在 `~/Library/WebKit/prompt-hub`**（进程名），正式 .app 在 `dev.prompt-hub`（bundle id）——两套 localStorage/设置互不相通，排查「设置不生效」类问题先分清实例
- ADR-024 及补遗均为授权代笔，措辞与两决策待 omar 复核追认 (carried)
- 双布局 Tab 序契约代码已改、product-spec §13.4 未回流，文档-代码短暂失配（pending human review）(carried)
- HMR 后 store 驱动行为异常时先重启 dev 进程（Zustand 旧实例）(carried)
- `.codex/` / `AGENTS.md` 归 omar (carried)

## Verify
- `pnpm test` → 324/324（33 文件，含 theme-parity 8 例）
- `pnpm lint` && `pnpm exec prettier --check .` && `pnpm build`
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml` → 0 failed（本轮 Rust 零 diff）
- `cargo clippy --workspace --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings`

## Modified Files
- `861fe23`：`src/styles/tokens.css`（§1d + `--selection`）/ `src/styles/theme-parity.test.ts`（新增）/ `docs/design/CHANGELOG.md` / `HANDOFF.md`
- 本 checkpoint：`HANDOFF.md` / `docs/.metrics/cochange.log`
