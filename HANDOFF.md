---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-03
status: active
audience: [ai]
description: Promoted ADR-014 Reserved→Accepted (NSPanel subclass canBecomeKeyWindow decision) + rippled CLAUDE/MANIFEST/ADR-008. Then ran ADR-012 Phase 5 visual acceptance via screencapture+osascript — 9/11 checklist items verified by eye (overlay/search-focus/PhaseBar 8段/AlignmentPhrases chip/MacroGrid flame/StatusBar/草稿 tab/待审 badge/DraftInbox). Blocked on promote/discard: small-button CGEvent clicks not registering though tab/phase clicks do. Committed 1e3cf38.
related:
  - CLAUDE
  - 014-nspanel-isa-swizzle
  - 012-lock-visual-quality-anchor
---

# Handoff — ADR-014 promote + ADR-012 Phase 5 visual acceptance

<!-- Generated by /checkpoint at 2026-06-03 20:55 -->

## Objective

(1) Promote ADR-014 Reserved → Accepted recording the NSPanel-subclass
`canBecomeKeyWindow` decision. (2) Resume ADR-012 Phase 5 manual visual
acceptance of the overlay + M-X.3 draft-inbox additions.

## Completed (this session)

- **ADR-014 → Accepted** (`docs/adr/014-nspanel-isa-swizzle.md`): rewrote the
  Reserved placeholder into a full 6-section decision. Decision = Option A
  (`define_class!` `PromptHubKeyablePanel : NSPanel` override `canBecomeKeyWindow`
  + isa-swizzle to it) vs B (Titled style bit) / C (fork wry) / D (wait upstream).
  Kept the 3 trigger-monitor conditions + implementation anchors.
- **Ripple (committed 1e3cf38):** CLAUDE §7 ADR count 13→14 Accepted, 2→1
  Reserved; `docs/MANIFEST.md` row 014 → Accepted (2026-06-03); ADR-008 downstream
  link Reserved→Accepted.
- **ADR-012 Phase 5 — automated visual pass (screencapture + osascript wake):**
  Verified **9/11** checklist items by eye (screenshots `/tmp/ph-*.png`):
  ✅ ⌥Space wake / clean single-frame / search box focused (keyboard-fix regression)
  / PhaseBar 8 段 + phase-select highlights purple / AlignmentPhrases region
  (empty「未选相位」→ select 规划 shows「● 默认·规划」chip) / MacroGrid lucide flame
  (not emoji) / StatusBar ⌘K + live update「当前相位：规划」/ **📥 草稿 tab leftmost
  count 3** / **待审 badge「3 条待审」** / DraftInbox renders all 3 seeded drafts
  with correct type-badge + name + preview + provenance tool_name + timestamp.
- Seeded payloads deserialize cleanly (no panic/serde error in dev log).

## In Progress

- **2/11 checklist items unverified: promote(归档) + discard(丢弃) flows.**
  CGEvent mouse clicks on the small inbox buttons (logical ~824, y 231/306/381)
  do NOT register — no promote IPC fires, DB unchanged. BUT clicks DO work on
  larger targets: 草稿 tab (555,160) opened the inbox; PhaseBar 规划 (522,56)
  selected the phase. Scale confirmed correct (logical = screenshot_px / 2;
  display 1470×956 @2x). Root cause still open — was about to test far-right
  迭代(8) phase (1378,56) to rule in/out a right-half click-delivery issue.

## Next Actions

1. **Diagnose the button-click miss.** Test 迭代(8) at logical (1378,56): if it
   selects, right-half delivery is fine and the inbox buttons need a different
   approach (hover-dwell, or the button hit-box is smaller/elsewhere than the
   measured screenshot center). Tools ready: `/tmp/click2.swift x y` (CGEvent),
   `/tmp/winlist.swift` (CGWindow list), `screencapture -l115874 -o -x out.png`.
2. **Or fall back to route B** for just these 2 flows: ask user to click 归档 /
   丢弃; I screencapture + DB-verify after each.
3. Verify on promote: Modifier draft (结构化输出) 归档 opens **四象限 popover**;
   AlignmentPhrase/Macro promote lands in `alignment_phrases`/`macros` table
   (DB cross-check). On discard-all: inbox empties → tab auto-falls back to Scene.
4. M0-4 Developer ID signing spike. M-X.4 (P2): `create_draft` size-cap.

## Risks & Decisions

- **Live runtime state to clean up or reuse:** `pnpm tauri dev` still running
  (PID 50788, log `/tmp/ph-tauri-dev.log`). 3 seeded **pending** drafts in dev DB
  (`~/Library/Application Support/dev.prompt-hub/prompt-hub.db`): 深挖追问 (macro,
  phase-generate) / 先对齐范围 (alignment_phrase, phase-plan) / 结构化输出
  (modifier, phase-understand, scene-plan). Killed two orphans earlier: stale
  Vite (43768) + stale tauri instance (47740, was holding ⌥Space hotkey).
- macOS Accessibility permission now granted to the terminal → osascript
  keystroke + CGEvent mouse injection work for future sessions.
- Phase state was mutated to 规划 + copy-count incremented during calibration
  (cosmetic, dev DB only).

## Verify

- `cargo test --workspace --manifest-path src-tauri/Cargo.toml`
- `pnpm test` (57/57) · `pnpm build`
- `cargo clippy --workspace --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings`
- Resume acceptance: drafts already seeded; if dev server died, `pnpm tauri dev`
  then osascript ⌥Space wake → screencapture window 115875-range (re-list via
  `/tmp/winlist.swift`).

## Modified Files (committed 1e3cf38)

- `docs/adr/014-nspanel-isa-swizzle.md` (Reserved → Accepted, full body)
- `CLAUDE.md` (§7 ADR count) · `docs/MANIFEST.md` (014 row) ·
  `docs/adr/008-enable-macos-private-api.md` (downstream link)
