---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-01
status: active
audience: [ai]
description: M-X.1 数据层 + workspace 重构进行中。已完成步骤 1-3（workspace 拆 repo-core / open_read_only+open_and_migrate / migration 0003 drafts + 0004 compositions + draft 模型）。14/14 cargo 测试 + clippy 全绿。卡在一个待 omar 拍板的决策：Modifier/Macro promote 字段映射（payload 有 phase_id 但表无此列；modifiers.group_kind 必填但 payload 无）。下一步建 DraftRepo/ReadOnlyAssetRepo + repo-write crate (AssetRepo+promote) + prompt-hub-mcp 空壳。
related:
  - CLAUDE
  - mcp-write-pipeline
  - 06-prd
  - 09-tech-stack
---

# Handoff — M-X.1 数据层 + workspace 重构（步骤 1-3 完成）

<!-- Generated by /checkpoint at 2026-06-01 -->

## Objective

M-X.1（plan `docs/plans/mcp-write-pipeline.md#§8`，L 级 12 步）：把 src-tauri 拆成 Cargo workspace 4 crate（repo-core / repo-write / prompt-hub bin / prompt-hub-mcp bin），落 drafts staging 数据层 + DraftRepo/ReadOnlyAssetRepo/AssetRepo trait 双层 + promote 跨表事务，编译期硬隔离 MCP（只依赖 repo-core）。本 session 采用**方案 B**（workspace 根放 `src-tauri/`，非 plan §3.2 字面的仓库根，省 Tauri 工具链返工），omar 已确认。

## Completed（commit `401928c`）

- **步骤 1 — workspace + repo-core 抽取**：`src-tauri/Cargo.toml` 改为 `[workspace] members=["crates/repo-core"]` + 仍是 Tauri bin。新建 `crates/repo-core`（零 tauri 依赖 rlib），迁入 `db.rs`/`models.rs`/`repo.rs`。error 拆双层：repo-core `RepoError`（无 Tauri 变体）+ bin `AppError`（`#[from] RepoError` + Tauri + LockPoisoned，保留 IPC Serialize）。`commands.rs::with_conn` 桥接 `RepoResult`→`AppResult`。
- **步骤 2 — 连接模式拆分**：`db.rs` 新增 `open_and_migrate`（owner=Tauri 主 app 跑 migration）+ `open_read_only`（MCP 用，READ_ONLY flags，不跑 migration，user_version≠latest 则返 `RepoError::SchemaVersionMismatch` 拒开，R1）。所有连接 `busy_timeout=5000`。`lib.rs` 调 `open_and_migrate`。
- **步骤 3 — drafts 数据模型**：`migrations/0003_drafts.sql`（drafts 表 4 类 target_type + payload_hash + provenance + status + 3 索引含 `idx_drafts_hash_pending` 部分唯一）；`migrations/0004_compositions.sql`（新建 compositions 表，方案 A，omar 确认——调和 PRD §10.2 promote 目标 vs §6.2 临时态：promoted composition 持久化）。`models.rs` 加 `DraftPayload`(4 variant internally-tagged `tag="target_type"`) / `Provenance` / `Draft` / `DraftSummary` / `DraftTargetType` / `DraftStatus`。`sha2` dep 备 payload_hash。
- **验证**：14/14 cargo 测试（12 迁移 + 2 个新 open_read_only 测试）；`clippy -D warnings` 全绿（加了一处 `#[allow(clippy::should_implement_trait)]` 在 `UsageTargetType::from_str` 上，pre-existing 非本次引入）。

## In Progress

- 步骤 4（task #1，in_progress）：DraftRepo + ReadOnlyAssetRepo（repo-core）+ repo-write crate（AssetRepo + promote_draft）。**被下方决策阻塞**（仅 Modifier/Macro 两类 promote）。

## Next Actions

1. **等 omar 拍板 Modifier/Macro promote 字段映射**（见 Risks）。
2. `crates/repo-core`：加 `DraftRepo` trait（drafts CRUD：create 算 payload_hash=SHA-256(payload_json) / get_draft / list_drafts→DraftSummary / update_draft / mark_discarded）+ `ReadOnlyAssetRepo` trait（list_modifiers 读 modifiers 表含 group_kind / list_compositions 读 0004 新表 / 复用现有 list_phases/list_alignment_phrases/list_macros/list_scenes）。需补 `Modifier`/`Composition` 两个 struct 到 `models.rs`（当前缺）。
3. 新建 `crates/repo-write`（`Cargo.toml` dep repo-core）：`AssetRepo` trait（insert_modifier/insert_macro/insert_composition/insert_alignment_phrase）+ `promote.rs::promote_draft`（PRD §10.2 流程：SELECT pending → 反序列化 DraftPayload → BEGIN → match 4 variant insert → DELETE draft → COMMIT；override_payload 参数）。先实现干净的 Composition/AlignmentPhrase variant。
4. 注册 `repo-write` 进 workspace members；bin `Cargo.toml` 加 repo-write 依赖（commands 新增 promote/update/discard IPC 留步骤后续或 M-X.3）。
5. 新建 `crates/prompt-hub-mcp` bin（`Cargo.toml` **只**写 repo-core 依赖行，无 repo-write）+ `tests/trybuild_negative.rs`（`use repo_write::AssetRepo` 必须 compile_fail）。本里程碑不实现 14 tool（属 M-X.2）。
6. 补 ≥8 新测试（drafts CRUD / promote 跨表事务 atomic / payload_hash 去重 / schema 漂移）+ clippy 全绿。
7. **doc-sync（务必）**：更新 `CLAUDE.md §2` 的 cargo test/clippy 命令加 `--workspace`；M-X.1 完成后走方法论 §7 给 PRD §6.2 补"promoted composition 例外"注 + §10.1.2 若改 Modifier payload（选项 ii）则 bump。

## Risks & Decisions

- **🚧 待决策 — Modifier/Macro promote 字段映射**：PRD §10.1.2（ratified）给 4 类 payload 都挂 `phase_id`，但 `modifiers`/`macros` 表无 phase_id 列；`modifiers.group_kind`（必填枚举 cognition/action/delivery/constraint）payload 里没有。AI 推荐：① Macro promote → `native=1`/`expand_from=NULL`/scene_id 透传/**phase_id 丢弃**（Macro 按 scene 不按 phase 归档）；② Modifier promote → `group_kind` 默认 `'cognition'`（omar 在草稿 tab promote 时本就在审，可改）/phase_id+scene_id 丢弃。Composition/AlignmentPhrase 两类字段全对齐无歧义，可先做。
- **已决策（方案 A，omar 确认）**：promoted Composition 持久化到新 `compositions` 表（id/name/modifier_ids JSON/phase_id NOT NULL/scene_id/usage 字段）；工作台临时 Composition 仍不持久化（§6.2 成立）。需 doc-sync 给 §6.2 补例外注。
- **已决策（方案 B，omar 确认）**：workspace 根在 `src-tauri/`（非 plan §3.2 仓库根），子 crate 在 `src-tauri/crates/`。物理隔离目标不变（mcp 只依赖 repo-core）。
- **CLAUDE.md §2 命令回归**：`cargo test --manifest-path src-tauri/Cargo.toml` 现只测 bin pkg（0 测试），真实 14 测试在 repo-core，必须 `--workspace`。未修，doc-sync 时改。
- **serde 限制**：`DraftPayload` 是 internally-tagged enum，serde 不支持其 `deny_unknown_fields`（PRD §10.1.2/plan §4.2 写了但编译不过）；未知键被静默忽略，缺字段/类型漂移仍由 promote 时重反序列化兜住。若必须拒未知键需改 per-variant struct。

## Verify

```bash
cd /Users/apple/WeChatProjects/prompt-hub/src-tauri
cargo test --manifest-path Cargo.toml --workspace      # 期望 14/14
cargo clippy --manifest-path Cargo.toml --workspace --all-targets -- -D warnings  # 0 警告
git log --oneline -1                                    # 401928c checkpoint: M-X.1 step 1-3 ...
```

## Modified Files（commit `401928c`，17 files）

- `src-tauri/Cargo.toml` — workspace 根 + repo-core 依赖（移除 bin 未用的 serde_json/chrono/uuid）
- `src-tauri/Cargo.lock` — repo-core + sha2 解析
- `src-tauri/crates/repo-core/Cargo.toml` — 新建（rusqlite/serde/serde_json/chrono/uuid/thiserror/sha2 + dev tempfile）
- `src-tauri/crates/repo-core/src/{lib,db,models,repo,error}.rs` — 数据层 + RepoError + open_read_only/open_and_migrate + draft 模型
- `src-tauri/crates/repo-core/migrations/{0001_initial,0002_seed,0003_drafts,0004_compositions}.sql`
- `src-tauri/src/{commands,error,lib}.rs` — 改依赖 repo_core / AppError 包 RepoError / open_and_migrate
- `src-tauri/src/{db,models}.rs` — 删除（已迁 repo-core）
