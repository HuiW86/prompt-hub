# Handoff — Promptscape 设计吸收（ADR-018, A1+B1+C1+D+E）
<!-- Generated by /checkpoint at 2026-06-25 11:05 -->

## Objective
把 Claude Design「Promptscape 全景仪表盘」设计稿以「改造现有组件」方式吸收进现有
Dashboard，组合锁定 A1（保项目语义色）+B1（不引 Modifier 右栏）+C1（改造现有组件）
+D（接既有 store）+E（保 prompt-hub 名 + 去头像），拿约 90% 视觉收益且零数据迁移，
不破任何 constitution 铁律。

## Completed
- 新增组件：`src/components/Header.tsx`（slim：logo+标题+内嵌 SearchBar+gear，去头像）/
  `src/components/ProtocolBand.tsx`（协议层暗色 band 容器）/
  `src/components/SettingsModal.tsx`（外观页 themeMode 三态 + 5 色中性强调 / 更新页复用 updaterStore）
- `src/stores/settingsStore.ts`：themeMode `light|dark|system` + accent 5 色 + settingsOpen +
  applyAppearance（root class 三态，onRehydrate 应用无首启闪烁），persist localStorage（A2 不出站）
- `src/styles/tokens.css`：+accent swatch/`--accent`/`--scrim`/`--w-settings-*` token（token-gate clean）
- `src/layouts/Dashboard.tsx` + CSS：Header 行 + ProtocolBand 行 + 任务层 3→2 列
  （resizable group id `panorama-2col` 丢弃旧三列缓存；Macro 收紧凑横条 / aside 承 Recent+SOP）
- `src/App.tsx`：⌘/Ctrl , 唤起设置弹窗
- 5 份文档涟漪（锚 ADR-018）：product-spec v0.10 / design-spec v0.11 / features v1.5（新 §3.11 6 功能 done）/
  ADR-016 补遗（3→2 列 + `panorama-2col`）/ CHANGELOG；外加 MANIFEST v1.6 + CLAUDE §7 状态指针
- 验证全绿：`pnpm test` 97/97 ✓ / lint ✓ / build ✓ / prettier ✓（后端 Rust 未动）

## In Progress
- 无未完成代码项；本批工程 + 文档涟漪已全部收口并 commit（`a094155`）

## Next Actions
1. 真机视觉验收（唯一待办）：用 CGWindow `screencapture -l <window-id>` 抓 off-screen
   Tauri 窗口，人验 Header/ProtocolBand/SettingsModal 三组件渲染；重点核对
   ① accent 切换只染品牌标记/主操作/焦点环（B2，protocol 紫 / task 绿 不变）
   ② themeMode 三态切换正确、刷新无闪烁 ③ 任务层两列布局不挤
2. （外部依赖）Claude Design pipeline：待 omar 上传 [[CLAUDE-DESIGN]] 跑 Task β（与本任务正交）
3. （前序待办）ADR-017 Phase 6 自动更新真机验收 + `bench:hotkey-wake` 复测守 C1

## Risks & Decisions
- 与设计稿三处「有意偏离」（非缺陷，ADR-018 §6 显式记录）：无头像 / 无 Modifier 右栏 /
  无全局「新建」按钮——真机验收时勿当 bug
- 强调色系统新增「中性强调面」概念：后续任何上色须复检 [[02-constitution#B2]]，
  防 accent 泄漏到 protocol/task 层（已用 `:root.accent-*` 物理隔离）
- MANIFEST 此前存历史 drift（仅更新了本次触及的 product-spec/design-spec/features 行
  + ADR 计数），其余旧行未全量对账——非本批职责

## Verify
- `pnpm test`（expect 97/97）
- `pnpm build`
- `pnpm lint`
- `pnpm exec prettier --check .`

## Modified Files
- `src/App.tsx`
- `src/layouts/Dashboard.tsx` / `src/layouts/Dashboard.module.css`
- `src/stores/settingsStore.ts`
- `src/styles/tokens.css`
- `src/components/Header.tsx` / `Header.module.css`（新）
- `src/components/ProtocolBand.tsx` / `ProtocolBand.module.css`（新）
- `src/components/SettingsModal.tsx` / `SettingsModal.module.css`（新）
- `src/components/{AlignmentPhrases,MacroGrid,PhaseBar,ScenePanel,SearchBar}.module.css`
- `docs/adr/018-absorb-promptscape-design.md`（新）
- `docs/adr/016-choose-dnd-and-resizable-layout.md`（补遗）
- `docs/design/{03-product-spec,05-design-spec,07-features,CHANGELOG}.md`
- `docs/MANIFEST.md`
- `CLAUDE.md`
