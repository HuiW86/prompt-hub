# Handoff — Scene/SubStage 结构编辑
<!-- Generated by /checkpoint at 2026-06-28 -->

## Objective
补齐 [[scene-phrase-editing]] 当初 defer 的另一半：Scene 容器本体 + SubStage 子阶段的增/改/删/排序。修复「子阶段死维度」——此前 `subStages` 永远为空、Phrase 的 `sub_stage_id` 永远 NULL、编辑态子阶段下拉永远只有「无分组」。

## Completed
- 后端 repo-write：`scenes.rs` + `sub_stages.rs`（create/update/delete/reorder，19 单测）
- D4 删除语义：非空 Scene 阻止（`RepoError::SceneNotEmpty`）；SubStage 删除单事务解绑 Phrase（sub_stage_id=NULL）后删行
- `0011_seed_sub_stages.sql` 纯 seed migration（user_version 10→11），scene-plan 灌 4 个示范子阶段 + 归类现有 2 phrase
- 8 个 Tauri IPC 注册（`commands.rs` + `src/lib.rs`）
- 前端：`ipc/index.ts` +8 方法 / `promptStore.ts` +8 actions（Scene 树变更走 listScenesWithChildren 重拉）
- `ScenePanel.tsx`：`SceneStructureEditor` 编辑态 inset（场景改名/删/新建 + 子阶段增改名删），空子阶段编辑态可见
- 文档涟漪：features v1.6（66 项）/ product-spec v0.11 / prd v0.11 / MANIFEST v1.7 / CHANGELOG / plan 状态 Done
- 验证全绿：前端 109 测试 / 后端 74 测试 / clippy + fmt + eslint + prettier + build clean

## In Progress
- 无（实现与自动化验证全部收口）

## Next Actions
1. 真机验证 `ScenePanel.tsx:SceneStructureEditor` CRUD 落盘（`pnpm tauri dev` 手测：新建/改名/删除 Scene + SubStage，核对 DB `scenes`/`sub_stages`/`phrases.sub_stage_id`）
2. 可选：`git push`（本地领先 origin/main 21 commits）

## Risks & Decisions
- **真机 CRUD 落盘未验**：CLI 无法驱动原生 Tauri 窗口，仅自动化测试 + 文档标注「待验」
- D1 范围：Scene + SubStage CRUD 一起做（否则子阶段下拉仍死）
- D3：Tauri-only，本次不上 MCP 写面（repo-write 不被 MCP binary 依赖）
- B2 复检通过：结构编辑只碰任务层 Scene/SubStage、中性表面，无 AlignmentPhrase
- 无新 ADR：PRD §6.4 契约已存，属补实现

## Verify
- `pnpm test`
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml`
- `pnpm lint && pnpm exec prettier --check .`
- `cargo clippy --workspace --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings`
- `pnpm build`

## Modified Files
- `src-tauri/crates/repo-write/src/scenes.rs`（new）
- `src-tauri/crates/repo-write/src/sub_stages.rs`（new）
- `src-tauri/crates/repo-core/migrations/0011_seed_sub_stages.sql`（new）
- `src-tauri/crates/repo-core/src/{db.rs,error.rs,draft_repo.rs}`
- `src-tauri/crates/prompt-hub-mcp/src/errors.rs`
- `src-tauri/crates/repo-write/src/{lib.rs,phrases.rs,modifiers.rs,macros.rs,compositions.rs,alignment_phrases.rs}`
- `src-tauri/src/{commands.rs,lib.rs}`
- `src/ipc/index.ts`
- `src/stores/promptStore.ts` + `__tests__/promptStore.test.ts`
- `src/components/ScenePanel.tsx` + `ScenePanel.module.css` + `__tests__/ScenePanel.test.tsx`
- `docs/{MANIFEST.md,plans/scene-substage-editing.md}`
- `docs/design/{03-product-spec.md,06-prd.md,07-features.md,CHANGELOG.md}`
