---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-05-24
status: active
audience: [ai]
description: ADR-012 实施 Phase 1/2 已 commit；Phase 3 partial（primitives + AlignmentPhrases.tsx）随本 checkpoint commit；剩余 80% 待续作。项目已从 ~/Desktop/人机协作/prompt-hub 迁移到 ~/WeChatProjects/prompt-hub 以避开 macOS TCC Desktop 访问限制
related:
  - CLAUDE
  - MANIFEST
  - CLAUDE-DESIGN
  - claude-design-prompts
  - 012-lock-visual-quality-anchor
  - prompt-hub-mvp
---

# Handoff — ADR-012 Phase 3 partial commit；项目已迁出 Desktop

<!-- Generated by /checkpoint at 2026-05-24 02:30 -->

## Objective

5 阶段实施 [[CLAUDE-DESIGN]] handoff bundle（已解压到 `docs/mockups/v2/`）。Phase 1/2 已独立 commit；Phase 3 partial 本次 checkpoint commit；剩余约 80% 待续作。

5 个决策点全部采纳推荐：D1 token 命名采用 bundle / D2 lucide-react@0.460.0 不开 ADR / D3 保留 mock 数据 / D4 新增组件不开 ADR / D5 逐 phase 暂停审查。

## Completed

### Phase 1（commit `b932ab4`）

- 解压 Claude Design handoff bundle 到 `docs/mockups/v2/prompt-hub-design-system/`（README + 28 preview HTML + 95 lucide SVG + 4 截图 + 8 jsx + styles.css + colors_and_type.css + SKILL.md + chat1.md）
- `src/assets/fonts/Geist-Variable.woff2` + `GeistMono-Variable.woff2` 自托管
- `package.json`：`lucide-react@0.460.0` 锁定
- `docs/design/09-tech-stack.md` §6 加 2 行（图标库 / 字体）标 ADR-012 派生

### Phase 2（commit `9a822d8`）

- 重写 `src/styles/tokens.css`（56 → ~200 行）：bundle 命名 `--protocol/task/aux` / `--canvas / --surface-1..3 / --border-1..3 / --fg-1..4` / `--t-12..20` / `--s-1..20` / `--r-1..frame` / `--d-fast/base/slow` / `--ease`；加 `@font-face Geist Sans + Mono`；dark 默认 + light 模式 override + `:root.light` class
- 10 个 `*.module.css` + `src/App.css` 批量 sed 替换 26 个旧 token → 新 token
- 保留 `--s-10/12/20`（off-primary surface 用，bundle 卡 32px 上限）
- 验证：`pnpm lint` ✅ / `pnpm test` 55/55 ✅ / `pnpm build` 435ms ✅ / Geist woff2 emit ✅

### Phase 3 partial（本 checkpoint commit）

6 个新文件落盘：

- `src/components/primitives/Kbd.tsx`（kbd badge + sm 变体）
- `src/components/primitives/RegionHeader.tsx`（title + count + hint + hotkey + right slot）
- `src/components/primitives/EmptyState.tsx`（centered fragment + hint + hotkey）
- `src/components/primitives/primitives.module.css`（三组件共享 CSS）
- `src/components/primitives/index.ts`（barrel export）
- `src/components/AlignmentPhrases.tsx`（协议层第二条 chip 行；从 `usePromptStore.alignmentPhrasesByPhase[activePhaseId]` 取数；click → `useCopy` with `targetType: "alignment_phrase"` + `source: "phase_bar"`；带 `flashId` 高亮逻辑）

**注意**：`AlignmentPhrases.tsx` 的 `targetType: "alignment_phrase"` 和 `source: "phase_bar"` 是写入时 EPERM 没法读 `src/ipc/types.ts` 写的，**未核对 `UsageTarget` / `UsageSource` 枚举**。下次续作第一步必须核对，不匹配则修。

## In Progress

Phase 3 剩余 ~80%：

- `src/components/AlignmentPhrases.module.css`（chip 视觉：`.label` / `.chip` / `.chip.active` / `.chip.dim` / `.dot` / `.empty`；参考 bundle `styles.css` §121-160）
- 8 组件 CSS+JSX 重写（PhaseBar / MacroGrid / ScenePanel / RecentList / StatusBar / SearchBar / SearchOverlay / SopProgress / Toast）按 bundle `styles.css` + 各 `.jsx` pattern：border-only default / hover border-darken / active 8% semantic fill / focus 2px semantic outline / lucide-react icons / RegionHeader+EmptyState 替换内联 header
- `src/layouts/Dashboard.tsx` 重构 → bundle overlay-frame + 4-row grid（PhaseBar / AlignmentPhrases / panorama-3col / StatusBar）；当前 5-region 结构收敛到 panorama 3 列（col1=MacroGrid / col2=ScenePanel / col3=RecentList+SopProgress 堆叠）
- `src/layouts/Dashboard.module.css` 全量重写
- `src/App.test.tsx` 更新：line 193 DOM 顺序数组加 `"alignment-phrases"` 在 `"phase-bar"` 后；region count 7→8（line 142 + line 156）
- `pnpm lint` + `pnpm test` + `pnpm build` + `cargo test`
- 单一 commit `feat(design): Phase 3 — visual rewrite per bundle + AlignmentPhrases + primitives`

## Next Actions

### 紧迫（下次会话第一步）

1. **验证 cwd 与 partial 落盘**：
   ```bash
   pwd                                         # 必须是 /Users/apple/WeChatProjects/prompt-hub
   ls src/components/primitives/               # 期望 5 文件
   git log --oneline -5                        # 应看到本 checkpoint + 9a822d8 + b932ab4
   ```
2. **核对 AlignmentPhrases 类型枚举**：
   ```bash
   grep -n "UsageTarget\|UsageSource\|TargetType\|alignment_phrase\|phase_bar" src/ipc/types.ts src/hooks/useCopy.ts
   ```
   如不匹配 `"alignment_phrase"` / `"phase_bar"` → 修 `src/components/AlignmentPhrases.tsx` 的 `targetType` / `source` 字段
3. **续作 Phase 3** 按下面顺序

### Phase 3 续作详细步骤

4. **`src/components/AlignmentPhrases.module.css`**（新）
   - 参考 `docs/mockups/v2/prompt-hub-design-system/project/ui_kits/prompt-hub/styles.css` 第 121-160 行 `.phrases / .chip / .dot`
5. **PhaseBar 重写** — segmented bar 5 段等分，每段 num 方块（16x16 monospace 1-5）+ 名称 + timer；active 加 `--protocol-8` 背景 + bottom 2px `--protocol` 条
6. **MacroGrid 重写** — 用 `<RegionHeader title="Macros" count={n} hint="new" hotkey="⌘N" />`，3 列 grid `.macro-grid`，每卡 `.macro` 含 row(title+icon) + body(2 行 line-clamp) + meta(kbd · uses · lastUsed)
7. **ScenePanel 重写** — RegionHeader（"Scenes" + count + "filter" hint + `/`），32px 高密度行 `.scene`，左侧 6px 圆点状态色（task=running with pulse / fg-4=idle / #C8941F=warn），name + path(右侧 ellipsis) + meta(duration)
8. **RecentList 重写** — bundle 没有完全对应组件（最近 `AuxPanel.jsx` 是 key:value 分组）；按现有数据保持简单 list 但应用 bundle border-only default + hover surface-1 + active task-8
9. **StatusBar 重写** — bundle 28px 高 footer：`grp` + `dot ok/proto` + `mono` (model/tokens) + `sep` (1×12 div) + `spacer` (flex:1)；现有今日复制 N 次 + 当前相位映射到 bundle grp 形态
10. **SearchBar 重写** — 不直接套 bundle `.quickfind`（bundle 是模态而 SearchBar 是常驻），保留当前职能，token 化即可
11. **SearchOverlay / SopProgress / Toast** — token 化 + bundle pattern 微调（border-only / focus outline / 8% active fill）
12. **Dashboard 重构** — `Dashboard.tsx` + `.module.css` 改 overlay-frame：
    ```css
    .overlay {
      position: fixed;
      inset: 12px;
      background: var(--canvas);
      border: var(--hairline) solid var(--border-2);
      border-radius: var(--r-frame);
      display: grid;
      grid-template-rows: auto auto auto 1fr auto;
    }
    ```
    DOM 顺序：SearchBar / PhaseBar / AlignmentPhrases / panorama(3 col grid 1.4fr 1fr 0.9fr) / StatusBar
13. **App.test.tsx 更新**（line 193）：
    ```ts
    expect(ordered).toEqual([
      "phase-bar",
      "alignment-phrases",   // 新增
      "macro-grid",
      "scene-panel",
      "recent-list",
      "sop-progress",
      "status-bar",
    ]);
    ```
    + 七 region 测试改为八 region（line 142 文案 + line 156 assert 加 alignment-phrases）
14. **验证 + commit**：
    ```bash
    pnpm lint && pnpm test && pnpm build
    cargo test --manifest-path src-tauri/Cargo.toml
    git add src/components/ src/layouts/Dashboard* src/App.test.tsx
    git commit  # message: "feat(design): Phase 3 — visual rewrite per bundle + AlignmentPhrases + primitives"
    ```

### Phase 4 + Phase 5（后续）

15. **Phase 4**：design-spec v0.6 → v0.7 bump 加 6 章（视觉锚点 / typography presets / 组件 pattern / states / icon 系统 / 视觉权重）按方法论 §7 八步；归档 `docs/mockups/prompt-hub.html` → `archive/v1-engineer-aesthetic.html`；CLAUDE.md §7 状态推进；CHANGELOG 2026-05-24 条目
16. **Phase 5**：`pnpm lint` / `pnpm test` / `cargo test` / `cargo clippy` / `pnpm exec prettier --check .` / `pnpm tauri dev` 手动 6 步验证

## Risks & Decisions

### Decisions（不可逆）

- ADR-012 Linear 视觉锚定（2026-05-24 Accepted）
- L5 协作契约层（方法论 v1.3）
- Token 命名采用 bundle（D1）—— 不保留 legacy alias
- lucide-react@0.460.0（D2）—— 未开独立 ADR，写入 09-tech-stack §6
- 保留 mock 数据，5 phase 名 scan/plan/compose/review/land 暂用（D3），真实命名待 [[01-spec]] 反查
- 逐 phase 单 commit（D5）
- **项目目录迁出 Desktop**（2026-05-24）—— 永久迁到 `~/WeChatProjects/prompt-hub`，避开 macOS TCC Desktop 访问限制。CLAUDE.md / docs / git 历史全量跟随 mv

### Risks

- **bundle 编的 mock 命名**（scan/plan/compose/review/land / phrase 名 / macro 名）不要渗入 src 代码或测试断言
- **App.test.tsx DOM 顺序是硬断言** — Phase 3 续作 step 13 必须同步更新，否则 55 测试退化
- **`AlignmentPhrases.tsx` 类型未核对** — `targetType: "alignment_phrase"` / `source: "phase_bar"` 写入时 EPERM 没读到 types.ts；下次续作 step 2 必须核对
- **Task α handoff bundle 整体落地** vs Task β 单组件先验证（已抉择走 α，不可逆）
- **多屏 / primary_monitor() 单屏假设** — 外接显示器场景未验证
- **omar 提过的「几个 bug」未采集** — 见上版 HANDOFF backlog，下次续作前建议 omar 补述

## Verify

```bash
pwd                                                      # /Users/apple/WeChatProjects/prompt-hub
git log --oneline -5                                     # 期望: checkpoint + 9a822d8 + b932ab4 + ...
git status --short                                       # 期望: 干净
ls src/components/primitives/                            # Kbd RegionHeader EmptyState .tsx + primitives.module.css + index.ts
ls src/components/AlignmentPhrases.tsx                   # 存在
pnpm lint                                                # 0 errors（Phase 2 完成时通过）
pnpm test                                                # 55/55（Phase 2 完成时通过）
```

## Modified Files

### 已 commit

- `b932ab4` Phase 1 — bundle 入库 + Geist 字体 + lucide-react
- `9a822d8` Phase 2 — tokens.css + 10 module.css 替换
- 本 checkpoint commit — Phase 3 partial（primitives + AlignmentPhrases.tsx）+ HANDOFF.md

### 未来会话需修改 / 新建

- `src/components/AlignmentPhrases.module.css`（新）
- `src/components/PhaseBar.tsx` + `.module.css`（重写）
- `src/components/MacroGrid.tsx` + `.module.css`（重写）
- `src/components/ScenePanel.tsx` + `.module.css`（重写）
- `src/components/RecentList.tsx` + `.module.css`（重写）
- `src/components/StatusBar.tsx` + `.module.css`（重写）
- `src/components/SearchBar.tsx` + `.module.css`（重写）
- `src/components/SearchOverlay.module.css`（token 化微调）
- `src/components/SopProgress.module.css`（token 化）
- `src/components/Toast.module.css`（token 化）
- `src/layouts/Dashboard.tsx` + `.module.css`（重构）
- `src/App.test.tsx`（DOM 顺序 + region count）
- `docs/design/05-design-spec.md` v0.6 → v0.7（Phase 4）
- `docs/design/CHANGELOG.md`（Phase 4）
- `CLAUDE.md` §7 状态指针（Phase 4）
- `docs/mockups/prompt-hub.html` → `docs/mockups/archive/v1-engineer-aesthetic.html`（Phase 4，mv）
