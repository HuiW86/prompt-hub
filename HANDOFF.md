# Handoff — UI 重塑 W1–W6 落地并过 verifier PASS；真机走查与人审待收
<!-- Generated by /checkpoint at 2026-07-21 16:20 -->

## Objective
ADR-023：v0.1.0 发布前完成 UI+组件架构系统性重塑（omar 拍板：范围 = UI+架构；时机 = 重塑后再发；supersede 审计 D-6 排序）。以一次连贯设计 pass 替代销账式补丁；Rust 层 / 数据契约 / spec / constitution 不动。

## Completed
- **治理**：ADR-023 Accepted + `docs/plans/ui-reshape.md` 北极星方案；main 已 push（`802e1db`）；重塑走 `reshape/ui-v2` 分支 3 commit
- **W1+W2 排版**（`9161920`）：preset 角色 7→12（+page-title/action/label/note/num）；正文层 13→14（审计 D-3）；ph-code 12→13；全仓 64 处 `font:` 简写清零迁移 composes（原批次 C 四步一次完成）；`.btn` 12px 根因修复；UpdaterBanner 失败态降级 StatusBar「更新失败 · 重试」
- **W3/W5/W6**（`1fb7b1a`）：响应式底线三修（A3-03 SettingsModal nav 可缩 / A3-04 SearchOverlay auto-fit + itemName min-width / A3-05 token 拆分 `--col-min-result`+`--w-inline-editor`）；动效语言（Settings/Search overlay 入场 + 全局 prefers-reduced-motion；唤起路径明确零入场动画守 C1）
- **W4 架构**（`d934884`）：ScenePanel 1100 行→`scene/` 7 文件（编排器 674）；promptStore 862 行→`prompt/` 12 切片（公共 API 冻结，promptStore.test.ts 1267 行零改动通过）；`useFocusRestore` 修 A1-05 焦点断链（提交后恢复 + 兄弟回落 + data-nav-id 契约），新增 5 条集成测试
- **勘误销账**：旧账「暗 band 待复验」= 非 bug——浅色带截图来自 `/Applications` 旧版实例（已退），dev 版 ADR-020 渲染正确
- **验证**：前端 314/314（309 基线 + 5 新）/ lint / prettier / build 全绿；真机冒烟通过（拆分后 HMR 实机渲染交互正常）
- **文档回流**：design-spec v0.14→v0.15 draft（§9 角色表 12 preset）+ CHANGELOG 2026-07-21 条目

## In Progress
- 无半成品；`reshape/ui-v2` 已含 checkpoint 待 push；未跟踪 `.codex/`+`AGENTS.md` 留置（归 omar）；dev server 已退出

## Verifier 结论（2026-07-21 对抗审查 **PASS**）
- 七门禁全绿自跑：前端 314 / lint / prettier / build / cargo test --workspace（0 failed）/ clippy -D warnings / fmt --check
- 三耦合点核实：ID 钉死（scene/ScenePanel.tsx:281-283）/ 6 标志单 reset effect（:141）/ guards.ts 计数器单例；`src-tauri/` 与两个既有测试文件 zero-diff
- 宪法 A2/D1/B1/B2/C1 grep 级零触线；useFocusRestore tries 重试推演**不会夺回用户焦点**（仅 neutral 时 focus）
- 非阻塞观察：焦点测试缺"不应夺焦"负路径用例（代码读证安全，建议后续补）

## Next Actions
1. **真机走查**（1024×640 基线）：verifier NEEDS HUMAN 清单——排版 14px/13px floor 观感、elevation 可感知度、overlay 入场/hover 动效手感、SettingsModal/SearchOverlay 窄窗降级、StatusBar「更新失败·重试」点击链路 + scrim 45% + 批次 A NEEDS HUMAN 4 项 + 批次 B 移动/撤销链路——完成前不入对外发布说明 (carried)
2. 真机走查通过后：`reshape/ui-v2` 并回 main（或继续在分支上做重塑收尾第 5 项后再并）
3. 补焦点恢复"负路径"测试（verifier 建议，非阻塞）
4. omar 人审：design-spec v0.15 draft（§9 角色表）+ product-spec v0.15/v0.16 + features v1.10/v1.11 + 01-spec v0.7 追认 + 旧账 ADR-021/scene.color (carried)
5. 重塑收尾评估：Macro 图标语义、SopProgress 占位观感、light/dark 双主题细调——按真机走查结论定
6. bench-c1 `continue-on-error` 处置 omar 复核 (carried)
7. 上会话 3 commit（`a24c7c0`/`4b722c1`/`716bd4c`）补 verifier (carried)
8. 可发现性裁决 ×2：hover 动作簇微显 / 场景删除入口外露 (carried)
9. P0-2 Composition 链路 ADR（P0-5 系于此）(carried)
10. v0.1.0 交付余量：落盘日志（tauri-plugin-log）/ 安装·快捷键·FAQ 文档 (carried)
11. tag dry-run 验 `.dmg` + ADR-017 Phase 6 updater 真机验收 (carried)
12. P2 余 4 评估 / ops-spec §3 定时备份 / P1-5 Phase 可配置性 / Macro 网格末行 auto-fit (carried)
13. Dock 图标正式素材 / CLAUDE-DESIGN v0.2 重传 / v0.1.0 发布时机（omar 外部依赖）(carried)
14. 评审遗留补 design-spec 上游：ipc-contract 扩扫 / `--color-danger` / `--scrim` 语义回流（随人审批次八步）(carried)
15. PRD §6.1 soft-delete 矛盾 + `status: pre-code` 僵尸 / ai-dev-lifecycle 仓收尾 (carried)

## Risks & Decisions
- **正文 13→14 与 5 个新 preset 是全站性视觉变更**，仅 source+截图级验证，1024×640 真机走查前不定稿；不合适回调 tokens/typography 单点
- W4 拆分依赖 314 测试safety net + verifier 复核；焦点恢复 tries=8 重试窗约数十 ms，理论上存在与用户快速操作竞争的窗口（verifier 已被要求推演）
- design-spec v0.15 为 AI 代笔 draft，未过人审不 ratify；ADR-023 为会话内口头拍板落档，omar 可复核措辞
- scrim 45% 观感确认仍挂账；`.codex/`/`AGENTS.md` 归 omar

## Verify
- `pnpm test` → 314/314（32 文件）
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml` → 全绿 0 failed（Rust 零 diff；verifier 报 94+3 e2e，与旧账 155 计数口径差异待下轮核对）
- `pnpm lint` && `pnpm exec prettier --check .` && `pnpm build`
- `cargo clippy --workspace --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings`

## Modified Files
- 分支 `reshape/ui-v2`：`9161920`（W1/W2 + updater 降噪 + ADR-023/plan 文档）→ `1fb7b1a`（W3/W5/W6）→ `d934884`（W4 + design-spec v0.15 draft + CHANGELOG）
- 关键新增：`src/components/scene/`（7 文件）/ `src/stores/prompt/`（12 切片）/ `src/hooks/useFocusRestore.ts` / `src/components/__tests__/ScenePanelFocusRestore.test.tsx` / `docs/adr/023-*.md` / `docs/plans/ui-reshape.md`
