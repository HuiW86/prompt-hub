---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-03
status: active
audience: [ai]
description: M-X.2 MCP server closed. /review fix-scope A + mutex chosen and shipped (commit 29020bc) — confidence trust boundary (non-finite reject + 0..1 clamp), schema_version rejection at create/update/import boundaries, 15x mutex poison recovery. Doc ripple done (features v0.5, CLAUDE.md §7). Tests 45/45, clippy 0, fmt clean. Deferred P2 (create_draft size cap, mid-session schema recheck) logged for M-X.3/4. Next: M-X.3 UI inbox.
related:
  - CLAUDE
  - mcp-write-pipeline
  - 06-prd
  - 015-expose-mcp-write-pipeline
---

# Handoff — M-X.2 收口（review 加固 ship + 文档涟漪）

<!-- Generated by /checkpoint at 2026-06-03 18:20 -->

## Objective

把 M-X.2（14 tool MCP server）`/review` 选出的 fix 范围落地，并完成 §7 八步文档涟漪，给 M-X.3 UI 收件箱清场。

## Completed

- **fix-scope A + Mutex 恢复已 ship**（commit `29020bc`，working tree clean）：
  - **信任边界 ①** `tools.rs::into_provenance` 改返 `Result`：非有限 `confidence`（NaN/Inf）拒绝，有限值 clamp `0.0..=1.0`。
  - **信任边界 ②** 新增 `LATEST_SCHEMA_VERSION` + `PayloadArg::validate()`，在 `create_draft` / `update_draft` / `import_json`（逐条）边界拒绝 `schema_version != 1`。
  - **Mutex 恢复** `server.rs` 15 处 `lock().expect(...)`（14 db + 1 import_log）→ `lock().unwrap_or_else(|e| e.into_inner())`。
  - 加 4 个单测（clamp / 非有限拒绝 / schema 拒绝 / schema 默认通过）。
- **文档涟漪**：`docs/design/07-features.md` v0.4→v0.5（§3.7 `prompt-hub-mcp` binary + 14 MCP tool 全转 `done`，加 v0.5 changelog）；`CLAUDE.md §7` 状态指针 → M-X.2 已 review+加固、features v0.5、下一动作改 M-X.3。
- 验证全绿：`cargo test --workspace` 45/45、clippy 0、fmt clean。

## In Progress

- 无。working tree clean。

## Next Actions

1. **进 M-X.3 UI 收件箱**：Scene 全景区「📥 草稿」tab + 顶部待审 badge（仅 N>0）+ `promote_draft`/`update_draft`/`discard_draft` Tauri IPC（`src-tauri/src/commands.rs`）。promote 入参含 `group_kind?` 对齐 [[06-prd#10.3]]。
2. **顺带补 mid-session schema recheck**（defer 自 /review P2）：每写 tool 起头 re-check `user_version`（`server.rs` handler 头部，或包一层 helper）。
3. **并行待办**（独立于 M-X）：ADR-012 Phase 5 `pnpm tauri dev` 6 步视觉 diff（overlay frame / ⌥Space 唤起 / PhaseBar 8 段 / AlignmentPhrases chip flash / MacroGrid Flame / StatusBar ⌘K）+ bench 脚本 + M0-4 Developer ID 签名 spike。

## Risks & Decisions

- **已决策**：/review 7 条 P2 中选 **范围 A（两条信任边界）+ Mutex 恢复** 立即补，已 ship。
- **Defer 留档（未做，记 TODO）**：
  - `create_draft`/`update_draft`/`save_conversation`/`bootstrap` 单写路径 **无 payload size cap**（`import_json` 有 64KB）→ M-X.4 随 250MB DB guard 一起。
  - **mid-session schema recheck**（`db.rs` 只启动校验，app 中途迁移后长连接写陈旧 schema）→ M-X.3 顺带补（见 Next Action 2）。
  - `list_compositions` 返全表全字段（token 放大）/ 3 read tool Rust 过滤而非 SQL → 本地小数据可接受，不改。
  - `open_write_checked` WAL 未断言 / DB 中途删无重连 → today fine。

## Verify

```bash
cd /Users/apple/WeChatProjects/prompt-hub/src-tauri
cargo test --workspace                                  # 期望 45/45
cargo clippy --workspace --all-targets -- -D warnings  # 0 警告
cargo fmt --check                                       # clean
git log --oneline -1                                    # 29020bc hardening + 涟漪
```

## Modified Files

- `src-tauri/crates/prompt-hub-mcp/src/tools.rs` — confidence 校验 + schema_version validate + 4 测试
- `src-tauri/crates/prompt-hub-mcp/src/server.rs` — 边界接 validate/Result provenance + 15x mutex 恢复
- `docs/design/07-features.md` — v0.5（§3.7 全 done + changelog）
- `CLAUDE.md` — §7 状态指针 M-X.2 收口
