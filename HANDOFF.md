---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-05-25
status: active
audience: [ai]
description: ADR-012 Phase 4 设计文档涟漪完成（3 atomic commits 1aa8324 / fc20a97 / 2acc168，design-spec v0.7 + product-spec v0.6 + ADR-013 + features v0.2 + mockup 归档 + CLAUDE §7 + MANIFEST v1.1，verify 全过）；下一会话起手 Phase 5 manual `pnpm tauri dev` 6 步视觉 diff + bench 脚本落地 + M0-4 签名 spike + Claude Design pipeline
related:
  - CLAUDE
  - MANIFEST
  - CLAUDE-DESIGN
  - claude-design-prompts
  - 012-lock-visual-quality-anchor
  - 013-alignment-phrases-tab-inclusion
  - prompt-hub-mvp
---

# Handoff — ADR-012 Phase 5 manual verify + 第一阶段收尾 backlog

<!-- Generated by /checkpoint at 2026-05-25 -->

## Objective

ADR-012 Claude Design handoff 收官的最后两件事：

1. **Phase 5 manual `pnpm tauri dev` 6 步视觉 diff** — 人主导（视觉锚点是 bundle preview HTML / Claude Design output）
2. **第一阶段收尾 backlog** — bench 脚本（`bench:cold-start` / `bench:hotkey-wake`）落地替换 M0-3 lib.rs 内嵌仪表 + M0-4 Developer ID 签名 spike + Claude Design pipeline 跑通（上传 CLAUDE-DESIGN.md 创建 design system → Task β 验证管线）

Phase 1/2/3/4 全部 commit 落盘且工作树 clean，verify 五项全过；本次会话三个 Phase 4 atomic commit 把 spec ↔ 代码 drift 全部消除。

## Completed

### Phase 4-1 (commit `1aa8324`) — design-spec §1-§7 token 命名 sync

- 把 design-spec v0.6 的 `--fs-*` / `--space-*` / `--color-protocol-bg/border` / `--duration-*` / 4 个 ease 全部 sync 到 tokens.css 单一真源命名（`--t-N` / `--s-N` + sub-grid / `--protocol` / `--task` / `--aux` ontology + neutral scale `--canvas/--surface/--border/--fg/--skeleton` / `--d-fast/base/slow` / 单 `--ease`）
- §2.1 字号：6 档收敛到 5 主 + 2 sub-grid
- §2.2 间距：+§2.2.1 组件 anchor 高度 + §2.2.2 opacity token + sub-grid precision tier 主 vs 子决策表
- §2.3 对比度：保留 light WCAG 实测；§2.3.2 dark mode 实测延后 v0.7.1
- §2.4 颜色：ontology hard rule（cross-contamination = 违宪，引 02-constitution#B2）+ neutral scale dark/light 两套
- §2.5 暗色模式：v1.0 占位 → v1.0 已实装（dark default + light @media + `.light` class override + runtime API）
- §3 动画：4 ease → 单 `--ease` 200ms hard cap 双重锁定 02-constitution#C1
- §6 PhaseBar 高度 80px → `--h-phasebar` 44px（视觉权重靠 typography + ontology 非 raw size）
- frontmatter 不动版本号（Stage 1 的 commit semantics）

### Phase 4-2 (commit `fc20a97`) — design-spec bump v0.7 + §8-§13 6 章新增

- §8 视觉锚点：Linear-class polish + Bloomberg terminal × Linear typography manual 目标 + 7 hard exclusions（no shadow / no gradient / no glassmorphism / no skeu / 装饰禁 / radius ≤ 6px functional / animation ≤ 200ms）+ anchor 同步策略
- §9 typography presets：7 个 `.ph-*` preset（region-header / card-title / card-body / meta / hotkey / empty / code），固化字号+字重+行高+tracking+颜色
- §10 组件 pattern：border-only baseline + 共享 primitive 三件套（RegionHeader / EmptyState / Kbd）+ v1.0 8 组件清单
- §11 states：9 态契约（default / hover / active / selected / focused / disabled / flash / loading / empty）+ focused outline 统一 `--protocol`（ontology 例外，a11y 标准）
- §12 icon 系统：lucide-react@^0.460.0 / 14px / `--op-icon` 0.7 / icon 不承载 ontology（跟随 text color）+ v1.0 已用 Flame / Search
- §13 视觉权重：三层规约 + cross-contamination = constitutional violation + code review 自检三问 + 视觉权重不靠尺寸蛮力
- frontmatter version v0.6 → v0.7 / status pre-code → ratified / related 加 02-constitution + CLAUDE-DESIGN
- 修订记录加 v0.7 条目（含 Stage 1 + Stage 2 完整描述）

### Phase 4-3 (commit `2acc168`) — 涟漪 7 文件

- **新增** `docs/adr/013-alignment-phrases-tab-inclusion.md`（`Accepted` 2026-05-25）：追认 ADR-012 Phase 3 已 ship 的 AlignmentPhrases 独立 region + Tab cycle 5 → 6 tab-reachable；3 options（A elevate / B revert / C remove）Option A 拍板
- **修改** `docs/design/03-product-spec.md` v0.5 → v0.6：§4.0.4 UI 共用 7→8 模块 / §4.1 一屏全景 7→8 区域 / §13.2 mermaid 加 AP 节点（顺手修 `#1D9E75` 旧绿 → `#178561` 新绿）/ §13.3 新增「区域 2-bis 对齐话术」/ §13.4 Tab cycle 5→6；新增「修订记录」章节
- **修改** `docs/design/07-features.md` v0.1 → v0.2：S1 5 模块 + AlignmentPhrases chip 行 + 跨模块 6 项 P0 → `done`；复制即隐藏 / UsageRecord → `in-progress`；§7 当前阶段重写含 commit 哈希 + 测试通过数；status pre-code → in-progress
- **修改** `CLAUDE.md` §7 当前状态指针：项目阶段 → "ADR-012 Phase 1-3 全 ship + Phase 4 涟漪进行中"；ADR 进度 11 → 12 Accepted + ADR-013
- **归档** `git mv docs/mockups/prompt-hub.html → docs/mockups/archive/v1-engineer-aesthetic.html`（保留作为 ADR-012 前视觉对比基线）
- **修改** `docs/MANIFEST.md` v1.0 → v1.1：design-spec v0.7 / product-spec v0.6 / features v0.2 / ADR 12→13 / mockup 归档
- **修改** `docs/design/CHANGELOG.md`：2026-05-25 条目（覆盖 Phase 4 三 commit 序列 + ADR-013）

### Verify ✓

- ESLint 0 errors
- Vitest 55/55 ✓（1.75s）
- pnpm build 915ms ✓
- prettier --check 全过
- cargo test 12/12 ✓

## In Progress

无。工作树 clean，Phase 4 闭合干净。

## Next Actions

### Phase 5 — `pnpm tauri dev` 6 步 manual verify（omar 主导）

1. **手动启动 + 视觉 diff 截图** 6 步：
   - Step 1：`pnpm tauri dev` 启动；overlay-frame inset 12px 与 bundle preview 一致
   - Step 2：⌥Space 唤起测试（仍 ≤200ms P95，参考 M0-3 baseline 10.49ms）
   - Step 3：PhaseBar 8 段 segmented + active 状态视觉（`--protocol-8` fill + 2px `--protocol` underline）
   - Step 4：AlignmentPhrases chip 行 + 切换 phase 触发 copy + chip flash `--protocol-16`
   - Step 5：MacroGrid 3 列卡 + `Flame` icon + ScenePanel tab + RecentList ellipsis + SopProgress 灰底
   - Step 6：StatusBar dot proto + 今日复制 N 次 + 4 个 Kbd sm + ⌘K SearchBar focus（focus outline `--protocol`）
2. **macOS Space fullscreen bug 回归** — 之前 commit `0fbab2d` 修过，验证 still OK
3. **多屏 / `primary_monitor()` 外接显示器场景** 验证（HANDOFF backlog 长期项）

### 第一阶段收尾 backlog

4. **bench 脚本落地** — `pnpm bench:cold-start` + `pnpm bench:hotkey-wake`（Vitest bench），替换 M0-3 lib.rs 内嵌仪表（CLAUDE §2 已留 TODO 注释）
5. **M0-4 签名 spike** — 等 Developer ID 证书，跑通 `pnpm tauri build` 出 .dmg / .app.tar.gz 签名
6. **Claude Design pipeline 跑通** — omar 上传 `docs/design/CLAUDE-DESIGN.md` 到 claude.ai/design 创建 design system → 跑 Task β 单组件 MacroGrid 卡验证整条管线（[[claude-design-prompts]] §β prompt 模板）→ 若不达标走 L5 失效检测（MANIFEST §7）

### Phase 6（可选 backlog）

7. **「几个 bug」采集** — omar 之前提过未列具体清单，建议续作前明确列出
8. **ADR-011 Reserved unblock**（search UsageSource 扩展 — 详见旧 HANDOFF backlog）
9. **Dark mode 对比度实测** → design-spec v0.7.1（§2.3.2 留的待办）

## Risks & Decisions

### Decisions（不可逆，已落本次三 commit）

- **design-spec v0.7 ratified** — token 命名以 tokens.css 为单一真源，所有命名约定 `--t-N / --s-N / --protocol / --task / --aux / --canvas / --surface / --border / --fg / --d- / --ease` 固化；任何 token 命名变更需走方法论 §7 八步 + 涟漪 design-spec
- **product-spec v0.6 ratified** — AlignmentPhrases 升格独立 region，一屏全景 8 区域 + Tab cycle 6 tab-reachable；未来若要 revert 需推翻 ADR-013
- **ADR-013 Accepted** — 追认 Phase 3 已 ship 的代码事实，不引入新代码改动
- **mockup v1 归档** — `docs/mockups/archive/v1-engineer-aesthetic.html` 保留作为视觉对比基线；v2 路径预留 `docs/mockups/v2/prompt-hub-design-system/` 等 Claude Design 落盘
- **features v0.2** — 状态字段升级到 `done`，但「测试覆盖」列暂以「55 集成 / 单元未量化」表达，待 11-test-spec 启动后量化

### Risks

- **Phase 5 视觉 diff 主观** — 无 pixel-perfect 自动对比，靠人眼对 bundle preview HTML 截图比对；omar 主导（建议 step 1-6 边跑边截图存档到 `docs/mockups/v2/` 或 vault）
- **「几个 bug」未采集** — omar 之前提过但未列具体清单，Phase 5 前建议补述
- **多屏验证未跑** — `primary_monitor()` 单屏假设外接显示器场景未验证（HANDOFF backlog 长期项）
- **Claude Design pipeline 待跑** — CLAUDE-DESIGN.md 已 sticky context 就绪，但 Task β 真实跑通前管线效能未知；若不达标可能需要 L5 派生 bump
- **bench 脚本待落** — M0-3 lib.rs 内嵌仪表 P95=10.49ms 是单次测量，正式 benchmark 待 `bench:*` 脚本就绪后跑出统计稳定值
- **dark mode 对比度未实测** — design-spec §2.3.2 留了 7 条关键组合占位，v0.7.1 补；当前所有 dark 颜色都是 tokens.css 已实装值，理论 ok 但缺数据背书

## Verify

```bash
pwd                                                      # /Users/apple/WeChatProjects/prompt-hub
git log --oneline -7                                     # 期望: 2acc168 + fc20a97 + 1aa8324 + e1ee72f + acf8229 + 26bc82e + 9a822d8
git status --short                                       # 期望: 本 checkpoint commit 后干净

# Phase 4 落盘后回归（必须全过，无代码改动应 100% ok）
pnpm lint                                                # 0 errors
pnpm test                                                # 55/55
pnpm build                                               # ≤ ~1s
pnpm exec prettier --check .                             # all match
cargo test --manifest-path src-tauri/Cargo.toml          # 12/12

# Phase 5 手动启动（omar 主导）
pnpm tauri dev                                           # 6-step visual diff vs bundle preview
```

## Modified Files

### 本 checkpoint commit

- `HANDOFF.md`（本次更新）

### Phase 4 commit 已落盘（10 files across 3 commits）

**Stage 1 `1aa8324`**：
- `docs/design/05-design-spec.md`（§1-§7 重写，frontmatter 未 bump）

**Stage 2 `fc20a97`**：
- `docs/design/05-design-spec.md`（§8-§13 新增 + frontmatter bump v0.7 + 修订记录）

**Stage 3 `2acc168`**：
- `docs/adr/013-alignment-phrases-tab-inclusion.md`（新建）
- `docs/design/03-product-spec.md`（v0.5 → v0.6）
- `docs/design/07-features.md`（v0.1 → v0.2）
- `docs/design/CHANGELOG.md`（2026-05-25 条目）
- `docs/MANIFEST.md`（v1.0 → v1.1）
- `CLAUDE.md`（§7 阶段指针）
- `docs/mockups/prompt-hub.html` → `docs/mockups/archive/v1-engineer-aesthetic.html`（git mv）

### Phase 5 待修改（无 spec 改动；含 backlog）

- `package.json` + `bench/*.bench.ts`（新增 bench 脚本）
- `src-tauri/lib.rs`（删 M0-3 内嵌仪表，替换为 bench 脚本调用）
- `docs/mockups/v2/prompt-hub-design-system/*`（Claude Design 落盘产物）
- 验收截图存档位置待定（建议 `docs/mockups/v2/screenshots/` 或 vault）
