# Handoff — UX 任务流专项：审计收口 + 批次 A 整理模式落地
<!-- Generated by /checkpoint at 2026-07-12 06:00 -->

## Objective
把走查从「功能可达」升级为「任务流体验」：静态审计 → omar 六项裁决 → 按垂直批次修复。本会话完成审计基线 v2 + 批次 A（D-0 整理模式等 6 项）并通过对抗审查；批次 B（跨 Scene 移动）卡在 ADR-022 审议。

## Completed
- **UX 任务流静态预审 v2**（3 个 Opus agent 并行只读扫描 + omar Request changes 修订）：`docs/research/2026-07-12-ux-taskflow-audit.md`——9 任务基线（三级证据等级）、26 条分级问题池（7 P1 + 1 P1 candidate + 14 P2）、§0 裁决记录 D-0~D-6、批次 A-D 方案；`docs/research/README.md` 索引已更
- **ADR-022 草稿 Proposed**：`docs/adr/022-cross-scene-phrase-move.md`——独立 `move_phrase` + MoveReceipt 撤销 + 分层选择器，3 Options + 3 子决策
- **批次 A 落地**（分支 `feat/organize-mode-batch-a`，Opus executor，10 commit）：
  - D-0 整理模式：`settingsStore` interactionMode 持久化 + Header `ModeToggle`（segmented，aria-pressed）+ 整理态整卡=预览/复制显式化（`ScenePanel.tsx:894/942`）+ `record_usage` suppressHide 门控（`commands.rs:19-22`，隐藏与 usage 统计解耦），点击语义变更仅限 Scene 话术卡（`d76cb68`/`76c800b`）
  - A1-03 promote 后 flashTargetId 定位 + phase 切换（`8d2bad1` + B2 修正 `c98921c`）
  - A1-04 discard 可撤销 toast（软状态 restore_draft 反向写，dedup 冲突诚实报错，`798ced5`）
  - A1-07 保存成功 toast（`d08d89e`）/ A1-08 提交键统一 ⌘Enter + Enter 移焦（`6f91a35`）/ A3-02 `.editorActions` flex-wrap（`9bbf394`）
- **verifier 对抗审查 PASS**（独立复跑门禁：前端 301/301、cargo workspace 全绿、clippy -D warnings 零告警；16 项逐条证据判定，0 P0/P1）
- **P2 处置**：`ccb0cb7` 取消跟踪 `6f91a35` 误提交的 `.codex/`+`AGENTS.md`（文件留磁盘）；Toast.module.css:41 新 `font:` 简写债记入审计 C-0 必纳清单

## In Progress
- **product-spec v0.15 模式契约 diff 提案**已在会话输出等 omar 审：§4.0.2 退出语义加"调用态"限定 / 新增 §4.0.7 交互模式契约（两态语义表+控件形态）/ §4.0.5 边界注"主形态整理态承接轻量整理" / §4.3 加整理态行 / §4.4 补 3 条新反馈 / 上游 01-spec §2.3 两字限定（人主笔，仅提案）

## Next Actions
1. **omar 审 ADR-022**（`docs/adr/022-cross-scene-phrase-move.md`，含子决策 2"双路径是否收敛"勾选）→ Accept 后启动批次 B executor：`repo-write/phrases.rs` 新增 `move_phrase`（事务校验 target SubStage∈target Scene + MoveReceipt）+ `commands.rs`/`lib.rs` 注册 + `promptStore.ts` action + `ScenePanel.tsx` 动作簇"移动到…"分层选择器
2. **omar 审 product-spec v0.15 diff 提案**（见 In Progress）→ 通过后走方法论 §7 八步落盘 + `07-features` 回写
3. **真机走查**（dev server，1024×640 基线）：verifier NEEDS HUMAN 4 项（整理态连续整理不被踢出 / ModeToggle 窄 Header 挤压 / 整理态长话术展开网格溢出 / 撤销 toast 6s 够不够）+ 审计"待真机确认清单"6 项（重点 A1-05 焦点丢失定级）
4. **批次 A 分支处置 omar 定**：`feat/organize-mode-batch-a`（10 commit）合并 main / push 时机；若在意提交卫生，合并前 rebase 把 `6f91a35` 混入的 4 个 docs 文件挪回独立 docs commit
5. **批次 C/D 发布后排期**（审计 §6：C-0 映射表→C-1a 等价迁移→C-1b 新角色回流→C-2 调值；D 响应式底线，注意 D-4 已否决主形态 minWidth）
6. 上会话 3 commit（`a24c7c0`/`4b722c1`/`716bd4c`）补 verifier 对抗审查 (carried from 2026-07-07)
7. push 积压 commit + 盯 `bench-c1` CI 首跑（main [ahead 9] + 本分支；headless 窗口可能创建失败）(carried from 2026-07-06)
8. 可发现性裁决 ×2：hover 动作簇默认微显（=审计 A1-10）/ 场景删除入口外露 (carried from 2026-07-06)
9. Dock 图标正式素材（violet hub mark 黑底无留白、与应用内蓝 logo 不一致）(carried from 2026-07-07)
10. omar 人审共创文档：ADR-021 / product-spec v0.14 §13.3 / design-spec v0.14 §12.4；scene.color 用户内容色待裁 (carried from 2026-07-06)
11. P0-2 Composition 链路 ADR（⌘N 工作台 vs 最小查看/删除 UI，`DraftInbox.tsx:43-48`；P0-5 promote 止血闸解锁系于此）(carried from 2026-07-06)
12. v0.1.0 交付余量：落盘日志立项（tauri-plugin-log）/ 用户侧安装·快捷键·FAQ 文档 (carried from 2026-07-06)
13. tag dry-run 验 `.dmg` 端到端 + ADR-017 Phase 6 updater 真机验收（可同批）(carried from 2026-07-02/06)
14. 评估 P2 余 4：release notes 桥接 / 回滚承诺纸面化 / 发布烟测 checklist / ScenePanel+promptStore 上帝文件拆分 (carried from 2026-07-06)
15. ops-spec §3 定时备份立项（`repo-core/src/backup.rs` 底座就绪）(carried from 2026-07-06)
16. 走查余账：P1-5 Phase 可配置性待 omar；P2 余 6（排序死维度/UsageRecord 清理/魔法数/空态 CTA/toast 单例/ADR-011）；Macro 网格末行 auto-fit 拉伸（与审计问题池"已知收编"节对齐）(carried from 2026-07-06)
17. 真机复验批次（旧账）：HotkeyBanner 观感 / updater 进度条 / 空子阶段 muted 列 / scene.color / 暗 band / light 主题 / SettingsModal 焦点 / ErrorBoundary 视觉——可与 Next 3 同场走查 (carried from 2026-07-02/06)
18. CLAUDE-DESIGN v0.2 重传 + v0.1.0 发布时机（omar 外部依赖）(carried from 2026-07-02)
19. 评审遗留：ipc-contract 扩扫 / `--color-danger` 补 design-spec 上游 (carried from 2026-07-02)
20. PRD §6.1 Modifier soft-delete vs 硬删矛盾 + 06-prd `status: pre-code` 僵尸待 omar（注意与 D-5"Phrase soft-delete 独立立项"合并考虑）(carried from 2026-07-02)
21. ai-dev-lifecycle 仓收尾：提交 `/walkthrough` 收编 3 文件改动 (carried from 2026-07-06)

## Risks & Decisions
- **D-0~D-6 六项产品裁决已定**（见审计文档 §0），后续实现不得偏离；整理态 usage 照计（隐藏与统计解耦）是 executor 附带语义点，需随 product-spec v0.15 一并确认
- **提交卫生**：`6f91a35`（feat 提交）混入 4 个 docs 文件 + 曾混入 .codex/AGENTS.md（后者已 `ccb0cb7` 摘除）；分支未 push，rebase 与否 omar 定
- **AGENTS.md / .codex/** 为本地 Codex 工具文件，已回归未跟踪；AGENTS.md 是 CLAUDE.md 机器翻译且带坏引用（`~/.Codex/AGENTS.md` 等），是否修/删归 omar
- 待 omar 旧裁决 ×3 沿袭：Modifier 复制 UsageSource 枚举 / Scene 删除级联 / scene.color（carried）
- ADR-005 Proposed（等 omar 提供 prompt-combiner 仓）、ADR-011 Reserved、ADR-022 Proposed——三个非终态 ADR
- 批次 A 的 NEEDS HUMAN 4 项未 live-confirmed 前，整理模式不应写进对外发布说明

## Verify
- `pnpm test` → 301/301（31 文件）
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml` → 147 用例全绿
- `pnpm lint` && `pnpm exec prettier --check .` && `pnpm build`
- `cargo clippy --workspace --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings`

## Modified Files
- 分支 `feat/organize-mode-batch-a`（自 `ec5dc31` 起 10 commit）：
  - `src-tauri/src/commands.rs`（suppressHide 门控 + hide_after_copy 纯函数）/ `src-tauri/crates/repo-write/src/draft_repo.rs`（mark_restored）
  - `src/stores/settingsStore.ts`（interactionMode）/ `src/stores/promptStore.ts`（promoteDraft 返回 phaseId）/ `src/stores/toastStore.ts`（showWithAction）
  - `src/components/ModeToggle.tsx`（新）/ `ScenePanel.tsx` / `DraftInbox.tsx` / `ModifierGrid.tsx` / `Header.tsx` / `primitives/PhraseFormEditor.tsx` / `primitives/primitives.module.css` / `Toast.module.css` 及配套测试（+24 前端用例）
  - `docs/research/2026-07-12-ux-taskflow-audit.md`（新，v2）/ `docs/research/README.md` / `docs/adr/022-cross-scene-phrase-move.md`（新）
- `HANDOFF.md`（本文件）/ `docs/.metrics/cochange.log`
