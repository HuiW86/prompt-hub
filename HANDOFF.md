---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-02
status: active
audience: [ai]
description: M-X.1 core closed except 2 stubbed promote arms. prompt-hub-mcp skeleton crate added (repo-core only) with trybuild compile_fail guarding write-isolation; CLAUDE.md §2 doc-sync (--workspace) + §7 status refresh. cargo test --workspace 28/28 + clippy + fmt green. BLOCKED on omar's Modifier/Macro promote field-mapping decision before M-X.1 can fully close.
related:
  - CLAUDE
  - mcp-write-pipeline
  - 06-prd
  - 09-tech-stack
---

# Handoff — M-X.1 数据层 + workspace 重构（收口前：等 omar promote 字段决策）

<!-- Generated by /checkpoint at 2026-06-02 -->

## Objective

M-X.1（plan `docs/plans/mcp-write-pipeline.md#§8`，L 级 12 步）：把 src-tauri 拆成 Cargo workspace 4 crate，落 drafts staging 数据层 + DraftRepo/ReadOnlyAssetRepo/AssetRepo trait 双层 + promote 跨表事务，编译期硬隔离 MCP（只依赖 repo-core）。本 session 完成第 4 个 crate（prompt-hub-mcp skeleton）+ trybuild 编译期边界 + CLAUDE.md doc-sync，并把 Modifier/Macro 字段映射整理成给 omar 的决策摘要。M-X.1 核心收口，**仅缺**两类 promote arm，卡在 omar 决策。

## Completed（commit `cb5bb93`）

- **新建第 4 crate `crates/prompt-hub-mcp`**（已入 workspace members）：
  - `Cargo.toml` — **只**依赖 `repo-core`，无 `repo-write`；dev-dep `trybuild`。`[[bin]]` name=prompt-hub-mcp。
  - `src/main.rs` — skeleton bin：从 argv[1] 或 `PROMPT_HUB_DB` 取路径 → `repo_core::db::open_read_only`（永不 migrate，R1）→ 报 schema version。诊断全走 stderr，stdout 留给 M-X.2 JSON-RPC。14 tool 不在本里程碑（属 M-X.2）。
  - `tests/trybuild_negative.rs` + `tests/ui/import_repo_write.rs`（+ 生成的 `.stderr`）：`use repo_write::AssetRepo` 必须 `compile_fail`（E0432 unresolved crate）——编译期写隔离守卫。重生成 stderr 用 `TRYBUILD=overwrite cargo test -p prompt-hub-mcp`。
- **CLAUDE.md doc-sync**：§2 `cargo test` → `cargo test --workspace`（裸 `--manifest-path` 只测 bin pkg=0 测试，真实用例在 3 子 crate）；`cargo clippy` 加 `--workspace --all-targets`（覆盖 trybuild test target）；§7 状态指针刷新为 M-X.1 进行中 / cargo 28/28。
- **验证**：`cargo test --workspace` 28/28（21 repo-core + 6 repo-write + 1 trybuild）；`clippy --workspace --all-targets -D warnings` 0；`fmt --check` clean；bin 双 guard path 烟测 exit=1 且 stdout 干净。

## In Progress

- 无新代码。Modifier/Macro promote arm 仍是 step-4 的 `PromoteUnsupported` stub，等下方决策。

## Next Actions

1. **等 omar 拍板 Modifier/Macro 字段映射**（决策摘要已生成，见下 Risks）。回复后：在 `crates/repo-write/src/asset_repo.rs` 加 `insert_modifier` / `insert_macro`，`crates/repo-write/src/promote.rs` 补两 arm + 删对应 `PromoteUnsupported` 分支 + 改 `promote_modifier_and_macro_are_unsupported_pending_decision` 测试。
2. **若 Modifier 选项 (ii)（payload 加 group_kind）**：改 `crates/repo-core/src/models.rs` `DraftPayload::Modifier` → 走方法论 §7：PRD §10.1.2 bump + 涟漪。选 (i)/(iii) 则 schema 不动。
3. M-X.1 收口后走方法论 §7：PRD §6.2 补"promoted composition 例外"注。
4. M-X.1 关闭 → 进 **M-X.2 MCP server 骨架**（`rmcp = "=1.7"` stdio + 14 tool 完整实现 + spawn 进程 JSON-RPC 端到端测试，提纲 [[mcp-write-pipeline#§8]]）。

## Risks & Decisions

- **🚧 待决策 — Modifier/Macro promote 字段映射（决策摘要已发 omar）**：
  - **Modifier**：(a) payload `phase_id`/`scene_id` 在 `modifiers` 表无列 → 建议 promote 时丢弃（modifier 是 phase 无关积木）；(b) `modifiers.group_kind`（NOT NULL，枚举 cognition/action/delivery/constraint）payload 没有 → 三选项：(i) 默认 `'cognition'`（省事但污染分类）/ (ii) 加进 payload 让 Claude 填（最准，但触发 §10.1.2 schema bump）/ (iii) promote UI 手选（人审把关，不动 schema）。**AI 倾向 (iii)**，次选 (ii)。
  - **Macro**：payload `phase_id` 在 `macros` 表无列 → 建议丢弃、按 `scene_id` 归档；其余 `native=0`/`expand_from=NULL`/`role=task=NULL` 走默认（满足 CHECK）。**AI 倾向：确认即落，无歧义。**
- **insert_alignment_phrase is_default=true 约束**：`idx_alignment_phrase_one_default_per_phase`（每 phase 至多一个 default），seeded phase 已各有 default → promote is_default=true 到 seeded phase 会 UNIQUE 冲突。测试用 is_default=false 规避。
- **serde 限制**：`DraftPayload` internally-tagged enum 不支持 `deny_unknown_fields`，未知键静默忽略；缺字段/类型漂移由 promote 时重反序列化兜（已测 `promote_rejects_schema_drifted_payload`）。
- **trybuild stderr 脆性**：rustc 升级可能改诊断文案；仓库无 toolchain pin / CI，本地用 `TRYBUILD=overwrite` 重生成即可。

## Verify

```bash
cd /Users/apple/WeChatProjects/prompt-hub/src-tauri
cargo test --workspace                                              # 期望 28/28
cargo clippy --workspace --all-targets -- -D warnings              # 0 警告
cargo fmt --check                                                   # clean
git log --oneline -1                                               # cb5bb93 feat(mcp): ... (本 checkpoint 之前的代码提交)
```

## Modified Files

- `src-tauri/Cargo.toml` — workspace members 加 `crates/prompt-hub-mcp`
- `src-tauri/Cargo.lock` — prompt-hub-mcp + trybuild 解析
- `src-tauri/crates/prompt-hub-mcp/Cargo.toml` — 新建（dep repo-core；dev-dep trybuild）
- `src-tauri/crates/prompt-hub-mcp/src/main.rs` — 新建 skeleton bin（open_read_only + schema 报告）
- `src-tauri/crates/prompt-hub-mcp/tests/trybuild_negative.rs` — 新建 trybuild 驱动
- `src-tauri/crates/prompt-hub-mcp/tests/ui/import_repo_write.rs` — 新建 compile_fail fixture
- `src-tauri/crates/prompt-hub-mcp/tests/ui/import_repo_write.stderr` — 新建期望诊断
- `CLAUDE.md` — §2 命令加 --workspace/--all-targets + §7 状态刷新
