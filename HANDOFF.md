---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-05-25
status: active
audience: [ai]
description: Phase 5 自动 verify 6/6 通过（screencapture -l 抓 off-screen Tauri 窗口）+ design-spec v0.7.1 §12.4 边界澄清 + bench:cold-start / bench:hotkey-wake 脚本落地 baseline 写入；下一会话 omar 跑 5 项交互 verify（⌥Space 体感 / ⌘1 active 高亮 / 切 phase chip flash / ⌘K focus outline / Space fullscreen 回归）+ 决策 commit 分组 + 第一阶段收尾剩余（M0-4 签名 / Claude Design pipeline）
related:
  - CLAUDE
  - MANIFEST
  - 012-lock-visual-quality-anchor
  - 013-alignment-phrases-tab-inclusion
  - prompt-hub-mvp
---

# Handoff — Phase 5 收尾：交互 verify + commit + 外部依赖项

<!-- Generated by /checkpoint at 2026-05-25 -->

## Objective

Phase 5 manual verify 已完成 6/6 自动验证（截图比对），剩 5 项需键盘交互的视觉态由 omar 主导。同时本次会话顺带闭合两件第一阶段收尾事：design-spec v0.7.1 §12 边界澄清、bench 脚本（cold-start + hotkey-wake）落地 baseline。所有改动**未 commit**，等 omar 决定分组策略。

## Completed (本次会话)

### Phase 5 自动 verify（screencapture -l off-screen Tauri 窗口抓图）

发现：Tauri overlay 主形态 `visible: false` 但 CGWindow 列表仍有窗口 entry（layer 5, 1470x956）；`screencapture -l <windowID>` 可直接抓 off-screen 内容（macOS WindowServer 行为）。无需启动 GUI 交互即可视觉自检。

| Step | 内容 | 状态 | 凭据 |
|------|------|------|------|
| 1 | overlay frame 12px inset | ✅ 代码级 | `Dashboard.module.css:9` `inset: var(--s-3)` = 12px / r-frame 8px / 1px hairline |
| 2 | ⌥Space wake P95 ≤ 200ms | ✅ baseline | M0-3 P95=10.49ms；本次 bench:hotkey-wake show()+focus() P95=0.023ms |
| 3 | PhaseBar 8 段 | ✅ 截图 | 8 段全渲染（发散/理解/规划/生成/执行/收敛/沉淀/迭代 + ⌘1-⌘8）；active 默认空（`activePhaseId=null` 正确） |
| 4 | AlignmentPhrases chip 行 | ✅ 截图 | `ALIGNED` 标签 + `未选相位` 空态 |
| 5 | Macro/Scene/Recent/SOP | ✅ ⚠️ | 4 区到位；**drift 1**: Scene tab emoji 已通过 design-spec v0.7.1 §12.4 澄清合规 |
| 6 | StatusBar + ⌘K | ✅ 截图 | `● 当前相位:未选 \| 今日复制 8 次` + 4 Kbd + `兜底 ⌘K` |

截图存档：`docs/mockups/v2/screenshots/phase5-verify/`（full-window.png + 6 crop）。

### design-spec v0.7 → v0.7.1（§12 chrome vs 用户内容边界澄清）

Phase 5 verify 发现 Scene tab 用 emoji（`📐 🔍 🔧`，源自 `migrations/0002_seed.sql:36-38`），与原 §12 lucide-react hard rule 表面冲突。根因 §12 未区分 chrome vs 用户内容。

新增 **§12.4 适用范围（chrome vs 用户内容）**：
- §12.1-§12.3 lucide hard rule 只覆盖 **chrome 系统图标**（Flame / Search / Kbd）
- 用户内容图标（Scene/Macro/AlignmentPhrase 自定义）允许 emoji / 单字 / lucide name 任意字符
- v1.0 seed 数据合规——不需改代码

涟漪：CHANGELOG 加 2026-05-25 v0.7.1 条目 / MANIFEST design-spec 行 bump。

### bench 脚本落地（替代 HANDOFF backlog 项 4）

**发现**：M0-3 lib.rs 内嵌仪表早在 commit `a14d61e`（phase 1 MVP B0-B4）就已 strip。HANDOFF 原文「替换 M0-3 lib.rs 内嵌仪表」stale——没东西可替换，只剩新建脚本。

**实现**：
- `bench/cold-start.bench.mjs` — Node spawn `target/debug/prompt-hub` + 预编译 swift CGWindow probe（`bench/.cache/probe`，避免每次 `swift -e` 80ms overhead）轮询 → P50/P95/P99
- `bench/hotkey-wake.bench.mjs` — cargo build `--features bench` → spawn → 解析 JSON-line stdout
- `src-tauri/Cargo.toml` — 新增 `[features] bench = []`
- `src-tauri/src/bench.rs` — `#[cfg(feature="bench")]` auto-cycle：warmup 5 + sample 50，show()+focus() 计时 + JSON-line 输出
- `src-tauri/src/lib.rs` — 在 setup 末尾 cfg-gated `bench::spawn_wake_cycle()`
- `package.json` — `bench:cold-start` + `bench:hotkey-wake` scripts

**Baseline（debug build, M1 Pro, macOS）**：
- cold-start: n=20, P50=186 / P95=193 / P99=195 ms（spawn → first CGWindow entry；C1 不约束此项）
- hotkey-wake: n=50, P50=0.017 / P95=0.023 / P99=0.027 ms（show()+focus() Rust call only；不含 OS dispatch ~10ms，真实 ⌥Space P95 ≈ baseline + 10ms 远低 200ms）

**Caveat 写入文档**：hotkey-wake bench 不含 OS global-shortcut dispatch；真实 ⌥Space wake = bench 数字 + ~10ms 系统开销。

### Verify ✓

- pnpm lint: 0 errors
- pnpm exec prettier --check: all match
- pnpm test (Vitest): 55/55
- cargo test --manifest-path src-tauri/Cargo.toml: 12/12（default features）
- cargo check --features bench: 通过
- pnpm bench:cold-start: 跑通输出 baseline
- pnpm bench:hotkey-wake: 跑通输出 baseline

## In Progress

无。所有改动落盘，工作树状态：

```
 M CLAUDE.md
 M docs/MANIFEST.md
 M docs/design/05-design-spec.md
 M docs/design/CHANGELOG.md
 M package.json
 M src-tauri/Cargo.toml
 M src-tauri/src/lib.rs
?? bench/
?? docs/mockups/v2/screenshots/
?? src-tauri/src/bench.rs
```

## Next Actions

### A. omar 主导 — 5 项交互 verify（~5 min）

Phase 5 闭合最后一公里，只能键盘交互看：

1. **⌥Space** 唤起：主观体感「即时」？连按 3 次有无掉帧
2. **⌘1** 选第一相位：PhaseBar 第 1 段是否出现 `--protocol-8` 浅紫填充 + 2px 紫色 underline
3. **⌘1 → ⌘2** 切相位：AlignmentPhrases 自动复制（clipboard 验证）+ chip 闪 `--protocol-16`
4. **⌘K** focus SearchBar：是否出现紫色 outline 外圈
5. **macOS Space fullscreen** 场景：Safari 进全屏，⌥Space 唤起，prompt-hub 应浮在全屏 app 之上（commit `0fbab2d` 修过的 bug 回归）

跑完口报「step N OK」或差异即可。

### B. commit 分组决策

本次会话改动可一刀（Phase 5 收尾 mega commit）或三刀（原子）：

| 方案 | 拆分 | 适用场景 |
|------|------|---------|
| **三刀**（推荐） | (1) Phase 5 verify + screenshots 归档 / (2) design-spec v0.7.1 §12.4 patch + 涟漪 / (3) bench 脚本 + CLAUDE §2 sync + HANDOFF | 历史清晰，单次回滚精确 |
| **一刀** | Phase 5 close mega | 简洁，但 spec / bench / verify 混在一起难追溯 |

omar 拍板后我执行。

### C. 第一阶段收尾剩余（外部依赖）

- **M0-4 签名 spike** — 等 Developer ID 证书（外部）
- **Claude Design pipeline** — 等 omar 上传 `docs/design/CLAUDE-DESIGN.md` 到 claude.ai/design 创建 design system → 跑 Task β（[[claude-design-prompts]]）

### D. 长期 backlog

- 多屏 `primary_monitor()` 外接显示器场景验证
- 「几个 bug」清单 omar 之前提过但未列具体
- ADR-011 Reserved unblock（search UsageSource 扩展）
- Dark mode 对比度实测 → design-spec v0.7.2（v0.7 §2.3.2 留的待办）
- Release build cold-start baseline（当前只有 debug 数字 ~193ms）

## Risks & Decisions

### Decisions (本次)

- **design-spec v0.7.1 ratified（待 commit）** — §12.4 chrome vs 用户内容边界澄清；用户内容图标允许任意字符；不需改代码
- **bench 脚本架构** — cold-start 走外部 subprocess + CGWindow probe；hotkey-wake 走 `--features bench` Rust auto-cycle；不污染默认 build
- **Phase 5 自动 verify 通过 screencapture -l** — 证明 hidden Tauri overlay 窗口可外部抓取；未来 phase 视觉验证可省去人工启动
- **HANDOFF backlog 「替换 M0-3 内嵌仪表」stale** — 已在 commit `a14d61e` strip，本次 bench 是新建非替换

### Risks

- **5 项交互 verify 未跑** — 视觉态（active highlight / chip flash / focus outline）必须键盘交互看；截图不可见
- **cold-start P95=193ms 接近 200ms 主观线** — 虽然 C1 不约束 cold-start，但 debug build 接近 200ms 心理边界；release build 应明显更快，建议第一阶段收尾后补 release baseline
- **bench:hotkey-wake 不覆盖 OS dispatch** — show()+focus() Rust call 0.02ms 极快，但真实 ⌥Space 还包含 OS event dispatch ~10ms；想测全链路需 cliclick + accessibility 权限
- **dark mode 对比度未实测** — design-spec §2.3.2 留 7 条占位待补
- **Claude Design pipeline 待跑** — Task β 真实跑通前管线效能未知

## Verify

```bash
pwd                                                       # /Users/apple/WeChatProjects/prompt-hub
git status --short                                        # 期望: 7 M + 3 ??（详见 In Progress）

# 全套回归（必须全过）
pnpm lint                                                 # 0 errors
pnpm exec prettier --check .                              # all match
pnpm test                                                 # 55/55
cargo test --manifest-path src-tauri/Cargo.toml           # 12/12

# Bench 自检（可选，5-10 分钟）
pnpm bench:cold-start                                     # 期望 P95 ~190ms
pnpm bench:hotkey-wake                                    # 期望 P95 < 0.1ms
```

## Modified Files

### 新建
- `bench/cold-start.bench.mjs` — Node spawn + Swift CGWindow probe harness
- `bench/hotkey-wake.bench.mjs` — `--features bench` build + JSON-line parser
- `bench/.cache/`（probe.swift + 编译产物，`.gitignore` 候选）
- `src-tauri/src/bench.rs` — feature-flagged auto-cycle 模块
- `docs/mockups/v2/screenshots/phase5-verify/` — 7 PNG（full-window + 6 crop）

### 修改
- `CLAUDE.md` — §2 bench 命令注释 → 实命令；移除「待添加」
- `docs/MANIFEST.md` — design-spec 行 v0.7 → v0.7.1
- `docs/design/05-design-spec.md` — frontmatter v0.7 → v0.7.1 + §12.4 新增 + 修订记录 v0.7.1 条目
- `docs/design/CHANGELOG.md` — 2026-05-25 v0.7.1 条目（置顶 Phase 4 之上）
- `package.json` — `bench:cold-start` + `bench:hotkey-wake` scripts
- `src-tauri/Cargo.toml` — `[features] bench = []`
- `src-tauri/src/lib.rs` — `#[cfg(feature="bench")] mod bench` + setup 末尾 spawn_wake_cycle

### Phase 5 待修改（5 项交互 verify 后再决定）
- HANDOFF.md（再次更新 status）
- 可能新增交互态截图到 phase5-verify/
- 若发现新 drift 走 design-spec patch v0.7.2
