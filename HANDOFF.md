---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-19
status: active
audience: [ai]
description: ADR-017 自动更新——Phase 0 密钥 + Phase 4 CI dry-run 端到端全绿 + Phase 5 文档涟漪全收口（features v1.1 / tech-stack v1.3 / CLAUDE §7 / CHANGELOG / MANIFEST + plan 勾掉）。剩余唯一待办：Phase 6 真机验收。draft v0.1.0 dry-run 产物已删。
related:
  - CLAUDE
  - 017-enable-auto-update
  - adr-017-auto-update
  - 07-features
  - 09-tech-stack
---

# Handoff — ADR-017 自动更新（Phase 0+4 dry-run 全绿 + Phase 5 文档涟漪收口）

<!-- Generated by /checkpoint at 2026-06-19 -->

## Objective
给应用加自动更新能力（`tauri-plugin-updater` + GitHub Releases `HuiW86/prompt-hub` + GitHub Actions 自动出包，mac 先行）。本轮：跑通 Phase 4 CI 端到端 dry-run（前一 checkpoint）+ 完成 Phase 5 文档涟漪 + 删除 dry-run 产物 draft release。

## Completed
- **Phase 0 密钥前置 + Phase 4 CI dry-run 端到端全绿**（前一 checkpoint `8801c43`）：9 secret + `release-signing` Environment + 真公钥；tag `v0.1.0` 触发 run `27855601462` 全绿（双架构 build + minisign 签名 + latest.json 核验）；过程修 4 个真实 CI bug（pnpm version `c2fa90f` / .p12 密码 / Developer ID 证书 localKeyID 抽取 / macOS bash 3.2 关联数组 `db272e5`）
- **draft release v0.1.0 + tag 已删**（dry-run 产物清理）
- **Phase 5 文档涟漪全收口**（方法论 §7，本轮，已 staged）：
  - `docs/design/07-features.md` v1.0→**v1.1**：新增 §3.9 自动更新区（5 功能：updater 客户端接入 / opt-in 总开关+UI / Vite 加固 / CI 出包 → done，真机验收 → planned）；§4 节奏表加 ADR-017 行，合计 51→56；§6 变更日志补记
  - `docs/design/09-tech-stack.md` v1.2→**v1.3**：§3 加 D14（自动更新机制）+ §4.4 updater 子系统（含 A2 出站豁免边界 + 密钥隔离三战线）+ §7 依赖锁加 `@tauri-apps/plugin-process`；frontmatter related 补 016/017
  - `CLAUDE.md §7`：ADR 14→**16 Accepted**（补 016 + 017）+ 新增「自动更新（ADR-017）landed」状态指针行 + tech-stack v1.2→v1.3 + 下一动作更新
  - `docs/design/CHANGELOG.md`：新增 2026-06-19 条目
  - `docs/MANIFEST.md` v1.4→**v1.5**：§8 ADR 表补 016+017（14→16 份）+ §1 概览计数；features→v1.1 / tech-stack→v1.3；顺带校正 prd v0.9→v0.10 / ops-spec v0.2→v0.3 drift；§9 加 adr-017-auto-update plan
  - `docs/plans/adr-017-auto-update.md`：Phase 0/4 勾掉 done + Phase 5 四项勾掉

## In Progress
- 无代码在写。Phase 5 涟漪已 staged 待本 checkpoint commit

## Next Actions
1. **Phase 6 真机验收**（ADR-017 唯一待办）：真机走查 updater opt-in/检查/提示链路（`src/stores/updaterStore.ts` `check()` + `UpdaterBanner.tsx` 四态）+ `pnpm bench:hotkey-wake` 复测守 [[02-constitution#C1]] 200ms（确认启动 check 不拖慢唤起）+ 遗留 #15170 多屏 re-fit
2. **可选 🧑 人主笔**：`docs/design/02-constitution.md` A2 加 ADR-017 指针 note（受限豁免边界）——AI 不得起草（CLAUDE §5.2）
3. **真实首发版本**（未来）：决定 prompt-hub 首个对外版本号，走 `release.yml` 正式出包 + 人核 hash 后 publish（非 draft）

## Risks & Decisions
- **决策**：reviewer 用 `HuiW86` 自审（单人项目，ADR §5.5「非 tag 作者」在单人场景放宽）
- **决策**：检查逻辑放 JS 侧（守 intent 不进唤起热路径）；latest.json 由 sign job jq 手装
- **决策**：Phase 5 顺带校正 MANIFEST 内 prd/ops-spec/ADR-016 版本 drift（既有 MANIFEST 漏登 ADR-016 + prd/ops-spec 版本滞后）
- Developer ID 证书在 System keychain 而非 login——重导 p12 需全域 export + localKeyID 配对（friendlyName 不可靠）
- ADR-017 已 Accepted；A2 处「受限豁免」状态；pubkey 已是真公钥，runtime check() 可验签

## Verify
- `git log --oneline -3`（确认本 checkpoint + `8801c43`）
- `pnpm test`（92）/ `pnpm build` / `pnpm lint`
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml`
- `pnpm bench:hotkey-wake`（Phase 6 守 C1）

## Modified Files（本轮 Phase 5）
- `CLAUDE.md`（§7 状态指针）
- `docs/design/07-features.md`（§3.9 自动更新区，v1.1）
- `docs/design/09-tech-stack.md`（D14 + §4.4 + 依赖锁，v1.3）
- `docs/design/CHANGELOG.md`（2026-06-19 条目）
- `docs/MANIFEST.md`（ADR 表 + 版本校正，v1.5）
- `docs/plans/adr-017-auto-update.md`（Phase 0/4/5 勾掉）
