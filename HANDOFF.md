# Handoff — v0.1.0 阻塞已解除：Apple 协议生效，只差改一个 secret 再重跑

<!-- Generated by /checkpoint at 2026-08-05 19:10 -->

## Objective

把 v0.1.0 发出去。代码、文档、流水线、凭据全部就绪，**Apple 403 已于 2026-08-05 19:08 解除**（notarytool 成功返回提交历史）。剩下的是三步机械动作：改 `APPLE_API_ISSUER` secret → rerun → 审批 + publish。**不要重打 tag。**

## Completed

- **Apple 403 已解除**：omar 于 2026-08-05 接受 Apple Developer Program License Agreement（签发 2026-06-18）后，Notary 侧约 40 分钟生效。本地 `xcrun notarytool history` 已成功返回历史（最早提交 2026-06-04，即 M0-4 那次）。**凭据三件套全部确认可用**
- **两份对外文案逐句对代码核查完毕**（`ca6490d`）。`docs/release-notes/v0.1.0.md` 零改动；`docs/install.md` 修三处失实：`backups/` 非自动备份（`snapshot()` 仅 `pre-import`/`pre-migrate` 两个调用点，`MAX_BACKUPS=5`）、⌘K 不搜 SOP（`src/hooks/useSearchResults.ts:15-16`）、运行时 Dock 有图标。第四处（updater 请求内容）经 omar 判定不改——错误方向偏保守
- **发现并修复公证从未发生**（`319f20c`）：`APPLE_API_KEY_ID` 是 Tauri 不读的变量名、`.p8` 内容错放进 `APPLE_API_KEY`、`APPLE_API_KEY_PATH` 从未设置 → Tauri 静默跳过公证仍返回 0。已修映射（故意交叉并注释）+ `openssl pkey` 硬校验 + **新增 `Assert notarized + stapled` 闸门**（对产物断言：双 `stapler validate` + `spctl -t exec` + `codesign`）
- **`docs/release-runbook.md` §3 重写**：旧文把 `spctl` reject 预先解释成"本地未公证构建的正常现象"，正是这句话让真信号在第一轮被当噪音。现拆分签名/公证为两组独立命令，并写明任何 reject 都不得归因于本地环境
- **首个 draft + tag 已干净回收**，未 publish、外界零感知；v0.1.0 重打在 `319f20c`
- **经验沉淀成文**（`a9d1905`）：新建 `docs/postmortems/`，首份 `2026-08-05-notarization-fail-open.md`——含 fail-open 断言三个同形实例、「能力≠行使」滑移的完整证据链、6 问自查清单；`docs/learnings.md` 信条三 M0-4 证据加限定注记并指回复盘（v0.3 → v0.4）；`docs/MANIFEST.md` §11.5 计数 1→2 并写明两种粒度分工
- **runbook §3 其余各项在真 CI 产物上跑通**：`latest.json` version/两架构/URL 全对；签名 keyID `5A1CC5FCD5ADA7A2` 与内嵌公钥逐位配对；manifest signature 与随包 `.sig` 逐字一致；aarch64 二进制确认 arm64；Actions log 无密钥回显

## In Progress

无代码任务在途，工作区干净（仅 `.codex/` + `AGENTS.md` 未跟踪，归 omar）。

**卡在一个尚未执行的机械动作**：`APPLE_API_ISSUER` 这个 repo secret 里仍是错值（CI 上一轮就是因它报 `must be a valid UUID` 而失败）。正确值已本地验证通过，见 Next 1。

## Next Actions

1. **改 secret**：`gh secret set APPLE_API_ISSUER --repo HuiW86/prompt-hub`，值为 `69a6de8b-9069-47e3-e053-5b8c7c11a4d1`。配套值（不需改动）：Key ID `642AP44DVF` 在 `APPLE_API_KEY_ID`、`.p8` 内容在 `APPLE_API_KEY`，源文件 `/Users/apple/Desktop/证书/AuthKey_642AP44DVF.p8`
2. **重跑，不要重打 tag**：`gh run rerun 30994660968 --failed --repo HuiW86/prompt-hub`。tag `v0.1.0` 已指向 `319f20c`；该 run 在 build 阶段失败、未建 draft、未碰 minisign 私钥。关注新增的 `Assert notarized + stapled` step 是否绿——它现在是公证是否真的发生的唯一权威
3. **审批 + 核对 + publish**：`release-signing` 环境的 sign job **必须 omar 本人在 Actions 页面点**（不得用 `gh api pending_deployments` 代批，否则闸门空转）→ 按 `docs/release-runbook.md` §3 五项核对 → publish。publish 那一刻更新通道首次真实激活（ADR-017 Phase 6 验收时机）
4. **memory 层两条待 omar 点头**（本轮提出未决）：新增 feedback「AI 主笔对外文档必须逐句反查代码，不能读通顺就算过」（与已有 `feedback_walkthrough_coverage` 同族，应互链）；更新 `feedback_decision_autonomy`（授权范围从"方案确认后的实现细节"扩到"失败恢复时的路径选择"，且需展示推理链而非选项菜单）
5. **发布收尾（非阻塞）**：`gh secret set TAURI_SIGNING_PRIVATE_KEY_PASSWORD --env release-signing`；`latest.json` 强制最低版本字段（ADR-017 §5.8 急停第二条）落在 `release.yml` sign job 的 `jq -n` 处；落盘日志 tauri-plugin-log 立项 (carried from 2026-08-05)
6. **`docs/release-runbook.md` 补 §1.5「凭据本地自检」**（本轮提出未做）：把 `xcrun notarytool history --key <p8> --key-id <10位> --issuer <UUID>` 与三值形状对照表写进手册。本次经验是它两秒给出 Apple 原话，而 CI 六分钟且错误被 `***` 遮罩；v0.2.0 还会用到，目前该知识只存在于会被覆写的 HANDOFF 里
7. **`a24c7c0` / `4b722c1` / `716bd4c` 三 commit 补 verifier 对抗审查**——`a24c7c0`「float phrase actions」正是 hover 遮挡的来源 (carried from 2026-07-07)
8. **契约回流八步**：product-spec §4.0/§13.4 + §4.0.7 卡片解剖（title-only）+ 外观设置（density）；design-spec §2/§8/§9 + §3c token 层 (carried from 2026-07-21)
9. **omar 人审批次**：design-spec v0.15 draft + ADR-023/024 措辞 + 图标定稿追认 + product-spec v0.15/v0.16 + features v1.10/v1.11 + 01-spec v0.7 + 旧账 ADR-021/`scene.color` (carried from 2026-07-06)
10. 补 `src/components/__tests__/ScenePanelFocusRestore.test.tsx` 焦点恢复负路径测试 (carried from 2026-07-21)
11. `.github/workflows/ci.yml` bench-c1 `continue-on-error` 处置复核 (carried from 2026-07-12)
12. **可发现性裁决 ×4**：hover 动作簇微显 / 场景删除入口外露 / 整理态保窗 vs 改 D-0 契约 / title-only 后卡面无内容线索——涉 product-spec §4.0.7，改契约走八步 (carried from 2026-07-06)
13. P0-2 Composition 链路 ADR（P0-5 系于此，`src/components/DraftInbox.tsx:43-48`）(carried from 2026-07-06)
14. **ops-spec §3 定时备份**（`src-tauri/crates/repo-core/src/backup.rs` 底座）——因 install.md 修正而加重：已对用户明说"没有自动副本"，落地它可把这句改回去 / P2 余 4 评估 / P1-5 Phase 可配置性 / Macro 网格末行 auto-fit (carried from 2026-07-06)
15. `docs/design/CLAUDE-DESIGN.md` v0.2 重传 + v2 基调同步 (carried from 2026-07-02)
16. 评审遗留补 design-spec 上游：ipc-contract 扩扫 / `--color-danger` / `--scrim` 语义回流（随人审八步）(carried from 2026-07-02)
17. `docs/design/06-prd.md` §6.1 soft-delete 矛盾 + `status: pre-code` 僵尸 / ai-dev-lifecycle 仓收尾 (carried from 2026-07-02)

## Dropped

- **Apple ID 备选公证通道（App 专用密码方式）** —— 上一版 Next 3。403 已由协议生效自然解除，API key 通道确认可用，无需备选，也不必再往对话里贴密码

## Risks & Decisions

- **`release.yml` 的 secret→变量映射是故意交叉的**：`APPLE_API_KEY: ${{ secrets.APPLE_API_KEY_ID }}`。Tauri 的 `APPLE_API_KEY` 要 Key ID 字符串，而仓库同名 secret 存的是 `.p8` 内容。**看着像 bug，不要"顺手改正"**，step 上方注释已说明
- **不得代批 sign job**：闸门的全部意义是注入 minisign 私钥前有人确认。ADR-017 §5.5 已因单人仓库退让一次（自批 + runbook §3 补偿核对），不能再退第二次
- **未解释的弱信号**：Developer ID 证书有效期 2026-06-04 → 2027-02-01 仅 8 个月，Developer ID 正常签 5 年。403 已解除故不再阻塞，但若 2027-02 前后再出公证问题，先查 Membership 到期日
- **hover 动作簇遮挡标题（未修，已知，已写入发布说明「已知问题」）**：`src/components/ScenePanel.module.css:420` `.phraseActions` 为 `position:absolute` + 不透明底，`.phraseTitle`（:446）未预留右侧空间。属 product-spec §4.0.7 契约，**不就地改行为**，并入 Next 12 裁决
- **状态栏「更新失败 · 重试」是预期态非缺陷**：仓库零 release 时 endpoint 必然 404，首个 release publish 后自然消失
- **构建副产物会污染 `/Applications`**：DMG 内含 `Applications` 符号链接。本地构建后须 `lipo -info /Applications/prompt-hub.app/Contents/MacOS/prompt-hub` 确认架构
- **禁止全屏截图**：走查截图须按窗口 ID 定向抓取（曾拍到前台微信机密会话）
- **脚本驱动本应用不可靠**：失焦即隐藏，`osascript` 按键会落到别的前台应用，外观走查只能人工
- dev 裸二进制 WebKit 存储在 `~/Library/WebKit/prompt-hub`，正式 `.app` 在 `dev.prompt-hub`，两套设置互不相通 (carried)
- HMR 后 store 驱动行为异常先重启 dev 进程（Zustand 旧实例）(carried)
- `.codex/` / `AGENTS.md` 归 omar，保持未跟踪 (carried)

## Verify

- `xcrun notarytool history --key "/Users/apple/Desktop/证书/AuthKey_642AP44DVF.p8" --key-id 642AP44DVF --issuer 69a6de8b-9069-47e3-e053-5b8c7c11a4d1` → **已通过**（2026-08-05 19:08）
- `pnpm test` → 331/331（34 文件）
- `pnpm lint` && `pnpm exec prettier --check .` && `pnpm build`
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml` → 155 passed
- `cargo clippy --workspace --all-targets --manifest-path src-tauri/Cargo.toml -- -D warnings`
- `bash scripts/check-version.sh 0.1.0` → 三处版本一致
- `node scripts/doc-governance/index.mjs --config doc-governance.config.mjs` → 0 error / 6 warn（均为既有）
- 本轮全部改动为 docs + workflow，未触前端/Rust；上列代码质量门最后一次全绿在 `993c1d8`

## Modified Files

- `ca6490d`：`docs/install.md`
- `319f20c`：`.github/workflows/release.yml` / `docs/release-runbook.md`
- `a9d1905`：新增 `docs/postmortems/2026-08-05-notarization-fail-open.md` / `docs/learnings.md` / `docs/MANIFEST.md`
- 本 checkpoint：`HANDOFF.md` / `docs/.metrics/cochange.log`
