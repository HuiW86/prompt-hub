# Handoff — Scene 编辑面真机走查收口
<!-- Generated by /checkpoint at 2026-07-07 02:20 -->

## Objective
从「ui好丑」的真机反馈出发，走查并修复 ADR-021 Scene 编辑面暴露的可读性/可达性问题，并对 Scene/SubStage/Phrase 三实体做源码级 CRUD 可达性验收。

## Completed
- **看板可读性修复**（`a24c7c0`）：话术卡动作簇改绝对定位浮层（隐藏态不占 ~130px，修中文标题 3-4 字词中断行）；子阶段列头 `position: sticky`（网格顶 padding 移到列头，滚动不再裁切标题）；`.phrases` 隐藏原生粗滚动条；未分组列去序号+降字重+tooltip，不再伪装成可编辑阶段
- **未分组桶转正**（`4b722c1`）：列头铅笔（命名未分组为子阶段）→ `handlePromoteUngrouped` 组合 `create_sub_stage`+`update_phrase` 批量归入（无新 IPC）；`createSubStage` 改返回创建行；逐条归入，中途失败保留已移+报错可重试
- **根治既有 flake**（`4b722c1` 同批）：properties-panel `update/delete_scene rejection` 偶发——根因 toast dwell 定时器靠单调 seq 守卫但 `setState(reset)` 把 seq 倒回 0，让早期块泄漏定时器重新匹配。测试改用 `clear()`（seq 只增）重置；**8/8 干净通过**
- **术语对齐**（`716bd4c`）：话术编辑器下拉「无分组」→「未分组」（全站统一）
- **CRUD 可达性矩阵走查**：Scene/SubStage/Phrase × 增删改读排序 15 格全部有可达 UI 入口，无隐藏缺失（Scene 排序在属性面板前移/后移；话术跨列移动走编辑器下拉）

## In Progress
- 真机走查：用户在 dev server 验证以上修复观感（标题不折行/列头不裁/滚动条消失/未分组转正）

## Next Actions
1. **本会话 3 commit（`a24c7c0`/`4b722c1`/`716bd4c`）补 verifier 对抗审查**：仅自验（测试+lint+build+8×flake），未过独立 verifier，与工程批 2 口径不一致
2. **push 8 commits + 盯 `bench-c1` CI 首跑**：headless 窗口可能创建失败，push 时机 omar 定（carried from 2026-07-06）
3. **可发现性裁决 ×2**：hover 动作簇默认微显 vs Notion 惯例（现新增浮层 phraseActions/promote 铅笔同属此议题）；场景删除入口外露（现藏属性面板 footer）（carried from 2026-07-06）
4. **Dock 图标需正式素材**：现打包 `e3dbc04` violet hub mark 全出血黑底无 macOS 留白、与应用内蓝 logo 不一致（新增走查发现）
5. **omar 人审共创文档**：ADR-021 / product-spec v0.14 §13.3 / design-spec v0.14 §12.4；待裁 scene.color 用户内容色（carried from 2026-07-06）
6. **P0-2 Composition 链路 ADR**：⌘N 工作台 vs 最小查看/删除 UI（`DraftInbox.tsx:43-48`）；P0-5 promote 止血闸解锁系于此（carried from 2026-07-06）
7. **v0.1.0 交付余量**：落盘日志立项（tauri-plugin-log）/ 用户侧安装·快捷键·FAQ 文档（carried from 2026-07-06）
8. **tag dry-run 验 `.dmg` 端到端** + **ADR-017 Phase 6 updater 真机验收**：可同批（carried from 2026-07-02/06）
9. **评估 P2 余 4**：release notes 桥接 / 回滚承诺纸面化 / 发布烟测 checklist / 上帝文件 ScenePanel+promptStore 拆分（carried from 2026-07-06）
10. **ops-spec §3 定时备份立项**：`repo-core/src/backup.rs` 底座就绪（carried from 2026-07-06）
11. **走查余账**：P1-5 Phase 可配置性待 omar；P2 余 6（排序死维度/UsageRecord 清理/魔法数/空态 CTA/toast 单例/ADR-011）；Macro 网格末行 auto-fit 拉伸宽卡（老行为，未修）（carried from 2026-07-06）
12. **真机复验批次**：HotkeyBanner 观感 / updater 进度条 / 空子阶段 muted 列 / scene.color / 暗 band / light 主题 / SettingsModal 焦点 / ErrorBoundary 视觉（carried from 2026-07-02/06）
13. **CLAUDE-DESIGN v0.2 重传** + **v0.1.0 发布时机**：omar 外部依赖（carried from 2026-07-02）
14. **评审遗留**：ipc-contract 扩扫 / `--color-danger` 补 design-spec 上游（carried from 2026-07-02）
15. **PRD §6.1 Modifier soft-delete vs 硬删矛盾** + 06-prd `status: pre-code` 僵尸待 omar（carried from 2026-07-02）
16. **ai-dev-lifecycle 仓收尾**：提交 `/walkthrough` 收编 3 文件改动（carried from 2026-07-06）

## Risks & Decisions
- **待 omar 裁 ×3**（沿上轮）：Modifier 复制 UsageSource 复用 `"macro_area"` 是否新增枚举；Scene 删除无级联（非空即拒 vs 预期级联）；scene.color 用户内容色
- 未分组转正为「新写链路」但仅组合现有两 IPC，判定不触 constitution，未开 ADR（若 omar 认为属产品语义变更需补 ADR-021 补遗）
- bench-c1 为全仓首个 GUI-in-CI job，首跑正确性未知（见 Next 2）
- doc-governance 7 warn（较上轮 6 增 1：`docs/mockups/prompt-hub.html` L5 引用不存在，历史/非本会话）

## Verify
- `pnpm test` → 277/277（30 文件）
- `cargo test --workspace --manifest-path src-tauri/Cargo.toml` → 143 用例
- `pnpm lint` && `pnpm exec prettier --check .` && `pnpm build`
- `node scripts/doc-governance/index.mjs --config doc-governance.config.mjs` → 0 error / 7 warn

## Modified Files
- `a24c7c0`：`src/components/ScenePanel.module.css`（浮层动作簇/sticky 列头/隐藏滚动条/未分组降格）+ `ScenePanel.tsx`
- `4b722c1`：`src/components/ScenePanel.tsx`（promote handler）/ `stores/promptStore.ts`（createSubStage 返回值）/ `__tests__/ScenePanel.test.tsx`（+2 用例 + flake 根治）
- `716bd4c`：`src/components/ScenePanel.tsx`（未分组 label 对齐）
- `HANDOFF.md`（本文件）/ `docs/.metrics/cochange.log`
