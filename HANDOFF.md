---
type: handoff
project: prompt-hub
created: 2026-05-22
updated: 2026-06-03
status: active
audience: [ai]
description: M-X.1 fully closed. All 4 promote arms implemented; Modifier/Macro field-mapping decided (iii) — group_kind supplied at promote time via PromoteOptions, phase_id/scene_id dropped. Two pre-existing PRD/code divergences fixed (promote soft-delete vs DELETE, deny_unknown_fields unusable). §7 ripple complete: PRD v0.8, features v0.4, ADR-015 补遗-1/-2, CLAUDE.md §7. cargo test --workspace 30/30 + clippy + fmt green. Next: /review + commit, then M-X.2 MCP server.
related:
  - CLAUDE
  - mcp-write-pipeline
  - 06-prd
  - 015-expose-mcp-write-pipeline
---

# Handoff — M-X.1 收口（promote 4 arm 全实现 + §7 涟漪完成）

<!-- Generated by /checkpoint at 2026-06-03 -->

## Objective

关闭 M-X.1（plan `docs/plans/mcp-write-pipeline.md#§8`）：基于项目"手动挡/方向盘在人手里"哲学，替 omar 拍板 Modifier/Macro promote 字段映射（决策 iii），补齐 repo-write 两个 promote arm，删除 `PromoteUnsupported` stub，并走方法论 §7 八步把决策涟漪进 PRD / features / ADR / CLAUDE.md。

## Completed

- **promote 4 arm 全实现**（`crates/repo-write/src/promote.rs`）：
  - 新增 `PromoteOptions { override_payload?, group_kind? }` struct，替换原裸 `override_payload: Option<_>` 参数（避免双 Option 脚枪 + 自文档化 promote-time 输入）。
  - Modifier arm：`group_kind` 来自 `opts.group_kind`（omar 手选，决策 iii），缺失返回 `RepoError::PromoteMissingField { target_type, field }`；payload 的 phase_id/scene_id 丢弃。
  - Macro arm：phase_id 丢弃、按 scene_id 归档；native=0 / expand_from=NULL / role=task=NULL（满足表 CHECK）。
  - 测试：原 `promote_modifier_and_macro_are_unsupported_pending_decision` 删除 → 改为 3 个：`promote_modifier_requires_group_kind`（缺 group_kind 被拒、draft 仍 pending、0 行写入）/ `promote_modifier_inserts_row_with_group_kind` / `promote_macro_inserts_non_native_row`。
- **asset_repo.rs**：新增 `insert_modifier(name, content, group_kind)` + `insert_macro(name, content, scene_id?)`。
- **error.rs**：`PromoteUnsupported(String)` → `PromoteMissingField { target_type, field }`。
- **§7 八步涟漪**：
  - PRD v0.7→v0.8：§6.2 promoted Composition 例外注 / §10.1.2 Modifier·Macro 字段映射 + serde 约束修正（deny_unknown_fields 不可用，改 promote 时重反序列化）/ §10.1.4 promote 后软删 discarded（非 DELETE）+ 幂等性改 DraftNotPending / §10.2 PromoteOptions + group_kind step / §10.3 IPC 入参加 group_kind?。
  - features v0.3→v0.4：drafts 数据层 + workspace 4 crate → `done`；prompt-hub-mcp binary + promote 跨表事务 → `in-progress`。
  - ADR-015 补遗-1（field-mapping 决策 iii 含哲学溯因）+ 补遗-2（两处文档/代码对齐）。
  - CLAUDE.md §7 状态指针刷新（阶段=已收口 / 30 测试 / ADR 补遗）。
- **验证**：`cargo test --workspace` 30/30（21 repo-core + 9 repo-write）；clippy `--workspace --all-targets -D warnings` 0；`cargo fmt --check` clean。

## In Progress

- 无。M-X.1 代码 + 文档全部收口，**尚未 commit**（本 checkpoint 提交）。

## Next Actions

1. **`/review` 审本批 diff**（7 文件：3 代码 + 3 文档 + CLAUDE.md），确认无误后本 checkpoint commit 即为正式记录（或按需追加 feat commit）。
2. 进 **M-X.2 MCP server 骨架**：`crates/prompt-hub-mcp` 从 skeleton → `rmcp = "=1.7"` stdio + 14 tool 完整实现（5 CRUD + 3 helpers + 6 read）+ spawn 进程 JSON-RPC 端到端测试。提纲见 [[mcp-write-pipeline#§8]] M-X.2。
3. **promote IPC 层接线**（M-X.2 内）：`src-tauri/src` 加 `promote_draft` Tauri command，入参含 `group_kind?`（对齐 PRD §10.3）；当前 repo 层已就绪、IPC 未接。
4. 并行待办：ADR-012 Phase 5 `pnpm tauri dev` 6 步视觉 diff + bench 脚本 + M0-4 Developer ID 签名 spike。

## Risks & Decisions

- **决策 iii 已落地（不可逆点已过）**：Modifier group_kind 由 promote UI 手选。M-X.2 接 IPC + M-X.3 接 UI 时，promote 草稿 tab **必须**为 Modifier 提供 group_kind 四象限选择控件（cognition/action/delivery/constraint），否则 Modifier 永远 promote 不了。这是 UI 硬需求，已在 PRD §10.2 / §10.3 标注。
- **API 破坏面**：`promote_draft` 第三参 `Option<DraftPayload>` → `PromoteOptions`。当前**无任何 Tauri/外部调用点**（grep `src-tauri/src` 无 promote），仅 repo-write 内部测试受影响，已全改。M-X.2 接 IPC 时按新签名写即可。
- **insert_alignment_phrase is_default 约束**：`idx_alignment_phrase_one_default_per_phase` 每 phase 至多一个 default，seeded phase 已各有 default → promote is_default=true 到 seeded phase 会 UNIQUE 冲突。测试用 is_default=false 规避（未变）。
- **trybuild stderr 脆性**：rustc 升级可能改诊断文案；无 toolchain pin / CI，本地 `TRYBUILD=overwrite cargo test -p prompt-hub-mcp` 重生成（未变）。

## Verify

```bash
cd /Users/apple/WeChatProjects/prompt-hub/src-tauri
cargo test --workspace                                  # 期望 30/30（21 repo-core + 9 repo-write）
cargo clippy --workspace --all-targets -- -D warnings  # 0 警告
cargo fmt --check                                       # clean
git log --oneline -1                                    # 本 checkpoint commit
```

## Modified Files

- `src-tauri/crates/repo-write/src/promote.rs` — PromoteOptions + Modifier/Macro arm + 3 新测试
- `src-tauri/crates/repo-write/src/asset_repo.rs` — insert_modifier / insert_macro
- `src-tauri/crates/repo-core/src/error.rs` — PromoteUnsupported → PromoteMissingField
- `docs/design/06-prd.md` — v0.8：§6.2 / §10.1.2 / §10.1.4 / §10.2 / §10.3 + 修订记录
- `docs/design/07-features.md` — v0.4：§3.7 状态回写 + 修订记录
- `docs/adr/015-expose-mcp-write-pipeline.md` — 补遗-1（决策 iii）+ 补遗-2（文档/代码对齐）
- `CLAUDE.md` — §7 当前状态指针刷新
